---
phase: 04-education-content
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - banking_api_ui/src/components/education/RFCIndexPanel.js
  - banking_api_ui/src/components/tour/DemoTourModal.js
  - banking_api_ui/src/components/tour/DemoTourModal.css
  - banking_api_ui/src/context/DemoTourContext.js
  - banking_api_ui/src/components/EducationBar.js
  - banking_api_ui/src/App.js
autonomous: true
requirements:
  - EDU-03
  - EDU-04

must_haves:
  truths:
    - "RFC 9700 appears in RFCIndexPanel alongside RFC 8693, 9396, 7519, OIDC CIBA"
    - "A 'Start Tour' button in EducationBar opens the guided demo tour"
    - "The tour steps through all 3 auth flows with narration and Next/Previous navigation"
    - "Presenter can advance through the full tour without leaving the browser"
    - "npm run build exits 0 after changes"
  artifacts:
    - path: "banking_api_ui/src/components/education/RFCIndexPanel.js"
      provides: "RFC 9700 row added to index"
    - path: "banking_api_ui/src/components/tour/DemoTourModal.js"
      provides: "Guided demo tour — step-by-step modal overlay"
    - path: "banking_api_ui/src/context/DemoTourContext.js"
      provides: "Tour state (step, isOpen, start, next, prev, close)"
    - path: "banking_api_ui/src/components/EducationBar.js"
      provides: "Start Tour button wired to DemoTourContext"
    - path: "banking_api_ui/src/App.js"
      provides: "DemoTourProvider + DemoTourModal mounted at app root"
  key_links:
    - from: "EducationBar Start Tour button"
      to: "DemoTourContext.start()"
      via: "useDemoTour hook"
      pattern: "useDemoTour|DemoTourContext"
    - from: "DemoTourModal Next button"
      to: "next step"
      via: "DemoTourContext.next()"
      pattern: "tour.next"
---

<objective>
Complete EDU-03 (RFC 9700 in reference index) and implement EDU-04 (guided demo tour — linear 5-min conference walkthrough).

The guided tour is a floating modal overlay that sequences the 3 auth flows and token exchange showcase with narrated steps. A presenter runs through it start to finish with Next/Previous buttons — no manual navigation or switching between contexts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@banking_api_ui/src/components/education/RFCIndexPanel.js
@banking_api_ui/src/components/EducationBar.js
@banking_api_ui/src/App.js
@banking_api_ui/src/context/EducationUIContext.js

<interfaces>
<!-- RFCIndexPanel ROWS array — add after RFC 9396 entry: -->
{ rfc: 'RFC 9700', name: 'OAuth 2.0 Security Best Current Practice', app: 'Security baseline for all OAuth flows in this demo', href: 'https://datatracker.ietf.org/doc/html/rfc9700', panel: null, tab: null },

<!-- DemoTourContext API: -->
const DemoTourContext = {
  step: number,        // current step index (0-based)
  total: number,       // total steps
  isOpen: boolean,
  start: () => void,   // open tour at step 0
  next: () => void,
  prev: () => void,
  close: () => void,
  goTo: (n) => void,
};
export function useDemoTour() { return useContext(DemoTourContext); }

<!-- Tour steps (9 steps total — maps all 3 auth flows + token exchange): -->
Step 0: Introduction
  title: "BX Finance AI Banking Demo"
  body: "This 5-minute tour walks through three distinct authentication flows and shows RFC 8693 token exchange in action."
  action: null

Step 1: Customer Login (Authorization Code + PKCE)
  title: "Flow 1 — Customer Login"
  body: "A customer logs in via Authorization Code + PKCE. Their tokens stay on the server. The browser only gets a session cookie."
  action: { label: "Log in as customer", route: "/marketing" }

Step 2: Dashboard — token in the BFF
  title: "Tokens held server-side"
  body: "After login, the customer reaches the dashboard. No token appears in the browser — the BFF holds it securely."
  action: { label: "View dashboard", route: "/dashboard" }

Step 3: Agent tool call — token exchange
  title: "Agent makes a tool call"
  body: "When the agent runs a banking tool, the BFF performs RFC 8693 token exchange — narrowing the audience to the MCP server."
  action: { label: "Open agent panel", hint: "Type 'show my accounts' in the agent" }

Step 4: Flow 2 — CIBA (phone approval)
  title: "Flow 2 — CIBA / Out-of-band approval"
  body: "For high-value transfers, the agent triggers a CIBA push. The user approves on their device. The agent unblocks automatically."
  action: { label: "Try a transfer over $500", hint: "Set CIBA threshold in demo settings, then transfer" }

Step 5: CIBA approval poll
  title: "Polling for approval"
  body: "The app polls /api/ciba/status every 5 seconds. When the user approves (email link or push), the token arrives and the agent continues."
  action: null

Step 6: Flow 3 — Agent HITL auth challenge
  title: "Flow 3 — Inline auth challenge"
  body: "If the agent's session expires mid-flow, an inline login prompt appears. After the user re-authenticates, the agent continues automatically."
  action: { hint: "This triggers automatically on session expiry" }

Step 7: Token inspector — 1-exchange vs 2-exchange
  title: "Token Exchange showcase"
  body: "Toggle the exchange mode to see the difference: 1-exchange (user token only) vs 2-exchange (user + agent actor — act claim appears)."
  action: { label: "Open token inspector", hint: "Look for the 'Exchange mode' toggle" }

Step 8: Tour complete
  title: "Tour complete"
  body: "You've seen all 3 auth flows and RFC 8693 token exchange. Use the EducationBar (top-right menu) to explore any concept in depth."
  action: { label: "Explore education panels", hint: "Click the menu icon in the top-right" }

<!-- DemoTourModal design: -->
- Position: fixed, bottom-right corner (non-intrusive)
- Width: clamp(280px, 380px, 90vw)
- Step indicator: "Step {n+1} of {total}" in header (small, muted)
- Progress bar: thin strip at top of modal, colored with --app-primary-red
- Prev button: disabled at step 0; "← Back" otherwise
- Next button: "Next →" (steps 0..7); "Finish" at step 8
- Action button (optional): outline CTA if step has action.label
- Close: ✕ top-right; also Escape key
- Animation: slide up from bottom on open, slide down on close (CSS transition)
- Brand: white card, --app-primary-red progress bar, system font
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RFC 9700 to RFCIndexPanel</name>
  <files>banking_api_ui/src/components/education/RFCIndexPanel.js</files>
  <action>
In RFCIndexPanel.js, add to the ROWS array after the RFC 9396 entry:

```javascript
{ rfc: 'RFC 9700', name: 'OAuth 2.0 Security Best Current Practice', app: 'Security baseline — PKCE required, implicit flow banned, redirect URI exact match', href: 'https://datatracker.ietf.org/doc/html/rfc9700', panel: null, tab: null },
```

(The `handleOpenPanel` handler already handles `panel: null` rows gracefully — they have no clickable link in the first column. Verify this before adding.)
  </action>
  <verify>
    <automated>grep -n "RFC 9700" banking_api_ui/src/components/education/RFCIndexPanel.js</automated>
  </verify>
  <done>RFC 9700 row present in ROWS array</done>
</task>

<task type="auto">
  <name>Task 2: Create DemoTourContext</name>
  <files>banking_api_ui/src/context/DemoTourContext.js</files>
  <action>
Create banking_api_ui/src/context/DemoTourContext.js.

Export:
- `DemoTourContext` (React.createContext)
- `DemoTourProvider` component (wraps children, manages step + isOpen state)
- `useDemoTour` hook (throws if used outside provider)

State: { step: 0, isOpen: false }
Actions: start() → setStep(0) + setIsOpen(true)
         next() → setStep(s => Math.min(s+1, STEPS.length-1))
         prev() → setStep(s => Math.max(s-1, 0))
         close() → setIsOpen(false)
         goTo(n) → setStep(n)

Export TOUR_STEPS array (the 9 steps from the interfaces section above) as a named export from this file so DemoTourModal can import it.

Each step shape:
```typescript
{ title: string, body: string, action?: { label?: string, hint?: string, route?: string } }
```

Use React.useMemo for the context value to avoid unnecessary re-renders.
  </action>
  <verify>
    <automated>node -e "const {createContext}=require('react');console.log('react ok')" && grep -n "useDemoTour\|DemoTourProvider\|TOUR_STEPS" banking_api_ui/src/context/DemoTourContext.js | head -10</automated>
  </verify>
  <done>DemoTourContext.js exports DemoTourProvider, useDemoTour, TOUR_STEPS; TOUR_STEPS has 9 items</done>
</task>

<task type="auto">
  <name>Task 3: Create DemoTourModal.js and DemoTourModal.css</name>
  <files>
    banking_api_ui/src/components/tour/DemoTourModal.js
    banking_api_ui/src/components/tour/DemoTourModal.css
  </files>
  <action>
Create banking_api_ui/src/components/tour/ directory and two files.

**DemoTourModal.js:**
- Reads state from useDemoTour()
- Renders nothing when !isOpen
- Fixed position bottom-right (right: 1.5rem, bottom: 1.5rem, z-index: 1200)
- Structure:
  ```
  [thin red progress bar]
  [header: "Step N of 9"  ✕]
  [title h3]
  [body p]
  [optional action button — outline, opens route or shows hint as tooltip/text]
  [footer: "← Back"  "Next →" / "Finish"]
  ```
- Escape key listener: calls close()
- Progress bar width: `${((step + 1) / total) * 100}%`, background: var(--app-primary-red)
- Prev button: disabled (opacity 0.4, cursor default) at step 0
- Next/Finish: primary style at step === total-1 label becomes "Finish", calls close()
- Action button: rendered as a secondary link-style button; if action.route exists use React Router Link; if not, show hint text below body

**DemoTourModal.css:**
- `.demo-tour-modal` — white card, border-radius 12px, box-shadow 0 8px 32px rgba(0,0,0,0.18)
- `.demo-tour-progress` — height 4px, background var(--app-primary-red), transition width 0.3s ease
- `.demo-tour-header` — display flex, align-items center, justify-content space-between, padding 0.75rem 1rem 0
- `.demo-tour-close` — background none, border none, cursor pointer, font-size 1.1rem, color #6b7280
- `.demo-tour-body` — padding 0.5rem 1rem 0.75rem
- `.demo-tour-title` — font-size 1rem, font-weight 600, margin 0 0 0.5rem
- `.demo-tour-text` — font-size 0.875rem, color #374151, line-height 1.5
- `.demo-tour-footer` — display flex, gap 0.5rem, padding 0.75rem 1rem, border-top 1px solid #f1f5f9
- `.demo-tour-btn-prev` — outline button style
- `.demo-tour-btn-next` — filled red button style (var(--app-primary-red))
- `.demo-tour-step-label` — font-size 0.75rem, color #9ca3af
  </action>
  <verify>
    <automated>ls banking_api_ui/src/components/tour/</automated>
  </verify>
  <done>DemoTourModal.js and DemoTourModal.css exist in banking_api_ui/src/components/tour/</done>
</task>

<task type="auto">
  <name>Task 4: Mount tour in App.js and add Start Tour to EducationBar</name>
  <files>
    banking_api_ui/src/App.js
    banking_api_ui/src/components/EducationBar.js
  </files>
  <action>
**App.js:**
1. Import DemoTourProvider: `import { DemoTourProvider } from './context/DemoTourContext';`
2. Import DemoTourModal: `import DemoTourModal from './components/tour/DemoTourModal';`
3. Wrap the existing `<EducationUIProvider>` with `<DemoTourProvider>` (DemoTourProvider should be outermost of the two)
4. Place `<DemoTourModal />` inside the app render tree, beside `<EducationPanelsHost />` (both are portal-style global components)

**EducationBar.js:**
1. Import useDemoTour: `import { useDemoTour } from '../context/DemoTourContext';`
2. Inside the component, add: `const { start: startTour } = useDemoTour();`
3. In the hamburger menu (non-demo mode), add a "Start Demo Tour" button at the TOP of the Learn section:
   ```jsx
   <button type="button" className="edu-bar-item edu-bar-item--highlight" onClick={() => { startTour(); close(); }}>
     ▶ Start 5-min demo tour
   </button>
   ```
   (Use a divider or small gap to visually separate from the spec/learn entries)

Do NOT move or reshuffle existing EducationBar entries. Add only at the top of the learn section.
  </action>
  <verify>
    <automated>grep -n "DemoTourProvider\|DemoTourModal\|startTour\|start-demo-tour" banking_api_ui/src/App.js banking_api_ui/src/components/EducationBar.js | head -10</automated>
  </verify>
  <done>DemoTourProvider wraps app in App.js; DemoTourModal mounted; EducationBar has Start Tour button</done>
</task>

<task type="auto">
  <name>Task 5: Build verification</name>
  <files>banking_api_ui/</files>
  <action>Run cd banking_api_ui && npm run build — must exit 0.</action>
  <verify>
    <automated>cd banking_api_ui && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build exits 0</done>
</task>

</tasks>

<verification>
1. `grep -n "RFC 9700" banking_api_ui/src/components/education/RFCIndexPanel.js` → row present
2. `grep -n "TOUR_STEPS" banking_api_ui/src/context/DemoTourContext.js` → 9 steps (length === 9)
3. `ls banking_api_ui/src/components/tour/` → DemoTourModal.js DemoTourModal.css
4. `grep -n "DemoTourProvider" banking_api_ui/src/App.js` → present
5. `grep -n "startTour\|start-demo-tour\|Start.*tour\|demo tour" banking_api_ui/src/components/EducationBar.js` → present
6. `cd banking_api_ui && npm run build` → exit 0
</verification>

<success_criteria>
- RFCIndexPanel has RFC 9700 row (EDU-03 requirement satisfied: 8693, 9396, 7519, 9700, OIDC CIBA all present)
- DemoTourContext with 9-step tour (EDU-04)
- DemoTourModal: bottom-right fixed card, progress bar, Next/Prev nav, all 9 steps render
- "Start 5-min demo tour" button in EducationBar hamburger
- DemoTourProvider + DemoTourModal mounted globally in App.js
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-education-content/04-03-SUMMARY.md`
</output>
