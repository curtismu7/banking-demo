---
phase: 04-education-content
plan: 04
type: execute
wave: 3
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - banking_api_ui/src/components/EmbeddedAgentDock.js
  - banking_api_ui/src/components/LandingPage.css
  - banking_api_ui/src/components/AgentFlowDiagramPanel.js
  - banking_api_ui/src/components/AgentFlowDiagramPanel.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "EmbeddedAgentDock on /marketing uses marketing page color palette and typography"
    - "Dashboard dock is visually unchanged (marketing variant is isolated)"
    - "AgentFlowDiagramPanel has consistent spacing and typography matching dashboard design"
    - "npm run build exits 0 after changes"
  artifacts:
    - path: "banking_api_ui/src/components/LandingPage.css"
      provides: "Marketing-surface EmbeddedAgentDock overrides"
    - path: "banking_api_ui/src/components/AgentFlowDiagramPanel.css"
      provides: "Spacing/typography fixes identified during audit"
  key_links:
    - from: "EmbeddedAgentDock on /marketing"
      to: "LandingPage.css dock slot override"
      via: ".App--marketing-page .embedded-agent-dock"
      pattern: "App--marketing-page"
---

<objective>
UI consistency audit + marketing agent dock polish.

Two tasks:
1. Make the EmbeddedAgentDock on /marketing match the LandingPage design language (marketing-surface variant, isolated from dashboard)
2. Light cross-SPA audit: identify and fix the top spacing/typography/color inconsistencies introduced by previous phases (agent flow diagram panel, token inspector, education panels)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@banking_api_ui/src/components/EmbeddedAgentDock.js
@banking_api_ui/src/components/LandingPage.js
@banking_api_ui/src/components/LandingPage.css
@banking_api_ui/src/components/AgentFlowDiagramPanel.js
@banking_api_ui/src/App.css
@banking_api_ui/src/index.css

<interfaces>
<!-- Marketing page isolation guard (App.css): -->
.App--marketing-page .main-content { background: white; }

<!-- LandingPage.css already has a dock slot section: -->
/* Slot for EmbeddedAgentDock (portal) — above closing CTA */
(around line 1002 in LandingPage.css)

<!-- isMarketingEmbeddedDockSurface utility — guards marketing surface styling in EmbeddedAgentDock.js -->
// Use CSS class override instead of JS conditional where possible to stay minimal

<!-- Brand tokens: -->
--app-primary-red:    #b91c1c
--brand-dashboard-header-start: #1e3a8a  (dashboard blue — NOT for marketing dock)
Landing page typical bg: white / #f8fafc
Landing page hero text: dark navy / #1e293b
Landing page CTA style: rounded pill, red (#b91c1c), white text

<!-- EmbeddedAgentDock current styling note: -->
The dock uses dashboard-centric styles (blue header, compact font).
On /marketing, it should feel like a natural product widget:
- White background (not gray)
- Navy heading (not blue gradient)
- Subtle shadow (not harsh border)
- Slightly larger font for readability in marketing context
- The dock header/title should say "Try the AI Banking Assistant" on /marketing
  (add a prop or use isMarketingEmbeddedDockSurface utility)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit — read current EmbeddedAgentDock and AgentFlowDiagramPanel CSS</name>
  <files>
    banking_api_ui/src/components/EmbeddedAgentDock.js
    banking_api_ui/src/components/AgentFlowDiagramPanel.css
  </files>
  <action>
Read both files to identify:
1. What CSS classes the EmbeddedAgentDock uses for its container and header
2. What class names AgentFlowDiagramPanel uses for its header, body, server events list
3. Any hardcoded colors or font sizes that conflict with the design system tokens

Capture findings to use in Tasks 2 and 3.
  </action>
  <verify>
    <automated>wc -l banking_api_ui/src/components/AgentFlowDiagramPanel.css banking_api_ui/src/components/EmbeddedAgentDock.js</automated>
  </verify>
  <done>Both files read; class names and hardcoded values identified</done>
</task>

<task type="auto">
  <name>Task 2: Marketing dock CSS override in LandingPage.css</name>
  <files>banking_api_ui/src/components/LandingPage.css</files>
  <action>
Extend the existing dock slot section in LandingPage.css with marketing-surface overrides.

Use the `.App--marketing-page` parent class guard (already set by App.js on /marketing route) so these overrides ONLY apply on the marketing page — zero risk of dashboard regression.

Add under the existing `/* Slot for EmbeddedAgentDock */` comment:

```css
/* ── Marketing surface: EmbeddedAgentDock variant ────────────────────── */
/* Scoped to .App--marketing-page so dashboard dock is completely unaffected */
.App--marketing-page .embedded-agent-dock {
  background: #ffffff;
  border: 1px solid #e2e8f0;
  border-radius: 12px;
  box-shadow: 0 4px 24px rgba(0, 0, 0, 0.10);
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', sans-serif;
}

.App--marketing-page .embedded-agent-dock__header {
  background: #1e293b;          /* landing page dark navy — not dashboard blue gradient */
  border-radius: 12px 12px 0 0;
  padding: 0.875rem 1rem;
}

.App--marketing-page .embedded-agent-dock__title {
  font-size: 0.9375rem;
  font-weight: 600;
  color: #ffffff;
  letter-spacing: -0.01em;
}

.App--marketing-page .embedded-agent-dock__body {
  padding: 1rem;
  font-size: 0.9375rem;
  line-height: 1.55;
  color: #1e293b;
}

.App--marketing-page .embedded-agent-dock__input-row input,
.App--marketing-page .embedded-agent-dock__input-row textarea {
  border: 1px solid #e2e8f0;
  border-radius: 8px;
  font-size: 0.9375rem;
  padding: 0.625rem 0.75rem;
  background: #f8fafc;
}

.App--marketing-page .embedded-agent-dock__send-btn {
  background: linear-gradient(180deg, #dc2626 0%, #b91c1c 100%);
  border: 1px solid #991b1b;
  border-radius: 8px;
  color: #fff;
  font-weight: 600;
}

.App--marketing-page .embedded-agent-dock__send-btn:hover {
  background: linear-gradient(180deg, #b91c1c 0%, #991b1b 100%);
}
```

NOTE: Use only class names that actually exist in EmbeddedAgentDock.js (confirmed in Task 1). Adjust selectors to match real class names. If a class name doesn't exist, add a wrapper or use a data-attribute pattern rather than inventing class names.
  </action>
  <verify>
    <automated>grep -n "App--marketing-page.*embedded-agent\|App--marketing-page.*dock" banking_api_ui/src/components/LandingPage.css | head -5</automated>
  </verify>
  <done>LandingPage.css has marketing-scoped dock overrides using real class names from EmbeddedAgentDock.js; no new classes added to JS files</done>
</task>

<task type="auto">
  <name>Task 3: Light AgentFlowDiagramPanel audit fixes</name>
  <files>banking_api_ui/src/components/AgentFlowDiagramPanel.css</files>
  <action>
Read AgentFlowDiagramPanel.css and identify any of these specific issues:
- Font sizes smaller than 0.75rem on non-label text (hard to read)
- Hardcoded hex colors that should use CSS variables (--app-primary-red, etc.)
- Missing or inconsistent border-radius on cards/panels (should be 8px or 12px)
- Overflow issues: tables or code blocks that have no max-width/overflow-x: auto

Fix ONLY clear, concrete issues found. Do not redesign the panel.
Maximum 8-10 CSS rule changes. If the file looks already consistent, make no changes and note that in the summary.
  </action>
  <verify>
    <automated>cd banking_api_ui && npm run build 2>&1 | grep -E "ERROR|error" | grep -v "node_modules" | head -5 || echo "NO_ERRORS"</automated>
  </verify>
  <done>AgentFlowDiagramPanel.css: specific issues fixed (or noted as already consistent); build passes</done>
</task>

<task type="auto">
  <name>Task 4: Build verification + no console errors check</name>
  <files>banking_api_ui/</files>
  <action>
1. Run cd banking_api_ui && npm run build — must exit 0.
2. No new PropTypes warnings in built output for EmbeddedAgentDock, LandingPage, or AgentFlowDiagramPanel.
  </action>
  <verify>
    <automated>cd banking_api_ui && npm run build 2>&1 | tail -8</automated>
  </verify>
  <done>Build exits 0; no new warnings for touched components</done>
</task>

</tasks>

<verification>
1. `grep -c "App--marketing-page.*dock\|App--marketing-page.*embedded" banking_api_ui/src/components/LandingPage.css` → ≥ 3 overrides
2. `cd banking_api_ui && npm run build` → exit 0
3. `grep -n "dashboard\|1e40af\|1e3a8a" banking_api_ui/src/components/AgentFlowDiagramPanel.css | wc -l` → 0 (no dashboard-blue hardcodes in diagram panel)
4. No changes to EmbeddedAgentDock.js JavaScript logic (CSS-only for marketing)
5. No changes to dashboard route components
</verification>

<success_criteria>
- Marketing dock overrides in LandingPage.css: white bg, dark navy header, red CTA
- Dashboard dock: visually unchanged
- AgentFlowDiagramPanel: top-priority readability/consistency issues resolved (or confirmed already clean)
- Build passes
- Zero changes to dashboard components (dashboard regression guard respected)
</success_criteria>

<output>
After completion, create `.planning/phases/04-education-content/04-04-SUMMARY.md`

Also move both todo files to completed:
- mv .planning/todos/pending/ui-consistency-audit.md .planning/todos/completed/
- mv .planning/todos/pending/marketing-agent-ui-match.md .planning/todos/completed/
</output>
