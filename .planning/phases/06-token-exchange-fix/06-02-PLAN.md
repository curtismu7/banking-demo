---
phase: 06-token-exchange-fix
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - banking_api_server/src/__tests__/agentMcpTokenService.test.js
  - banking_api_server/src/__tests__/token-exchange-pingone.integration.test.js
autonomous: true
requirements:
  - TOKEN-FIX-02

must_haves:
  truths:
    - "1-exchange path: resolveMcpAccessTokenWithEvents issues a token with act.client_id matching the agent OAuth client"
    - "2-exchange path: _performTwoExchangeDelegation emits 4 events (agent-actor, exchange1, mcp-actor, exchange2) and returns a token with nested act.act"
    - "Security: user access token is NEVER the return value when MCP_RESOURCE_URI is set — only the exchanged token is returned"
    - "Security: when ff_two_exchange_delegation is ON and credentials missing, returns HTTP 503 with missingVars list, never a partial token"
    - "Security: acted claim sub on 1-exchange matches AGENT_OAUTH_CLIENT_ID; act.act.sub on 2-exchange matches AI_AGENT_CLIENT_ID"
    - "Integration smoke: live test scaffold exists and is guarded by RUN_PINGONE_TOKEN_EXCHANGE=true AND required env vars"
  artifacts:
    - path: "banking_api_server/src/__tests__/agentMcpTokenService.test.js"
      provides: "2-exchange delegation tests + security property tests"
      min_lines: 500
    - path: "banking_api_server/src/__tests__/token-exchange-pingone.integration.test.js"
      provides: "Live integration tests for both 1-exchange and 2-exchange (opt-in)"
      contains: "RUN_PINGONE_TOKEN_EXCHANGE"
  key_links:
    - from: "resolveMcpAccessTokenWithEvents"
      to: "_performTwoExchangeDelegation"
      via: "ff_two_exchange_delegation flag + sessionExchangeMode"
      pattern: "ff_two_exchange_delegation|sessionExchangeMode"
    - from: "_performTwoExchangeDelegation"
      to: "performTokenExchangeAs"
      via: "two sequential RFC 8693 exchange calls"
      pattern: "performTokenExchangeAs"
    - from: "test assertions"
      to: "act claim structure"
      via: "decoded final token.claims.act.act.sub"
      pattern: "act\\.act\\.sub|nested.*act"
---

<objective>
Comprehensive tests for both RFC 8693 exchange paths and security properties — enterprise-grade coverage for TOKEN-FIX-02.

Purpose: The 1-exchange path has good unit test coverage but no tests for: `act` claim presence/structure, user token not forwarded, audience narrowing. The 2-exchange path (`_performTwoExchangeDelegation`) has zero test coverage. The live integration file only has one live test (and it's skipped in CI). This plan closes all gaps and adds security property assertions so regressions are caught automatically.

Output: ~30 new tests in agentMcpTokenService.test.js + enhanced live integration file.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/PINGONE_MAY_ACT_ONE_TOKEN_EXCHANGE.md
@docs/PINGONE_MAY_ACT_TWO_TOKEN_EXCHANGES.md
@.planning/phases/06-token-exchange-fix/06-01-SUMMARY.md

<interfaces>
<!-- Key facts for test authoring — do not explore further -->

<!-- 2-exchange flag resolution in resolveMcpAccessTokenWithEvents: -->
```javascript
const ffTwoExchange =
  sessionExchangeMode === 'double' ||
  (sessionExchangeMode == null && (
    configStore.getEffective('ff_two_exchange_delegation') === true ||
    configStore.getEffective('ff_two_exchange_delegation') === 'true'
  ));
if (ffTwoExchange) {
  return await _performTwoExchangeDelegation(
    tokenEvents, userToken, userAccessTokenClaims, effectiveToolScopes, userSub, toolTrigger, mcpResourceUri
  );
}
```

<!-- _performTwoExchangeDelegation env vars needed: -->
```javascript
const aiAgentClientId     = configStore.getEffective('ai_agent_client_id') || process.env.AI_AGENT_CLIENT_ID;
const aiAgentClientSecret = process.env.AI_AGENT_CLIENT_SECRET;
const mcpExchangerClient  = process.env.AGENT_OAUTH_CLIENT_ID;
const mcpExchangerSecret  = process.env.AGENT_OAUTH_CLIENT_SECRET;
// missingVars check: any falsy → 503 + missingVars list
```

<!-- Token chain for 2-exchange: -->
// Step 1: getClientCredentialsTokenAs(aiAgentClientId, aiAgentClientSecret, agentGatewayAud, aiAgentAuthMethod)
// Step 2: performTokenExchangeAs(userToken, agentActorToken, aiAgentClientId, aiAgentClientSecret, intermediateAud, scopes, aiAgentAuthMethod)
// Step 3: getClientCredentialsTokenAs(mcpExchangerClient, mcpExchangerSecret, mcpGatewayAud, mcpExchangerAuthMethod)
// Step 4: performTokenExchangeAs(agentExchangedToken, mcpActorToken, mcpExchangerClient, mcpExchangerSecret, mcpResourceUri, scopes, mcpExchangerAuthMethod)

<!-- Existing mock setup in agentMcpTokenService.test.js (add to, don't replace): -->
```javascript
const mockGetClientCredentialsTokenAs = jest.fn();
const mockPerformTokenExchangeAs = jest.fn();
jest.mock('../../services/oauthService', () => ({
  config: { clientId: 'bff-client-id' },
  getAgentClientCredentialsToken: (...args) => mockGetAgentClientCredentialsToken(...args),
  performTokenExchange: (...args) => mockPerformTokenExchange(...args),
  performTokenExchangeWithActor: (...args) => mockPerformTokenExchangeWithActor(...args),
  // ADD:
  getClientCredentialsTokenAs: (...args) => mockGetClientCredentialsTokenAs(...args),
  performTokenExchangeAs: (...args) => mockPerformTokenExchangeAs(...args),
  getClientCredentialsToken: jest.fn(),  // used by _performTwoExchangeDelegation?
}));
```

<!-- Security properties to assert explicitly: -->
// 1. When exchange succeeds, returned token !== userToken (user token never forwarded)
// 2. When MCP_RESOURCE_URI is set, returned token must be the exchanged token
// 3. act claim on 1-exchange: act.client_id === AGENT_OAUTH_CLIENT_ID
// 4. act on 2-exchange final token: act.sub === mcpExchangerClient, act.act.sub === aiAgentClientId
// 5. tokenEvent 'exchanged-token' audMatches=true when aud matches mcpResourceUri
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 2-exchange delegation unit tests</name>
  <files>banking_api_server/src/__tests__/agentMcpTokenService.test.js</files>
  <behavior>
    - When ff_two_exchange_delegation=true, resolveMcpAccessTokenWithEvents calls getClientCredentialsTokenAs twice (once for AI Agent actor, once for MCP actor)
    - When ff_two_exchange_delegation=true, calls performTokenExchangeAs twice (Exchange #1 and Exchange #2)
    - tokenEvents has events with ids: two-ex-agent-actor, two-ex-exchange1, two-ex-mcp-actor, two-ex-final-token (in that order, failed or success variants)
    - When any required env var is missing (AI_AGENT_CLIENT_ID, AI_AGENT_CLIENT_SECRET, AGENT_OAUTH_CLIENT_ID, AGENT_OAUTH_CLIENT_SECRET), throws error with httpStatus=503 and tokenEvent id two-exchange-not-configured
    - Exchange #1 failure: tokenEvent two-ex-exchange1 has status 'failed', error contains guidance about may_act.sub mismatch
    - Exchange #2 success: returned token is the final token from performTokenExchangeAs call #2, not from call #1
    - Session override: req.session.mcpExchangeMode='double' triggers 2-exchange path even when ff_two_exchange_delegation=false
    - Session override: req.session.mcpExchangeMode='single' does NOT trigger 2-exchange path even when ff_two_exchange_delegation=true
  </behavior>
  <action>
    Add `describe('resolveMcpAccessTokenWithEvents — 2-exchange delegation (ff_two_exchange_delegation)', () => { ... })` block to agentMcpTokenService.test.js.

    **Setup for 2-exchange tests:**
    1. Extend the existing `jest.mock('../../services/oauthService', ...)` to include `getClientCredentialsTokenAs` and `performTokenExchangeAs` mock functions (introduced in mock-variable block at top of file).
    2. In `beforeEach`: set process.env `AI_AGENT_CLIENT_ID`, `AI_AGENT_CLIENT_SECRET`, `AGENT_OAUTH_CLIENT_ID`, `AGENT_OAUTH_CLIENT_SECRET` to fixture values.
    3. Build JWT fixtures for: `agentActorJwt` (aud=agentGateway), `agentExchangedJwt` (aud=mcp-server, act={sub: AI_AGENT_CLIENT_ID}), `mcpActorJwt` (aud=mcpGateway), `finalMcpJwt` (aud=mcpResourceUri, act={sub: MCP_CLIENT_ID, act:{sub: AI_AGENT_CLIENT_ID}}).
    4. `mockGetClientCredentialsTokenAs` returns `agentActorJwt` on first call, `mcpActorJwt` on second call.
    5. `mockPerformTokenExchangeAs` returns `agentExchangedJwt` on first call, `finalMcpJwt` on second call.

    **Test: full happy path**
    - `mockConfigStore.getEffective` returns `ff_two_exchange_delegation: 'true'` and `mcp_resource_uri: 'https://mcp-server.pingdemo.com'`
    - Call `resolveMcpAccessTokenWithEvents(req, 'get_my_accounts')`
    - Assert `result.token === finalMcpJwt` (not userToken, not agentExchangedJwt)
    - Assert `mockPerformTokenExchangeAs.mock.calls.length === 2`
    - Assert `mockGetClientCredentialsTokenAs.mock.calls.length === 2`
    - Assert event ids in order include: two-ex-agent-actor, two-ex-exchange1, two-ex-mcp-actor, two-ex-final-token

    **Test: preflight — missing AI_AGENT_CLIENT_ID**
    - Delete `process.env.AI_AGENT_CLIENT_ID`
    - Expect `resolveMcpAccessTokenWithEvents` to reject with `httpStatus=503`
    - Assert the error's `tokenEvents` includes an event with `id: 'two-exchange-not-configured'`

    **Test: Exchange #1 failure**
    - `mockPerformTokenExchangeAs.mockRejectedValueOnce(...)` with a PingOne-shaped error
    - Assert rejection, assert tokenEvents includes event with id 'two-ex-exchange1' and status 'failed'

    **Test: session.mcpExchangeMode='double' triggers 2-exchange path**
    - Set `req.session.mcpExchangeMode = 'double'` and `ff_two_exchange_delegation = false`
    - Assert `mockPerformTokenExchangeAs` was called (2-exchange path taken)

    **Test: session.mcpExchangeMode='single' skips 2-exchange**
    - Set `req.session.mcpExchangeMode = 'single'` and `ff_two_exchange_delegation = true`
    - Assert `mockPerformTokenExchangeAs` was NOT called; `mockPerformTokenExchangeWithActor` WAS called
  </action>
  <verify>
    <automated>cd /Users/cmuir/P1Import-apps/Banking/banking_api_server && CI=true npx jest --testPathPattern=agentMcpTokenService --no-coverage --forceExit 2>&1 | tail -20</automated>
  </verify>
  <done>All agentMcpTokenService tests pass including new 2-exchange block. At least 8 new tests added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Security property tests + live integration coverage</name>
  <files>
    banking_api_server/src/__tests__/agentMcpTokenService.test.js,
    banking_api_server/src/__tests__/token-exchange-pingone.integration.test.js
  </files>
  <behavior>
    Security properties (agentMcpTokenService.test.js):
    - User token never returned when MCP_RESOURCE_URI is set and exchange succeeds
    - Exchanged token in tokenEvent 'exchanged-token' has audMatches=true when aud === mcpResourceUri
    - 1-exchange act claim: event 'exchanged-token' has actPresent=true and actDetails containing AGENT_OAUTH_CLIENT_ID
    - 2-exchange final token: event 'two-ex-final-token' claims show nested act (act.act.sub = AI agent client id)
    - ff_skip_token_exchange=true: user token IS returned (bypass path — test is an explicit security opt-out test with comment)

    Integration file (token-exchange-pingone.integration.test.js):
    - Guard: `RUN_PINGONE_TOKEN_EXCHANGE=true` AND required env vars present — rename from RUN_PINGONE_TOKEN_INTEGRATION to match phase naming
    - 1-exchange live test: calls performTokenExchange; asserts 3-part JWT, sub present, aud===mcpResourceUri
    - 2-exchange live test: calls performTokenExchangeWithActor; asserts 3-part JWT, act.sub present, aud===mcpResourceUri
    - getAgentClientCredentialsToken live test: asserts 3-part JWT, aud present
    - All live tests are in a single (live ? describe : describe.skip) block; CI always skips
  </behavior>
  <action>
    **In agentMcpTokenService.test.js** — add `describe('Security properties', () => { ... })` block:

    Test A — User token never forwarded:
    - Mock exchange to return `mockMcpToken`
    - Assert `result.token !== sampleJwtUserAccessToken`
    - Assert `result.token === mockMcpToken`
    - Comment: `// SECURITY: the user's original session token must never be sent to the MCP server`

    Test B — Audience narrowing reflected in tokenEvent:
    - Inspect `result.tokenEvents.find(e => e.id === 'exchanged-token').audMatches`
    - Assert `audMatches === true` when mock MCP token aud matches configured mcpResourceUri
    - Assert `audMatches === false` when mock MCP token aud is a different string (simulate mis-issued token)

    Test C — 1-exchange act claim present:
    - Mock `performTokenExchangeWithActor` to return a JWT with `act: { client_id: 'AGENT_CLIENT_ID' }`
    - Assert `result.tokenEvents.find(e => e.id === 'exchanged-token').actPresent === true`

    Test D — ff_skip_token_exchange bypass (explicit opt-out documentation):
    - Set `ff_skip_token_exchange = 'true'`
    - Assert `result.token === sampleJwtUserAccessToken`
    - Add comment: `// SECURITY OPT-OUT: ff_skip_token_exchange bypasses RFC 8693. Only for dev/demo without PingOne configured.`

    **In token-exchange-pingone.integration.test.js**:
    - Update guard variable name to `RUN_PINGONE_TOKEN_EXCHANGE` (keep backward compat: also check `RUN_PINGONE_TOKEN_INTEGRATION`)
    - Add live test for `performTokenExchangeWithActor`: requires `INTEGRATION_AGENT_ACCESS_TOKEN` env var; asserts 3-part JWT, checks if `act` claim is present (warns if absent, does not fail — exchange may succeed without act if PingOne policy isn't set up)
    - Add live test for `getAgentClientCredentialsToken`: requires `AGENT_OAUTH_CLIENT_ID` + `AGENT_OAUTH_CLIENT_SECRET` env vars; asserts access_token is a 3-part JWT
    - All live tests remain in the `live ? describe : describe.skip` block; no change needed for CI behaviour
  </action>
  <verify>
    <automated>cd /Users/cmuir/P1Import-apps/Banking/banking_api_server && CI=true npx jest --testPathPattern="agentMcpToken|token-exchange-pingone" --no-coverage --forceExit 2>&1 | tail -20</automated>
  </verify>
  <done>All tests pass. Security property block has 4 tests. Integration file has 3 live tests guarded by env var (skipped in CI). No raw userToken ever asserted equal to result.token when MCP_RESOURCE_URI is set.</done>
</task>

</tasks>

<verification>
1. `CI=true npx jest --testPathPattern="agentMcpToken|token-exchange-pingone" --no-coverage --forceExit` — all pass, none unexpected-skip
2. `CI=true npx jest --no-coverage --forceExit 2>&1 | grep -E "Tests:|Suites:"` — 0 failures, 0 unexpected skips
3. Manual: `grep "result.token === sampleJwtUserAccessToken" ...test.js` — only appears in the explicit ff_skip_token_exchange bypass test with the SECURITY OPT-OUT comment
</verification>

<success_criteria>
- All 2-exchange delegation scenarios tested: happy path, preflight failure, Exchange #1 failure, session mode overrides
- Security properties: user token never forwarded assertion, audience narrowing assertion, act claim assertion — all explicitly tested
- Live integration file: 3 guarded tests for 1-exchange, withActor, and agent CC — skipped unless RUN_PINGONE_TOKEN_EXCHANGE=true
- TOKEN-FIX-02: ✅ both 1-exchange and 2-exchange paths are tested end-to-end in unit + documented integration
- Full test suite: 0 regressions on existing 92 tests
</success_criteria>

<output>
After completion, create `.planning/phases/06-token-exchange-fix/06-02-SUMMARY.md`
</output>
