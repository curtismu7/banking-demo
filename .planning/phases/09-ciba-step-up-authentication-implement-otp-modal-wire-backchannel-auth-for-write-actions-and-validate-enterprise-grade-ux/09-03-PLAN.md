---
phase: 09-ciba-step-up-authentication-implement-otp-modal-wire-backchannel-auth-for-write-actions-and-validate-enterprise-grade-ux
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - banking_api_server/config/runtimeSettings.js
  - banking_api_server/services/configStore.js
  - banking_api_ui/src/components/Config.js
  - banking_api_server/routes/transactions.js
autonomous: true
requirements: [CIBA-03]

must_haves:
  truths:
    - "A fresh server start defaults step_up_method to 'email' (not 'ciba')"
    - "The Admin Config page shows a 'Step-up threshold' number input next to the method dropdown, defaulting to 250"
    - "Changing the threshold in Admin Config takes effect on the next transaction (runtime, no restart needed)"
    - "transactions.js reads the threshold from configStore first, runtimeSettings second, 250 as fallback"
  artifacts:
    - path: "banking_api_server/config/runtimeSettings.js"
      provides: "stepUpMethod default changed to 'email'"
      contains: "'email'"
    - path: "banking_api_server/services/configStore.js"
      provides: "step_up_method default 'email', step_up_amount_threshold key added"
      contains: "step_up_amount_threshold"
    - path: "banking_api_ui/src/components/Config.js"
      provides: "Threshold number field in Step-Up Authentication section"
      contains: "Step-up threshold"
    - path: "banking_api_server/routes/transactions.js"
      provides: "Reads threshold from configStore before runtimeSettings"
      contains: "step_up_amount_threshold"
  key_links:
    - from: "Config.js form.step_up_amount_threshold"
      to: "/api/admin/config PUT"
      via: "existing handleChange + save flow in Config.js"
      pattern: "step_up_amount_threshold"
    - from: "transactions.js STEP_UP_THRESHOLD"
      to: "configStore.getEffective('step_up_amount_threshold')"
      via: "parseFloat, falls back to runtimeSettings.get('stepUpAmountThreshold') || 250"
      pattern: "step_up_amount_threshold"
---

<objective>
Change the default step-up authentication method from CIBA to email/OTP across all three layers (runtimeSettings, configStore, Config.js UI). Add a `step_up_amount_threshold` field to configStore and the Admin Config UI so the dollar threshold is configurable in the same panel as the method toggle. Wire transactions.js to read the threshold from configStore first.

Purpose: Email OTP has wider availability (no device enrollment required). Admin Config provides one place to tune both method and threshold without code changes.
Output: Default changed in 3 files; threshold field added to configStore, Config.js, transactions.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-ciba-step-up-authentication-implement-otp-modal-wire-backchannel-auth-for-write-actions-and-validate-enterprise-grade-ux/09-CONTEXT.md

@banking_api_server/config/runtimeSettings.js
@banking_api_server/services/configStore.js
@banking_api_ui/src/components/Config.js
@banking_api_server/routes/transactions.js

<interfaces>
<!-- Exact changes needed per file. Read each file fully before editing. -->

runtimeSettings.js ~line 18:
  stepUpMethod: process.env.STEP_UP_METHOD || 'ciba',   // CHANGE 'ciba' → 'email'

configStore.js ~line 143:
  step_up_method: { public: true, default: 'ciba' },    // CHANGE 'ciba' → 'email'
  // ADD after step_up_method:
  step_up_amount_threshold: { public: true, default: 250 },

configStore.js ~line 423 (env var mapping section):
  step_up_method: ['STEP_UP_METHOD'],
  // ADD after step_up_method:
  step_up_amount_threshold: ['STEP_UP_AMOUNT_THRESHOLD'],

Config.js ~line 87:
  step_up_method: 'ciba',    // CHANGE to 'email'
  // ADD after step_up_method:
  step_up_amount_threshold: 250,

Config.js ~line 1322-1325 (Step-Up Authentication CollapsibleCard, step-up method form-group):
  // ADD a new form-group div AFTER the step-up method select, BEFORE the ciba_enabled select

transactions.js ~line 313:
  const STEP_UP_THRESHOLD = runtimeSettings.get('stepUpAmountThreshold');
  // CHANGE TO:
  const STEP_UP_THRESHOLD = parseFloat(configStore.getEffective('step_up_amount_threshold'))
    || runtimeSettings.get('stepUpAmountThreshold')
    || 250;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change default method to 'email' in runtimeSettings, configStore, Config.js; add threshold to configStore</name>
  <files>
    banking_api_server/config/runtimeSettings.js,
    banking_api_server/services/configStore.js,
    banking_api_ui/src/components/Config.js
  </files>
  <action>
**runtimeSettings.js**: Change line:
  `stepUpMethod: process.env.STEP_UP_METHOD || 'ciba',`
to:
  `stepUpMethod: process.env.STEP_UP_METHOD || 'email',`

**configStore.js** — two changes:
1. In the public-config schema section, change:
   `step_up_method: { public: true, default: 'ciba' },`
   to:
   `step_up_method: { public: true, default: 'email' },`
   Add immediately after it:
   `step_up_amount_threshold: { public: true, default: 250 },`

2. In the env var mapping section (after the `step_up_method: ['STEP_UP_METHOD'],` line), add:
   `step_up_amount_threshold: ['STEP_UP_AMOUNT_THRESHOLD'],`

**Config.js** — two changes:
1. In the initial form defaults object (around line 87), change:
   `step_up_method: 'ciba',`
   to:
   `step_up_method: 'email',`
   Add immediately after it:
   `step_up_amount_threshold: 250,`

2. In the `ALLOWED_KEYS` or initial state array (if it exists — search for where `step_up_method` is exported or submitted), add `step_up_amount_threshold` to the list.
  </action>
  <verify>
    grep -n "step_up_amount_threshold" banking_api_server/services/configStore.js banking_api_server/config/runtimeSettings.js banking_api_ui/src/components/Config.js
  </verify>
  <done>All 3 files contain the expected changes; grep returns lines from each file</done>
</task>

<task type="auto">
  <name>Task 2: Add threshold form field to Config.js + wire transactions.js</name>
  <files>banking_api_ui/src/components/Config.js, banking_api_server/routes/transactions.js</files>
  <action>
**Config.js** — add a new `form-group` div in the "Step-Up Authentication" CollapsibleCard section, after the method select div and before the `ciba_enabled` select div:

```jsx
<div className="form-group">
  <label className="form-label">Step-up threshold ($)</label>
  <input
    type="number"
    className="form-input"
    min="0"
    step="1"
    value={form.step_up_amount_threshold}
    onChange={(e) => handleChange('step_up_amount_threshold', e.target.value)}
    disabled={readOnly}
    placeholder="250"
  />
  <p style={{ fontSize: '0.75rem', color: '#6b7280', marginTop: '0.25rem' }}>
    Minimum transaction amount (USD) that triggers step-up authentication. Default: $250.
  </p>
</div>
```

**transactions.js** — change the threshold read (around line 313):
Replace:
```js
const STEP_UP_THRESHOLD = runtimeSettings.get('stepUpAmountThreshold');
```
With:
```js
const STEP_UP_THRESHOLD = parseFloat(configStore.getEffective('step_up_amount_threshold'))
  || runtimeSettings.get('stepUpAmountThreshold')
  || 250;
```
Note: `configStore` is already imported at the top of `transactions.js`. If not, add:
`const configStore = require('../services/configStore');`
  </action>
  <verify>
    <automated>cd banking_api_ui && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `npm run build` exits 0
    - `grep "step_up_amount_threshold" banking_api_ui/src/components/Config.js` shows both the default value and the form input
    - `grep "step_up_amount_threshold\|configStore.getEffective" banking_api_server/routes/transactions.js` shows the configStore read
  </done>
</task>

</tasks>

<verification>
1. `cd banking_api_ui && npm run build` → exits 0
2. `grep "step_up_method.*email" banking_api_server/config/runtimeSettings.js` confirms new default
3. `grep "step_up_amount_threshold" banking_api_server/services/configStore.js` returns 2 lines (schema + env mapping)
4. `grep "step_up_amount_threshold" banking_api_server/routes/transactions.js` confirms read from configStore
</verification>

<success_criteria>
- Default `stepUpMethod` is `'email'` in runtimeSettings.js and configStore
- Default `step_up_method` initial value in Config.js is `'email'`
- `step_up_amount_threshold` field exists in configStore schema and env mapping
- "Step-up threshold" input visible in Admin Config Step-Up Authentication section
- transactions.js reads threshold from configStore first
- `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/09-ciba-step-up-authentication-implement-otp-modal-wire-backchannel-auth-for-write-actions-and-validate-enterprise-grade-ux/09-03-SUMMARY.md`
</output>
