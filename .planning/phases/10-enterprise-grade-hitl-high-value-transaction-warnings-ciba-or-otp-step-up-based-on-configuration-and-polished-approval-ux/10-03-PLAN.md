---
phase: 10-enterprise-grade-hitl-high-value-transaction-warnings-ciba-or-otp-step-up-based-on-configuration-and-polished-approval-ux
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - banking_api_ui/src/components/EmbeddedAgentDock.js
autonomous: true
requirements:
  - HITL-03

must_haves:
  truths:
    - "EmbeddedAgentDock collapsed state shows a 44px bar with chevron icon — no chat content visible"
    - "Collapse/expand toggle uses ▾/▴ icon characters (not text labels)"
    - "Toolbar row has a bottom border separator (rgba(0,0,0,0.08))"
    - "Collapsed dock: 44px bar, chevron, title — no BankingAgent content rendered"
  artifacts:
    - path: "banking_api_ui/src/components/EmbeddedAgentDock.js"
      provides: "Updated toolbar with chevron icons, 44px min-height, border-bottom"
  key_links:
    - from: "embedded-dock-collapse-btn click"
      to: "setCollapsed(c => !c)"
      via: "onClick handler"
      pattern: "setCollapsed"
---

<objective>
Update EmbeddedAgentDock toolbar to match the UI-SPEC toolbar anatomy: icon-only chevron (▾/▴) for collapse/expand, minimum 44px toolbar height, border-bottom separator. This aligns the bottom dock surface with the BankingAgent header anatomy.

Purpose: All 3 agent surfaces (FAB, middle inline, bottom dock) share the same toolbar language — collapse left, title center, action right — while respecting each surface's constraints.
Output: EmbeddedAgentDock toolbar uses chevron icons, 44px height, border-bottom. No text "Expand"/"Collapse" labels on the button.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@banking_api_ui/src/components/EmbeddedAgentDock.js
</context>

<interfaces>
<!-- Current collapse button in EmbeddedAgentDock (~line 145-154): -->
<button
  type="button"
  className="embedded-dock-collapse-btn"
  onClick={() => setCollapsed((c) => !c)}
  aria-expanded={!collapsed}
  title={collapsed ? 'Expand assistant' : 'Collapse assistant'}
>
  {collapsed ? '▲ Expand' : '▼ Collapse'}
</button>

<!-- Target (per UI-SPEC): icon only, aria-label for accessibility, 44px min-height toolbar -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update toolbar — chevron icon, 44px height, border-bottom</name>
  <files>banking_api_ui/src/components/EmbeddedAgentDock.js</files>
  <action>
In EmbeddedAgentDock.js:

1. Update the collapse button's visible content from text+icon to icon-only:
   - Change `{collapsed ? '▲ Expand' : '▼ Collapse'}` → `{collapsed ? '▴' : '▾'}`
   - Add explicit `aria-label` since the visible text is now icon-only:
     `aria-label={collapsed ? 'Expand assistant' : 'Collapse assistant'}`
   - Keep the `title` attribute as-is (tooltip for hover)

2. Add inline styles to the toolbar `<div>` to enforce spec dimensions:
   ```jsx
   <div
     className="embedded-agent-dock__toolbar"
     style={{
       minHeight: 44,
       borderBottom: '1px solid rgba(0,0,0,0.08)',
       display: 'flex',
       alignItems: 'center',
       justifyContent: 'space-between',
     }}
   >
   ```
   If the toolbar div already has these as className-driven CSS that already works, prefer adding a `style` prop to keep changes minimal rather than editing a separate CSS file.

3. The result should look like:
   ```
   [ AI banking assistant ]    [ ▾ ]
   ```
   Where clicking ▾ collapses and shows:
   ```
   [ AI banking assistant ]    [ ▴ ]
   ```
   with no chat content below.
  </action>
  <verify>
    <automated>cd banking_api_ui && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Collapse button shows ▾ (expanded) and ▴ (collapsed) icon characters only — no "Expand"/"Collapse" text
    - aria-label set correctly for screen readers
    - Toolbar div has minHeight: 44 and borderBottom style
    - npm run build exits 0
  </done>
</task>

</tasks>

<verification>
`cd banking_api_ui && npm run build` exits 0. Bottom dock toolbar shows chevron icon, 44px height when inspected.
</verification>

<success_criteria>
EmbeddedAgentDock toolbar: icon-only chevron toggle, 44px min-height, 1px border-bottom separator. Consistent with BankingAgent header anatomy across all 3 agent surfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/10-enterprise-grade-hitl-high-value-transaction-warnings-ciba-or-otp-step-up-based-on-configuration-and-polished-approval-ux/10-03-SUMMARY.md`
</output>
