---
phase: 102-redesign-app-ui-to-match-ping-operations-fabric-style
plan: 02
type: execute
wave: 1
depends_on: [102-01]
files_modified:
  - banking_api_ui/src/components/ArchitectureTabsPanel.jsx
  - banking_api_ui/src/components/ArchitectureTabsPanel.css
  - banking_api_ui/src/components/Dashboard.js
  - banking_api_ui/src/components/Admin.js
  - banking_api_ui/src/components/BankingAgent.css
autonomous: true
requirements: []
must_haves:
  truths:
    - "Architecture panel displays two tabs: 'System Architecture' and 'Token Exchange Flow'"
    - "Clicking 'Token Exchange Flow' tab shows TokenExchangeFlowDiagram component"
    - "TokenExchangeFlowDiagram receives mode prop from ExchangeModeContext and updates in real-time"
    - "When user toggles exchange mode (1-exchange ↔ 2-exchange), diagram re-renders automatically"
    - "Tab styling uses PingIdentity red accent (#b91c1c) for active tab indicator"
    - "System Architecture tab shows placeholder or static content (ready for future enhancement)"
  artifacts:
    - path: "banking_api_ui/src/components/ArchitectureTabsPanel.jsx"
      provides: "Tab switcher component with System Architecture and Token Exchange Flow tabs"
      exports: ["default export ArchitectureTabsPanel component"]
    - path: "banking_api_ui/src/components/ArchitectureTabsPanel.css"
      provides: "Styling for tab headers, content areas, active state with red accent"
    - path: "banking_api_ui/src/components/Dashboard.js"
      provides: "Updated to wrap main content with SplitPaneLayout"
      contains: "import SplitPaneLayout"
    - path: "banking_api_ui/src/components/Admin.js"
      provides: "Updated to wrap main content with SplitPaneLayout"
      contains: "import SplitPaneLayout"
  key_links:
    - from: "ArchitectureTabsPanel"
      to: "TokenExchangeFlowDiagram"
      via: "Tab 2 renders component"
    - from: "TokenExchangeFlowDiagram"
      to: "ExchangeModeContext"
      via: "useExchangeMode() hook for mode state"
    - from: "SplitPaneLayout"
      to: "Dashboard/Admin"
      via: "wraps existing content"
    - from: "BankingAgent.css"
      to: "Split-pane container"
      via: "adjusted width constraints for 60% pane"
---

<objective>
Create the architecture tabs panel (right side) with two tabs: System Architecture and Token Exchange Flow. Wire TokenExchangeFlowDiagram into the Token Exchange Flow tab with real-time mode syncing via ExchangeModeContext. Integrate SplitPaneLayout into Dashboard and Admin components to enable the split-pane layout.

Purpose: Complete the split-pane layout integration and enable real-time token flow visualization
Output: ArchitectureTabsPanel component + integration into main app
</objective>

<execution_context>
@/Users/cmuir/.copilot/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/102-*/102-CONTEXT.md
@.planning/phases/102-*/102-RESEARCH.md
@.planning/phases/101-*/101-01-SUMMARY.md
@banking_api_ui/src/components/TokenExchangeFlowDiagram.jsx
@banking_api_ui/src/context/ExchangeModeContext.js
@banking_api_ui/src/components/SplitPaneLayout.jsx
@banking_api_ui/src/components/Dashboard.js
@banking_api_ui/src/components/Admin.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ArchitectureTabsPanel component with tab switcher</name>
  <files>banking_api_ui/src/components/ArchitectureTabsPanel.jsx</files>
  <read_first>
    - banking_api_ui/src/components/TokenExchangeFlowDiagram.jsx (to see component interface and props)
    - banking_api_ui/src/context/ExchangeModeContext.js (to understand mode state provider)
    - banking_api_ui/src/index.css (for CSS variable names and color palette)
  </read_first>
  <action>
Create a new React functional component `ArchitectureTabsPanel` that:

1. **Props interface:**
   - No required props (can be expanded later if needed)
   - Component is self-contained

2. **State:**
   - `activeTab`: 'architecture' | 'token-flow' — tracks which tab is selected (default: 'architecture')
   - Use `useState` to manage active tab
   - Tab state is local; no need for Context

3. **Hooks:**
   - Import `useExchangeMode` from `../context/ExchangeModeContext`
   - Call it to subscribe to mode changes
   - Usage: `const { mode } = useExchangeMode();`
   - The TokenExchangeFlowDiagram will re-render when mode changes (no action needed here; React handles it)

4. **JSX structure:**
   ```jsx
   <div className="architecture-tabs-panel">
     {/* Tab header row */}
     <div role="tablist" className="architecture-tabs-header">
       <button
         role="tab"
         aria-selected={activeTab === 'architecture'}
         aria-controls="arch-content"
         onClick={() => setActiveTab('architecture')}
         className="architecture-tab-button"
       >
         System Architecture
       </button>
       <button
         role="tab"
         aria-selected={activeTab === 'token-flow'}
         aria-controls="flow-content"
         onClick={() => setActiveTab('token-flow')}
         className="architecture-tab-button"
       >
         Token Exchange Flow
       </button>
     </div>

     {/* Tab content panels */}
     <div role="tabpanel" id="arch-content" className="architecture-tab-content">
       {activeTab === 'architecture' && (
         <div className="architecture-diagram">
           <h3>System Architecture</h3>
           <p>Users → BFF → PingOne → MCP Server</p>
           {/* Placeholder: can be replaced with SVG diagram later */}
         </div>
       )}
     </div>

     <div role="tabpanel" id="flow-content" className="architecture-tab-content">
       {activeTab === 'token-flow' && (
         <div className="token-flow-display">
           <TokenExchangeFlowDiagram mode={mode} />
         </div>
       )}
     </div>
   </div>
   ```

5. **Imports:**
   ```javascript
   import React, { useState } from 'react';
   import { useExchangeMode } from '../context/ExchangeModeContext';
   import TokenExchangeFlowDiagram from './TokenExchangeFlowDiagram';
   import './ArchitectureTabsPanel.css';
   ```

6. **Export:** Default export the component

7. **Documentation:** Add JSDoc comment explaining tabs and real-time mode syncing

8. **Why this approach:**
   - Native HTML tab roles (role="tablist", role="tab", role="tabpanel") — accessible from the start
   - No new dependencies; just React hooks
   - ExchangeModeContext automatically triggers rerenders via React Context subscription
   - Placeholder content in System Architecture tab can be enhanced later
  </action>
  <verify>
    <automated>grep -n "const ArchitectureTabsPanel\|export.*ArchitectureTabsPanel" banking_api_ui/src/components/ArchitectureTabsPanel.jsx</automated>
  </verify>
  <acceptance_criteria>
    - File exists at banking_api_ui/src/components/ArchitectureTabsPanel.jsx
    - Component exported as default export
    - Component uses useState for activeTab management
    - Component imports and uses useExchangeMode hook
    - TokenExchangeFlowDiagram is imported and rendered in tab 2
    - Tab buttons have role="tab" and aria-selected attributes
    - Tab panels have role="tabpanel" and aria-controls attributes
    - Component is syntactically valid React
  </acceptance_criteria>
  <done>ArchitectureTabsPanel component created with tab switching and TokenExchangeFlowDiagram integration</done>
</task>

<task type="auto">
  <name>Task 2: Style ArchitectureTabsPanel with tab styling and red accents</name>
  <files>banking_api_ui/src/components/ArchitectureTabsPanel.css</files>
  <read_first>
    - banking_api_ui/src/index.css (to see --app-primary-red and other CSS variables)
    - banking_api_ui/src/components/BankingAgent.css (to understand component styling patterns)
  </read_first>
  <action>
Create `ArchitectureTabsPanel.css` with the following styles:

1. **Container:**
   ```css
   .architecture-tabs-panel {
     display: flex;
     flex-direction: column;
     height: 100%;
     background: var(--app-bg-color, #ffffff);
     border-radius: 4px;
     overflow: hidden;
   }
   ```

2. **Tab header row:**
   ```css
   .architecture-tabs-header {
     display: flex;
     flex-direction: row;
     border-bottom: 1px solid var(--app-border-color, #e0e0e0);
     gap: 0;
     background: var(--app-bg-secondary, #fafafa);
   }
   ```

3. **Tab buttons:**
   ```css
   .architecture-tab-button {
     flex: 1;
     padding: 12px 16px;
     border: none;
     background: transparent;
     color: var(--app-text-secondary, #666666);
     font-size: 14px;
     font-weight: 600;
     cursor: pointer;
     transition: all 0.2s ease;
     border-bottom: 3px solid transparent;
     font-family: var(--app-font-family, 'Inter', 'Segoe UI', sans-serif);
   }

   .architecture-tab-button:hover {
     background: var(--app-bg-hover, #f0f0f0);
     color: var(--app-text-color, #1a1a1a);
   }

   .architecture-tab-button[aria-selected="true"] {
     color: var(--app-primary-red, #b91c1c);
     border-bottom-color: var(--app-primary-red, #b91c1c);
     background: transparent;
   }

   .architecture-tab-button[aria-selected="true"]:hover {
     background: transparent;
   }
   ```

4. **Tab content areas:**
   ```css
   .architecture-tab-content {
     flex: 1;
     overflow-y: auto;
     padding: 16px;
     display: flex;
     flex-direction: column;
   }

   .architecture-tab-content h3 {
     margin: 0 0 12px 0;
     font-size: 16px;
     font-weight: 600;
     color: var(--app-text-color, #1a1a1a);
     font-family: var(--app-font-family, 'Inter', 'Segoe UI', sans-serif);
   }

   .architecture-tab-content p {
     margin: 0;
     font-size: 14px;
     color: var(--app-text-secondary, #666666);
     line-height: 1.5;
   }
   ```

5. **Token flow display (wrapper for diagram):**
   ```css
   .token-flow-display {
     flex: 1;
     display: flex;
     flex-direction: column;
     gap: 12px;
   }

   .architecture-diagram {
     flex: 1;
   }
   ```

6. **Dark mode support:**
   Use CSS variables that adapt per theme (IndustryBrandingContext sets these):
   - `--app-bg-color` → background color
   - `--app-text-color` → text color
   - `--app-primary-red` → red accent (#b91c1c)
   - All other colors via variables

7. **BEM naming:** Use `.architecture-*` prefix for all classes

8. **Transitions:** Smooth 0.2s ease on color/background changes

9. **PingIdentity styling:**
   - Active tab indicator: solid red (#b91c1c) bottom border
   - Font: PingIdentity compatible (Inter or system sans-serif)
   - No shadows on tabs (clean, flat design)
  </action>
  <verify>
    <automated>grep -c "\.architecture-tabs-header\|\.architecture-tab-button\|\.architecture-tab-content" banking_api_ui/src/components/ArchitectureTabsPanel.css</automated>
  </verify>
  <acceptance_criteria>
    - File exists at banking_api_ui/src/components/ArchitectureTabsPanel.css
    - Contains .architecture-tabs-header with flex-direction: row
    - Tab buttons have aria-selected[="true"] selector with var(--app-primary-red)
    - Active tab indicator uses border-bottom-color in red
    - Content area has flex: 1 and overflow-y: auto
    - Uses CSS variables (not hardcoded colors)
    - No syntax errors; CSS is valid
  </acceptance_criteria>
  <done>ArchitectureTabsPanel.css created with tab styling and red accent indicators</done>
</task>

<task type="auto">
  <name>Task 3: Integrate SplitPaneLayout into Dashboard.js</name>
  <files>banking_api_ui/src/components/Dashboard.js</files>
  <read_first>
    - banking_api_ui/src/components/Dashboard.js (current main dashboard structure)
    - banking_api_ui/src/components/SplitPaneLayout.jsx (to understand props: children, archPanel)
    - banking_api_ui/src/components/ArchitectureTabsPanel.jsx (to understand component being passed)
  </read_first>
  <action>
Update Dashboard.js to integrate the split-pane layout:

1. **Add imports** (near top of file, after existing imports):
   ```javascript
   import SplitPaneLayout from './SplitPaneLayout';
   import ArchitectureTabsPanel from './ArchitectureTabsPanel';
   ```

2. **Identify the main content render area** — Find where Dashboard JSX renders the agent or main content panels. Currently likely rendering:
   - BankingAgent component
   - TokenChainDisplay
   - Operation panels
   - etc.

3. **Wrap main content with SplitPaneLayout:**
   - Existing content (agent operations, token chain, etc.) becomes the `children` (agent pane)
   - Create `<ArchitectureTabsPanel />` as the `archPanel` prop
   - Keep all existing functionality; just change the wrapper

   Example transformation:
   ```javascript
   // BEFORE (existing):
   return (
     <div className={...}>
       <BankingAgent />
       <TokenChainDisplay />
     </div>
   );

   // AFTER (with split-pane):
   return (
     <SplitPaneLayout archPanel={<ArchitectureTabsPanel />}>
       <BankingAgent />
       <TokenChainDisplay />
     </SplitPaneLayout>
   );
   ```

4. **No behavior changes:** Only the layout wrapper changes; all existing logic remains untouched

5. **CSS note:** Dashboard may have existing `.dashboard-content` or similar—these can coexist with SplitPaneLayout styles; no conflicts expected

6. **Test instruction:** After edit, verify Desktop dashboard still renders correctly in dev tools (flexbox layout should be visible)
  </action>
  <verify>
    <automated>grep -n "import.*SplitPaneLayout\|import.*ArchitectureTabsPanel" banking_api_ui/src/components/Dashboard.js</automated>
  </verify>
  <acceptance_criteria>
    - SplitPaneLayout is imported in Dashboard.js
    - ArchitectureTabsPanel is imported in Dashboard.js
    - Dashboard renders main content wrapped in SplitPaneLayout
    - SplitPaneLayout receives archPanel prop with ArchitectureTabsPanel component
    - Existing Dashboard functionality is preserved (no logic changes)
    - No new console errors or TypeScript errors in Dashboard
  </acceptance_criteria>
  <done>Dashboard.js updated to use SplitPaneLayout wrapper with ArchitectureTabsPanel</done>
</task>

<task type="auto">
  <name>Task 4: Integrate SplitPaneLayout into Admin.js</name>
  <files>banking_api_ui/src/components/Admin.js</files>
  <read_first>
    - banking_api_ui/src/components/Admin.js (current admin dashboard structure)
    - banking_api_ui/src/components/Dashboard.js (to see how integration was done there, for consistency)
  </read_first>
  <action>
Update Admin.js to integrate the split-pane layout (same approach as Dashboard.js):

1. **Add imports:**
   ```javascript
   import SplitPaneLayout from './SplitPaneLayout';
   import ArchitectureTabsPanel from './ArchitectureTabsPanel';
   ```

2. **Wrap main content with SplitPaneLayout:**
   - Same pattern as Dashboard.js
   - Existing admin operations/panels → children
   - ArchitectureTabsPanel → archPanel

3. **Consistency:** Use identical wrapper pattern as Dashboard for maintainability

4. **No behavior changes:** Only layout wrapper changes; admin features remain intact
  </action>
  <verify>
    <automated>grep -n "import.*SplitPaneLayout\|import.*ArchitectureTabsPanel" banking_api_ui/src/components/Admin.js</automated>
  </verify>
  <acceptance_criteria>
    - SplitPaneLayout is imported in Admin.js
    - ArchitectureTabsPanel is imported in Admin.js
    - Admin renders main content wrapped in SplitPaneLayout
    - Same integration pattern as Dashboard.js
    - No new errors in Admin component
  </acceptance_criteria>
  <done>Admin.js updated to use SplitPaneLayout wrapper with ArchitectureTabsPanel</done>
</task>

<task type="auto">
  <name>Task 5: Update BankingAgent.css for 60% pane width containment</name>
  <files>banking_api_ui/src/components/BankingAgent.css</files>
  <read_first>
    - banking_api_ui/src/components/BankingAgent.css (current BankingAgent styles; very large file, look for width, max-width, flex properties)
    - banking_api_ui/src/components/SplitPaneLayout.css (to understand 60% pane width)
  </read_first>
  <action>
Update BankingAgent.css to work within the 60% flex container:

1. **Find existing width/max-width constraints:**
   - Search for: `width: 100%`, `width: 100vw`, `width: auto`, `max-width`, `flex`
   - If BankingAgent has `width: 100vw` → this needs to change to `width: 100%` (width of parent container, not viewport)

2. **Update main component container:**
   ```css
   .banking-agent {
     width: 100%;  /* Changed from 100vw or full if needed */
     height: 100%;
     display: flex;
     flex-direction: column;
     /* Keep other existing properties */
   }
   ```

3. **Remove or adjust any position: fixed elements:**
   - Search for any `position: fixed` that positions BankingAgent full-screen
   - Change to `position: relative` or remove if not needed

4. **Ensure overflow behavior:**
   - Add `overflow-y: auto` to scrollable content areas inside BankingAgent
   - This allows content to scroll within the 60% pane

5. **Test note:** BankingAgent should now respect its parent container's 60% width instead of expanding to full viewport

6. **Minimal changes:** Only modify sizing constraints; keep all other BankingAgent styling intact (colors, typography, spacing, interactions all remain the same)

7. **IMPORTANT:** Do NOT refactor BankingAgent's internal structure or state management—this is CSS-only sizing adjustment
  </action>
  <verify>
    <automated>grep -n "width: 100vw\|width: 100%\|position: fixed" banking_api_ui/src/components/BankingAgent.css | head -10</automated>
  </verify>
  <acceptance_criteria>
    - BankingAgent.css updated to use width: 100% (not 100vw)
    - No position: fixed elements that would break out of the 60% pane
    - Overflow properties adjusted to allow scrolling within pane
    - All existing BankingAgent styling preserved (colors, typography, etc.)
    - No removal of major style rules; only sizing constraints modified
  </acceptance_criteria>
  <done>BankingAgent.css updated for 60% pane width containment without behavioral changes</done>
</task>

<task type="auto">
  <name>Task 6: Build and verify integration</name>
  <files>banking_api_ui</files>
  <read_first>
    - package.json (to confirm build script)
  </read_first>
  <action>
Run the React build to verify all integrations compile correctly:

1. Execute: `npm run build --prefix banking_api_ui`
2. Monitor output for:
   - No TypeScript errors
   - No ESLint warnings
   - Build completes successfully (exit code 0)
   - Bundle size remains reasonable (< 400KB JS gzipped)
   - All new components compile

If build fails:
   - Check for missing imports
   - Verify component export syntax
   - Check for CSS syntax errors

Expected: Build completes in < 30 seconds with no errors.
  </action>
  <verify>
    <automated>npm run build --prefix banking_api_ui 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Build exits with code 0
    - No TypeScript compilation errors
    - No "TS2304" or similar errors
    - Output includes "The project was built successfully"
    - Bundle size reported and reasonable
  </acceptance_criteria>
  <done>Build succeeds; all changes compile correctly with no errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Visual Verification of Split-Pane Layout</name>
  <what-built>
  - SplitPaneLayout wrapper component with responsive flexbox
  - ArchitectureTabsPanel with System Architecture and Token Exchange Flow tabs
  - Integration into Dashboard and Admin components
  - BankingAgent.css updated for 60% pane width
  - TokenExchangeFlowDiagram embedded in Token Flow tab with real-time mode syncing
  </what-built>
  <how-to-verify>
  1. Start dev server (or open existing running instance)
  2. Navigate to /dashboard (customer view)
  3. Verify on desktop (1024px+ width):
     - Left pane (60%): Chat/agent interface visible
     - Right pane (40%): Architecture panel visible with "System Architecture" and "Token Exchange Flow" tabs
     - "System Architecture" tab shows placeholder text ("Users → BFF → PingOne → MCP Server")
     - "Token Exchange Flow" tab shows the RFC 8693 flow diagram component
  4. Toggle exchange mode (1-exchange ↔ 2-exchange) and verify:
     - Flow diagram updates in real-time (path switches between single and double exchange)
     - Agent bubble (🤖) appears/disappears appropriately
  5. Test responsive: Resize browser to 768px width
     - Both panes still visible side-by-side, widths adjusted
  6. Resize to 360px (mobile)
     - Panes stack vertically (if using tab switcher: tap "Architecture" to view right pane)
  7. Verify no console errors or warnings
  8. Confirm existing agent features still work (operations, token inspection, etc.)
  </how-to-verify>
  <resume-signal>Type "approved" (looks good) or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mode state (ExchangeModeContext) | Trusted; managed by app, not user input |
| Tab switching | No security concerns; local state only |
| Component props (archPanel, children) | React nodes; author-controlled |

## STRIDE Threat Register

| ID | Category | Component | Disposition | Mitigation |
|----|----|---------|-------------|-----------|
| T-102-01 | Information | TokenExchangeFlowDiagram in Tab 2 | Mitigate | Display only public architecture info (no secrets); RFC 8693 details are educational reference material |
| T-102-02 | Tampering | Tab switcher state | Accept | Local component state; no persistence; user can only toggle between legitimate tabs |

No high-risk security issues identified for this UI redesign phase.
</threat_model>

<verification>
After checkpoint approval:
1. Verify all tasks completed (6/6 tasks + checkpoint)
2. Confirm build success and visual verification passed
3. Ensure no regressions in existing functionality
4. Document any visual tweaks or CSS adjustments needed
5. Mark Phase 102-02 complete
</verification>

<success_criteria>
- [ ] ArchitectureTabsPanel component created with tab switching
- [ ] TokenExchangeFlowDiagram integrated in Token Flow tab
- [ ] Mode syncing via ExchangeModeContext working (diagram updates in real-time)
- [ ] SplitPaneLayout integrated in Dashboard and Admin
- [ ] BankingAgent.css updated for 60% pane width
- [ ] Build passes with exit code 0
- [ ] Desktop layout: 60/40 split visible and functional
- [ ] Tablet layout: Side-by-side with adjusted widths
- [ ] Mobile layout: Stacked or tab-switchable
- [ ] Red accent styling applied to active tab
- [ ] No console errors or regressions
- [ ] Human verification checkpoint approved
</success_criteria>

<output>
After execution, create `.planning/phases/102-*/{phase}-02-SUMMARY.md` documenting:
- Architecture tabs panel implementation details
- Real-time mode syncing verification
- Split-pane integration into Dashboard/Admin
- Responsive layout verification at all breakpoints
- Build and checkpoint results
- Ready for Plan 102-03 (PingOps styling refinements)
</output>

