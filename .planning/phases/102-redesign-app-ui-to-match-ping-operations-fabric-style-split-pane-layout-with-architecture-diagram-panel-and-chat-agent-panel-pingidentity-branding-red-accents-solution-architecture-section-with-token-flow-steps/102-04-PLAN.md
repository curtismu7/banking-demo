---
phase: 102-redesign-app-ui-to-match-ping-operations-fabric-style
plan: 04
type: execute
wave: 2
depends_on: [102-03]
files_modified: []
autonomous: false
requirements: []
must_haves:
  truths:
    - "Split-pane layout displays correctly at all breakpoints (1024px, 768px, 360px)"
    - "Agent chat pane and architecture panel are responsive and accessible"
    - "Mode toggling (1-exchange ↔ 2-exchange) updates token flow diagram in real-time"
    - "All existing agent features work without regression (operations, consent, tokens)"
    - "No console errors or warnings in dev tools"
    - "PingIdentity red accents and font styling visible and consistent"
    - "Light and dark mode themes both functional"
  artifacts: []
  key_links:
    - from: "Split-pane layout"
      to: "Responsive breakpoints"
      via: "CSS flex-direction and width adjustments"
    - from: "TokenExchangeFlowDiagram"
      to: "ExchangeModeContext"
      via: "useExchangeMode() hook subscription"
---

<objective>
Final end-to-end verification and integration testing for Phase 102. Verify all components work together, responsive layout functions at all breakpoints, real-time mode syncing works, and no regressions in existing functionality. This is the verification checkpoint before Phase 102 is marked complete.

Purpose: Ensure Phase 102 redesign is production-ready
Output: Comprehensive test results and go/no-go decision
</objective>

<execution_context>
@/Users/cmuir/.copilot/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/102-*/102-CONTEXT.md
@.planning/phases/102-*/102-RESEARCH.md
@.planning/phases/102-*/102-01-SUMMARY.md
@.planning/phases/102-*/102-02-SUMMARY.md
@.planning/phases/102-*/102-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Full End-to-End Verification & Responsive Testing</name>
  <what-built>
  **Phase 102 Complete Build:**
  - SplitPaneLayout wrapper component with responsive flexbox
  - ArchitectureTabsPanel with System Architecture and Token Exchange Flow tabs
  - TokenExchangeFlowDiagram embedded with real-time mode syncing
  - Dashboard and Admin integrated with split-pane layout
  - BankingAgent adapted for 60% pane width
  - PingIdentity red accents applied to buttons, active tabs, status states
  - PingIdentity font family applied throughout
  - Light and dark mode support via CSS variables
  </what-built>
  <how-to-verify>
  **TEST 1: Desktop Layout (1024px+)**
  1. Open browser to http://localhost:3000/dashboard (or your running dev instance)
  2. Window width >= 1024px
  3. Verify:
     - [ ] Agent pane visible on left (60% width)
     - [ ] Architecture panel visible on right (40% width)
     - [ ] Architecture panel has two tab buttons: "System Architecture" and "Token Exchange Flow"
     - [ ] "System Architecture" tab shows placeholder content
     - [ ] "Token Exchange Flow" tab shows RFC 8693 flow diagram
     - [ ] Layout visually balanced (not cramped or stretched)

  **TEST 2: Real-Time Mode Syncing**
  1. In browser DevTools or agent UI, find mode toggle or exchange mode selector
  2. Start in 1-exchange mode
  3. In Architecture panel Token Flow tab, verify:
     - [ ] Single exchange path visible: User → BFF → PingOne → MCP
     - [ ] No Agent bubble (🤖)
     - [ ] Token details box shows subject-only delegation (no act claim)
  4. Toggle to 2-exchange mode
  5. Verify diagram updates in REAL-TIME (instant, no page reload):
     - [ ] Double exchange path visible: User → Agent → BFF → PingOne → MCP
     - [ ] Agent bubble (🤖) visible, yellow background
     - [ ] Token details box shows act claim and delegation info
  6. Toggle back to 1-exchange mode
     - [ ] Diagram instantly reverts
  7. Repeat 2-3 times to confirm consistent real-time updates

  **TEST 3: Tablet Responsive (768px-1023px)**
  1. Resize browser window to ~900px width (or use DevTools responsive mode)
  2. Verify:
     - [ ] Both panes still visible side-by-side
     - [ ] Pane widths adjusted (likely agent 55%, architecture 45%)
     - [ ] No pane hidden or stacked vertically
     - [ ] Tab buttons still clickable
     - [ ] Diagram still renders (may be more compact)

  **TEST 4: Mobile Responsive (360px)**
  1. Resize browser to ~360px (or DevTools mobile viewport)
  2. Verify:
     - [ ] Panes now stacked vertically
     - [ ] Agent pane visible first (full width)
     - [ ] Architecture panel either:
         a) Hidden by default (tab switcher shows "Architecture"); Click to reveal, OR
         b) Below agent pane (scroll down to see)
     - [ ] Tab buttons still functional if using switcher approach
     - [ ] All content readable (no text wrapping issues)
     - [ ] Buttons have adequate touch targets (>44px recommended)

  **TEST 5: Button Styling & PingOps Design**
  1. On desktop, examine UI elements:
     - [ ] Primary action buttons are RED (#b91c1c) with white text
     - [ ] Button text is using Inter or compatible sans-serif (not serif)
     - [ ] Button on hover: darker red (#991b1b)
     - [ ] Active tab: Red bottom border (#b91c1c) and red text
     - [ ] No jarring color changes or flashing

  **TEST 6: Light & Dark Mode**
  1. If theme toggle exists in UI, find it (usually top-right or settings)
  2. Ensure currently in LIGHT MODE:
     - [ ] Background is white or very light
     - [ ] Text is dark (readable)
     - [ ] Red accents are vibrant
  3. Toggle to DARK MODE:
     - [ ] Background is dark (#1a1a1a range)
     - [ ] Text is light (still readable)
     - [ ] Red accents remain visible and bold
     - [ ] No unreadable text or poor contrast
  4. Toggle back to LIGHT MODE
     - [ ] Colors correctly revert

  **TEST 7: Agent Functionality (No Regressions)**
  1. In agent pane, perform a basic operation:
     - [ ] Type a message or select an operation
     - [ ] Agent responds or shows progress
     - [ ] Operation completes without errors
     - [ ] TokenChainDisplay (if visible) shows token history
  2. Verify no console errors (open DevTools > Console):
     - [ ] No red error messages
     - [ ] No warnings about TypeError or undefined refs
  3. Try an auth flow operation (if available):
     - [ ] Consent modal appears (if needed)
     - [ ] Modal styling matches PingOps design (red accents)
     - [ ] Operation completes

  **TEST 8: Mobile Touch Interactions**
  1. On 360px mobile view:
     - [ ] All buttons are tappable (not too small)
     - [ ] Tab switcher works with taps (if using tabs)
     - [ ] Scroll works smoothly in both panes (if stacked)
     - [ ] No sticky elements break mobile layout

  **TEST 9: Console & Performance**
  1. Open DevTools > Console
     - [ ] No TypeScript errors (red Xs)
     - [ ] No runtime errors about missing components
     - [ ] No warnings about deprecated React features
  2. Open DevTools > Network
     - [ ] All assets load (no 404s)
     - [ ] Build outputs appear in dist/build folder
  3. Page load time < 3 seconds (typical for React dev)

  **TEST 10: Cross-Browser (if possible)**
  1. Test in Chrome/Edge:
     - [ ] All layout and styling works
  2. Test in Safari (if on Mac):
     - [ ] Flexbox layout renders correctly
     - [ ] Red colors display accurately
     - [ ] Tab switching works
  3. Test in Firefox:
     - [ ] Same checks as Chrome/Edge

  **SUMMARY CHECKLIST**
  - [ ] Desktop layout (1024px) working ✓
  - [ ] Tablet layout (768px) working ✓
  - [ ] Mobile layout (360px) working ✓
  - [ ] Mode syncing real-time ✓
  - [ ] Red accents visible and correct ✓
  - [ ] Font family applied (Inter/sans-serif) ✓
  - [ ] Light mode: white + dark text + red accents ✓
  - [ ] Dark mode: dark + light text + red accents ✓
  - [ ] Agent operations working (no regressions) ✓
  - [ ] No console errors ✓
  - [ ] All interactive elements responsive to hover/focus ✓
  </how-to-verify>
  <resume-signal>Type "approved" (all tests pass, ready to close Phase 102) OR list any issues/adjustments needed</resume-signal>
</task>

</tasks>

<threat_model>
No new security threats from integration testing. This is verification only.
</threat_model>

<verification>
After checkpoint resolution:
- If approved: All Phase 102 tasks complete; ready for ROADMAP update and phase closeout
- If issues found: Note specific failures, determine if they're blockers or can be deferred to Phase 103
</verification>

<success_criteria>
- [ ] End-to-end verification checkpoint completed
- [ ] Desktop (1024px+) layout verified ✓
- [ ] Tablet (768px) layout verified ✓
- [ ] Mobile (360px) layout verified ✓
- [ ] Real-time mode syncing tested ✓
- [ ] PingOps design language verified ✓
- [ ] Light and dark modes functional ✓
- [ ] Agent operations working without regressions ✓
- [ ] No console errors ✓
- [ ] User provides go/no-go decision
</success_criteria>

<output>
After checkpoint is resolved:
1. If approved: Create `.planning/phases/102-*/{phase}-04-SUMMARY.md` with:
   - Verification results from all 10 tests
   - Any visual adjustments made
   - Go-live decision: READY FOR PRODUCTION
2. If issues found: Document issues and determine Phase 103 follow-up items
3. Create Phase 102 COMPLETION SUMMARY documenting:
   - All 4 plans executed successfully
   - Visual redesign complete
   - Split-pane layout responsive
   - Real-time token flow integration working
   - PingOps design language applied
   - Ready to mark Phase 102 COMPLETE in ROADMAP.md
</output>

