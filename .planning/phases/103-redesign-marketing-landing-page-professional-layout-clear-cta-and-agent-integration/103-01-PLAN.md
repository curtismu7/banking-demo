---
phase: 103-redesign-marketing-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - banking_api_ui/src/components/LandingPage.js
autonomous: true
requirements: [MKT-01, MKT-02, MKT-03, MKT-06]
user_setup: []

must_haves:
  truths:
    - "Page displays a professional header with logo, navigation links, and admin/customer login shortcuts"
    - "Hero section shows compelling headline ('Secured AI Banking'), subheadline, and two CTA buttons (Try as Admin / Try as Customer)"
    - "Feature grid displays exactly 4 cards: Auth Flows, RFC 8693, MCP Integration, AI Agent"
    - "Each feature card includes an icon, title, and brief description"
    - "All CTAs link to correct login routes (/admin/login, /login for customer)"
    - "No TypeScript errors or console warnings in component"
  artifacts:
    - path: "banking_api_ui/src/components/LandingPage.js"
      provides: "Complete professional landing page with header, hero, and feature grid"
      exports: ["default export LandingPage component"]
    - path: "CSS will be separate file (103-02)"
  key_links:
    - from: "LandingPage.js header"
      to: "/admin/login and /login routes"
      via: "Link component from react-router-dom"
    - from: "Feature grid cards"
      to: "Placeholder SVG icons (built-in)"
      via: "Inline SVG or Unicode symbols"
---

<objective>
Create the React component structure for the redesigned /marketing landing page with professional header, hero section, and feature grid showcasing the demo's core capabilities.

Purpose: Establish the JSX foundation that will be styled in 103-02
Output: LandingPage.js with all structural elements, copy, and routing configured
</objective>

<execution_context>
@/Users/cmuir/.copilot/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/103-*/103-CONTEXT.md
@banking_api_ui/src/components/Dashboard.js (reference for page structure pattern)
@banking_api_ui/src/pages/LandingPage.js (current implementation to replace)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create professional header with logo, nav, and login shortcuts</name>
  <files>banking_api_ui/src/components/LandingPage.js</files>
  <action>
Start creating LandingPage.js (or update existing src/pages/LandingPage.js to src/components/LandingPage.js if path differs).

Create the header section with:
1. **Structure:** `<header className="landing-header">`
2. **Logo/branding:** BX Finance logo or "BX Finance" text + subtitle "AI Banking Demo"
3. **Navigation links:** Quick links for Admin Dashboard, Customer Dashboard (pointing to /admin/login and /login)
4. **Login shortcuts:** "Sign In as Admin" and "Sign In as Customer" buttons with `<Link>` to /admin/login and /login
5. **Use:** `import { Link } from 'react-router-dom';`
6. **Imports:** React, useState for any interactive header elements if needed
7. **Props:** None required (static landing page)
8. **JSX structure:**
   ```jsx
   <header className="landing-header">
     <div className="landing-header-content">
       <div className="landing-logo">
         <h1>BX Finance</h1>
         <p>AI Banking Demo</p>
       </div>
       <nav className="landing-nav">
         <Link to="/admin/login" className="nav-link">Admin Dashboard</Link>
         <Link to="/login" className="nav-link">Customer Dashboard</Link>
       </nav>
       <div className="landing-header-actions">
         <Link to="/admin/login" className="btn btn-primary">Sign In as Admin</Link>
         <Link to="/login" className="btn btn-secondary">Sign In as Customer</Link>
       </div>
     </div>
   </header>
   ```

9. **CSS classes:** Use BEM naming (.landing-*, .landing-header, .landing-nav, .landing-header-actions, .landing-logo)
10. **Accessibility:** Add `role="banner"` to header, ARIA labels on nav links
  </action>
  <verify>
    <automated>grep -c "landing-header" banking_api_ui/src/components/LandingPage.js && echo "Header structure present"</automated>
  </verify>
  <done>Header JSX complete with logo, nav, login shortcuts; no syntax errors</done>
</task>

<task type="auto">
  <name>Task 2: Create hero section with headline, subheadline, and CTAs</name>
  <files>banking_api_ui/src/components/LandingPage.js</files>
  <action>
Continue LandingPage.js with hero section:

1. **Section tag:** `<section className="landing-hero">`
2. **Headline:** "Secured AI Banking" or "AI Banking Meets RFC 8693" (pick one; professional tone)
3. **Subheadline:** "See how token exchange, MCP spec, and AI agents work together in a real banking scenario"
4. **Background:** Will be styled in 103-02; add className for styling (e.g., `.landing-hero`)
5. **CTA buttons:** Two primary buttons for clarity
   ```jsx
   <div className="landing-hero-actions">
     <Link to="/admin/login" className="hero-cta hero-cta-primary">
       Try as Admin
     </Link>
     <Link to="/login" className="hero-cta hero-cta-secondary">
       Try as Customer
     </Link>
   </div>
   ```
6. **Accessibility:** `role="region"` with `aria-label="Hero section"`
7. **Typography:** Use `<h1>` for headline, `<p className="landing-hero-subheadline">` for subheadline
8. **Copy per D-103-01:** Headline must be compelling; subheadline explains 3 core concepts (token exchange, RFC 8693, MCP, AI)

JSX example:
```jsx
<section className="landing-hero" role="region" aria-label="Hero section">
  <div className="landing-hero-content">
    <h1 className="landing-hero-headline">Secured AI Banking</h1>
    <p className="landing-hero-subheadline">
      Explore RFC 8693 token delegation, MCP spec integration, and how AI agents safely access banking APIs on behalf of users.
    </p>
    <div className="landing-hero-actions">
      <Link to="/admin/login" className="hero-cta hero-cta-primary">
        Try as Admin
      </Link>
      <Link to="/login" className="hero-cta hero-cta-secondary">
        Try as Customer
      </Link>
    </div>
  </div>
</section>
```
  </action>
  <verify>
    <automated>grep -c "landing-hero" banking_api_ui/src/components/LandingPage.js && grep "Secured AI Banking" banking_api_ui/src/components/LandingPage.js</automated>
  </verify>
  <done>Hero section JSX complete with headline, subheadline, CTA buttons; all links route correctly</done>
</task>

<task type="auto">
  <name>Task 3: Create feature grid with 4 cards (Auth Flows, RFC 8693, MCP, Agent)</name>
  <files>banking_api_ui/src/components/LandingPage.js</files>
  <action>
Continue LandingPage.js with feature grid section:

1. **Section:** `<section className="landing-features">`
2. **Grid structure:** BEM classes (.landing-features, .landing-features-grid, .landing-feature-card)
3. **4 cards:** Exactly 4 feature cards in order (per D-103-02 discretion: Auth Flows first)
4. **Card structure (repeat 4x for each feature):**
   ```jsx
   <article className="landing-feature-card">
     <div className="landing-feature-icon">
       {/* Icon: Unicode emoji or inline SVG placeholder */}
     </div>
     <h3 className="landing-feature-title">{title}</h3>
     <p className="landing-feature-description">{description}</p>
   </article>
   ```

5. **Feature 1: Auth Flows**
   - Icon: 🔐 (lock emoji) or simple SVG icon
   - Title: "3 Auth Flows"
   - Description: "Experience OIDC, CIBA push auth, and in-flight step-up challenges — all protecting banking operations"

6. **Feature 2: RFC 8693**
   - Icon: 📜 (scroll emoji) or RFC icon
   - Title: "RFC 8693 Token Exchange"
   - Description: "Watch secure delegation in action: user tokens transformed to agent tokens with act claims"

7. **Feature 3: MCP Integration**
   - Icon: 🔌 (plug emoji) or integration icon
   - Title: "MCP Spec Integration"
   - Description: "See how AI agents connect to banking APIs via the Model Context Protocol with full auth context"

8. **Feature 4: AI Agent**
   - Icon: 🤖 (robot emoji) or agent icon
   - Title: "AI Agent Banking"
   - Description: "Observe real-time agent operations: transfers, balance checks, transaction analysis — all secured by tokens"

9. **Container:** Wrap in `<section className="landing-features">` with a grid wrapper `<div className="landing-features-grid">`
10. **Accessibility:** Add `role="list"` to grid, `role="listitem"` to cards

JSX example:
```jsx
<section className="landing-features">
  <div className="landing-features-heading">
    <h2>Core Capabilities</h2>
  </div>
  <div className="landing-features-grid" role="list">
    <article className="landing-feature-card" role="listitem">
      <div className="landing-feature-icon">🔐</div>
      <h3 className="landing-feature-title">3 Auth Flows</h3>
      <p className="landing-feature-description">
        Experience OIDC, CIBA push auth, and in-flight step-up challenges — all protecting banking operations
      </p>
    </article>
    {/* Repeat for RFC 8693, MCP, AI Agent */}
  </div>
</section>
```

11. **Styling placeholder:** Add className `landing-feature-card` to each card; spacing/layout handled in 103-02
12. **No content fetching:** All copy is static; no API calls. This is purely presentational.
  </action>
  <verify>
    <automated>grep -c "landing-feature-card" banking_api_ui/src/components/LandingPage.js | awk '{if ($1 == 4) print "✅ 4 feature cards found"; else print "❌ Expected 4 cards, found " $1}'</automated>
  </verify>
  <done>Feature grid with 4 cards complete; all titles and descriptions present; no console errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → LandingPage | Marketing copy is static; no user input processed |
| LandingPage → Router | Navigation links point to /admin/login and /login (trust router to validate auth routes) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-01 | Tampering | Feature copy hardcoded | Accept | Copy is marketing-only; no security impact if modified locally |
| T-103-02 | Information Disclosure | Links to login pages are public | Accept | Login pages are public endpoints; no sensitive data exposed |

</threat_model>

<verification>
1. **JSX syntax check:** `npm run build --prefix banking_api_ui` should compile without errors
2. **Route verification:** Clicking "Try as Admin" navigates to /admin/login; clicking "Try as Customer" navigates to /login
3. **Component structure:** All 3 sections (header, hero, feature grid) render without blank spaces or missing elements
4. **No console errors:** Open DevTools Console; no red errors or unhandled rejections
</verification>

<success_criteria>
- [ ] LandingPage.js created with header, hero, and feature grid sections
- [ ] All JSX syntax is valid; no TypeScript errors
- [ ] 4 feature cards with correct titles and descriptions (Auth Flows, RFC 8693, MCP, Agent)
- [ ] Both CTA buttons link to correct login routes
- [ ] Accessibility: ARIA labels, role attributes, semantic HTML
- [ ] Build completes with exit code 0
</success_criteria>

<output>
After completion, create `.planning/phases/103-*/103-01-SUMMARY.md` documenting:
- LandingPage.js structure (4 sections: header, hero, feature grid, agent dock placeholder)
- Copy delivered (headlines, descriptions, button labels)
- Routing verified (links to /admin/login, /login)
- Build metrics: JS/CSS bundle sizes
</output>
