---
phase: 11-education-content-review-and-accuracy-audit-oauth-rfcs-mcp-pingone-ai-completeness-check
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - banking_api_ui/src/components/AgentGatewayPanel.js
  - banking_api_ui/src/components/OidcPanel.js
  - banking_api_ui/src/components/McpProtocolPanel.js
  - banking_api_ui/src/components/TokenExchangePanel.js
autonomous: true
requirements: [EDU-AUDIT-01, EDU-AUDIT-02, EDU-AUDIT-03]

must_haves:
  truths:
    - "All RFC references in education panels match the actual spec (RFC 8693, RFC 9728, RFC 9396, RFC 7519)"
    - "PingOne-specific claims and endpoints described in panels match what the live demo actually does"
    - "MCP spec references updated to spec version 2025-11-25"
    - "Token exchange panel shows accurate may_act and act claim structure matching current implementation"
    - "Any dead links or stale spec section references are corrected"
    - "No panel describes a flow that does not exist in the live demo"
  artifacts:
    - path: "banking_api_ui/src/components/AgentGatewayPanel.js"
      provides: "Audited gateway panel with correct RFC 9728 references"
    - path: "banking_api_ui/src/components/OidcPanel.js"
      provides: "Audited OIDC panel with accurate PingOne claim descriptions"
    - path: "banking_api_ui/src/components/McpProtocolPanel.js"
      provides: "Audited MCP panel referencing spec version 2025-11-25"
    - path: "banking_api_ui/src/components/TokenExchangePanel.js"
      provides: "Audited token exchange panel with correct may_act/act structure"
---

<objective>
Audit every education panel in the UI for technical accuracy. Compare each panel's claims against: (1) the actual live demo behaviour, (2) the referenced RFCs, and (3) PingOne's documented behaviour. Fix inaccurate descriptions, stale spec section numbers, and missing context.

Purpose: Education panels are the primary way presenters explain the demo to audiences. Inaccurate panels undermine credibility.

Output: All education panels corrected and accurate. No new panels created — this is an audit-and-fix pass only.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Audit AgentGatewayPanel RFC 9728 content</name>
  <files>banking_api_ui/src/components/AgentGatewayPanel.js</files>
  <action>
Read AgentGatewayPanel.js. For each RFC 9728 claim:
- Verify the /.well-known/oauth-protected-resource endpoint path is correct
- Verify the metadata fields described (authorization_servers, resource, bearer_methods_supported, scopes_supported) match the actual BFF implementation
- Fix any incorrect field names or missing fields
- Ensure the "live demo" fetch button points to the correct endpoint
  </action>
  <done>AgentGatewayPanel RFC 9728 content verified and corrected.</done>
</task>

<task type="auto">
  <name>Task 2: Audit OidcPanel PingOne-specific claims</name>
  <files>banking_api_ui/src/components/OidcPanel.js</files>
  <action>
Read OidcPanel.js. Verify:
- PKCE flow description matches the actual auth code + PKCE implementation in routes/oauth.js
- Nonce handling description is accurate
- PingOne-specific claims (azp, env, org) match what PingOne actually returns in ID tokens
- Logout URL format matches PingOne's actual end_session_endpoint
Fix any discrepancies found.
  </action>
  <done>OidcPanel PingOne claims verified and corrected.</done>
</task>

<task type="auto">
  <name>Task 3: Audit McpProtocolPanel spec version and tool lifecycle</name>
  <files>banking_api_ui/src/components/McpProtocolPanel.js</files>
  <action>
Read McpProtocolPanel.js. Verify:
- Spec version references say "2025-11-25" (not an older date)
- tools/list and tools/call JSON-RPC payloads match the actual MCP server implementation
- Auth challenge flow description matches what banking_mcp_server actually does on 401
- Error code ranges (-32000 to -32099) are correct per MCP spec
Fix any stale references.
  </action>
  <done>McpProtocolPanel spec references updated to 2025-11-25 and verified.</done>
</task>

<task type="auto">
  <name>Task 4: Audit TokenExchangePanel may_act and act claim structure</name>
  <files>banking_api_ui/src/components/TokenExchangePanel.js</files>
  <action>
Read TokenExchangePanel.js. Verify:
- The 1-exchange diagram shows: user token (with may_act) → MCP token (sub=user, no act)
- The 2-exchange diagram shows: user token (may_act) + agent token → MCP token (sub=user, act={client_id: agentId})
- The may_act claim structure shown uses URI format: { sub: "https://..." } not just a string
- The act claim structure shown is nested correctly per RFC 8693 §4.1
Fix any structural errors in the claim diagrams.
  </action>
  <done>TokenExchangePanel claim structures verified against RFC 8693 and current implementation.</done>
</task>

</tasks>

<verification>
1. `cd banking_api_ui && npm run build` → exit 0
2. Navigate to each education panel in the running app — no console errors
3. Cross-reference 3 RFC 9728 fields in AgentGatewayPanel against the BFF /.well-known route
4. Confirm McpProtocolPanel shows spec date 2025-11-25
</verification>

<success_criteria>
- All RFC references in panels are accurate
- No panel describes a non-existent flow or endpoint
- may_act/act claim structures match RFC 8693 §4.1 exactly
- Build passes with no new warnings
</success_criteria>
