---
phase: 110-fix-demo-data-page-layout-add-may-act-demo-button-fix-config-button-overflow-improve-discoverability
plan: "01"
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: []
files_modified:
  - banking_api_ui/src/components/DemoDataPage.js
  - banking_api_ui/src/components/DemoDataPage.css

must_haves:
  truths:
    - "A may_act quick-action card appears near the top of DemoDataPage (in or directly below the hero section)"
    - "The quick-action card shows current may_act status (✅/❌/loading) using existing mayActEnabled state"
    - "Enable and Clear buttons in the quick-action card wire to the existing handleMayActToggle handler"
    - "The quick-action card has a 'Full controls →' link that scrolls to #demo-mayact-heading"
    - "The full may_act section at line ~1472 is unchanged"
    - "The toolbar 'PingOne config' Link label is shortened to 'Config' (or '⚙ Config') with title='PingOne environment and OAuth client settings'"
    - "All toolbar buttons remain functional"
    - "npm run build exits 0"
  artifacts:
    - path: "banking_api_ui/src/components/DemoDataPage.js"
      provides: "may_act quick-action card near hero; shortened Config toolbar button"
      exports: ["default (DemoDataPage)"]
    - path: "banking_api_ui/src/components/DemoDataPage.css"
      provides: "Styles for the may_act quick-action card (.demo-data-mayact-quick)"
  key_links:
    - from: "DemoDataPage.js may_act quick-action"
      to: "DemoDataPage.js #demo-mayact-heading section"
      via: "onClick scrollIntoView or href='#demo-mayact-heading'"
      pattern: "demo-mayact-heading"
---

# Phase 110 Plan 01: may_act Quick-Action + Config Button Overflow Fix

## Objective

Two targeted changes to `DemoDataPage.js`:
1. Add a `may_act` quick-action card near the top of the page (below hero) showing current status + Enable/Clear shortcuts
2. Fix the toolbar "PingOne config" label that overflows/wraps on narrower screens

---

## Context References

Read before implementing:
- `banking_api_ui/src/components/DemoDataPage.js` — existing `mayActEnabled`, `mayActSaving`, `handleMayActToggle(true/false)` state (lines ~186-220); hero section (line ~663); toolbar (line ~616); full may_act section (line ~1472)
- `banking_api_ui/src/components/DemoDataPage.css` — existing styles and BEM patterns
- `banking_api_ui/src/components/UserDashboard.css` — `.dashboard-toolbar-btn` styles

---

## Task 1: may_act Quick-Action Card

<task id="110-01-T1" type="auto">
<name>Add may_act quick-action card below hero</name>

<read_first>
- banking_api_ui/src/components/DemoDataPage.js (lines 186-220 for state; lines 663-700 for hero section; lines 1472-1640 for existing full may_act section)
- banking_api_ui/src/components/DemoDataPage.css (for existing card/section patterns)
</read_first>

<action>
In `DemoDataPage.js`, insert a new `.demo-data-mayact-quick` card immediately after the closing `</div>` of the hero section (`demo-data-page__hero`, around line 680) and before the "Storage backend" section.

The card must:
- Show an `<h3>` or label: "may_act demo — quick controls"
- Show a status pill using existing `mayActEnabled` state:
  - `null` → "Checking…" (muted)
  - `true` → "✅ may_act present in token" (green)
  - `false` → "❌ may_act absent" (red/muted)
- Render an Enable button (same `handleMayActToggle(true)` call, disabled when `mayActEnabled === true || mayActSaving`)
- Render a Clear button (same `handleMayActToggle(false)` call, disabled when `mayActEnabled === false || mayActSaving`)
- Render a "Full controls ↓" link that scrolls to `#demo-mayact-heading` using `href="#demo-mayact-heading"` or `onClick={() => document.getElementById('demo-mayact-heading')?.scrollIntoView({ behavior: 'smooth' })}`

Reuse existing button class `demo-data-btn` for Enable/Clear — keep consistent with the full section below.

**Do NOT modify** the existing may_act section at line ~1472.

In `DemoDataPage.css`, add:
```css
.demo-data-mayact-quick {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 0.75rem;
  padding: 0.75rem 1rem;
  background: var(--surface-1, #f8fafc);
  border: 1px solid var(--border-light, #e2e8f0);
  border-radius: 8px;
  margin-bottom: 1rem;
}
.demo-data-mayact-quick__label {
  font-size: 0.8rem;
  font-weight: 600;
  color: var(--text-muted, #64748b);
  white-space: nowrap;
}
.demo-data-mayact-quick__status {
  font-size: 0.8rem;
  font-weight: 500;
}
.demo-data-mayact-quick__link {
  font-size: 0.78rem;
  color: var(--app-primary-red, #dc2626);
  text-decoration: underline;
  cursor: pointer;
  background: none;
  border: none;
  padding: 0;
}
```
</action>

<verification>
- DemoDataPage.js contains `demo-data-mayact-quick` className
- DemoDataPage.js contains `demo-mayact-heading` reference (scroll link)
- DemoDataPage.js contains `handleMayActToggle(true)` inside the quick-action card JSX
- DemoDataPage.css contains `.demo-data-mayact-quick` rule
- The original `mayActEnabled` state variable is reused (not duplicated)
</verification>

<acceptance_criteria>
- `grep "demo-data-mayact-quick" banking_api_ui/src/components/DemoDataPage.js` returns match
- `grep "demo-mayact-heading" banking_api_ui/src/components/DemoDataPage.js` returns ≥2 matches (existing heading + new scroll link)
- `grep "handleMayActToggle" banking_api_ui/src/components/DemoDataPage.js | wc -l` returns ≥4 (existing 2 + new 2)
- `grep "demo-data-mayact-quick" banking_api_ui/src/components/DemoDataPage.css` returns match
</acceptance_criteria>
</task>

---

## Task 2: Fix Toolbar "PingOne config" Button Overflow

<task id="110-01-T2" type="auto">
<name>Shorten "PingOne config" toolbar button label</name>

<read_first>
- banking_api_ui/src/components/DemoDataPage.js (lines 616-660 — toolbar section)
</read_first>

<action>
In `DemoDataPage.js`, find the toolbar `<Link to="/config" ...>` element (around line 633):

```jsx
<Link to="/config" className="dashboard-toolbar-btn" title="PingOne environment and OAuth client settings">
  PingOne config
</Link>
```

Change to:
```jsx
<Link to="/config" className="dashboard-toolbar-btn" title="PingOne environment and OAuth client settings">
  ⚙ Config
</Link>
```

This shortens the label from 13 characters to 8, preventing line-break at narrower widths while preserving the full label in the `title` tooltip for hover.

Do NOT change the `to="/config"` href, the `className`, or the `title` attribute.
</action>

<verification>
- DemoDataPage.js toolbar Link to="/config" has label "⚙ Config" (not "PingOne config")
- title attribute still reads "PingOne environment and OAuth client settings"
</verification>

<acceptance_criteria>
- `grep -A2 'to="/config"' banking_api_ui/src/components/DemoDataPage.js` shows "⚙ Config" as button text
- `grep 'PingOne config' banking_api_ui/src/components/DemoDataPage.js` returns 0 matches in the toolbar (may still appear in heading h1 — exclude those)
</acceptance_criteria>
</task>

---

## Task 3: Build Verification

<task id="110-01-T3" type="auto">
<name>Build verification</name>

<read_first>
- banking_api_ui/src/components/DemoDataPage.js (scroll to quick-action card insertion point to verify no JSX syntax errors)
</read_first>

<action>
Run: `cd banking_api_ui && npm run build`

Must exit 0. If build fails, fix the issue before committing.
</action>

<verification>
- `cd banking_api_ui && npm run build` exits 0
</verification>

<acceptance_criteria>
- Build output contains "The build folder is ready to be deployed." (or "Compiled successfully")
- No new TypeScript/ESLint errors introduced in DemoDataPage.js
</acceptance_criteria>
</task>

---

## Verification

All tasks complete when:
- [ ] may_act quick-action card renders in hero area with correct state wiring
- [ ] "⚙ Config" in toolbar (title tooltip preserved)
- [ ] `npm run build` → exit 0
- [ ] Full may_act section at line ~1472 unchanged

## Success Criteria

```
grep "demo-data-mayact-quick" banking_api_ui/src/components/DemoDataPage.js
grep "⚙ Config" banking_api_ui/src/components/DemoDataPage.js
cd banking_api_ui && npm run build
```
