---
phase: 112-marketing-and-dashboard-ui-polish-ensure-consistent-light-and-dark-mode
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - banking_api_ui/src/App.css
  - banking_api_ui/src/components/LandingPage.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "The /marketing route follows html[data-theme='dark'] (no forced white background)"
    - "LandingPage hero and body sections have correct dark surface colors"
    - "No hardcoded #fff or 'white' values remain on the LandingPage in App.css override block"
  artifacts:
    - path: banking_api_ui/src/App.css
      provides: "App--marketing-page white override REMOVED"
      contains: "REMOVED: App--marketing-page .main-content.*background.*white"
    - path: banking_api_ui/src/components/LandingPage.css
      provides: "Dark mode overrides for landing-hero and landing-page body"
      contains: "html\\[data-theme='dark'\\].*landing"
  key_links:
    - from: "App.css"
      to: "LandingPage.css"
      via: "Remove force-white, allow --dash-bg cascade to landing body"
      pattern: "data-theme.*dark.*landing"
---

<objective>
Fix dark mode on the `/marketing` route by removing the hardcoded `background: white` override from App.css and adding proper dark surface tokens to LandingPage.css so the landing hero and body sections render correctly in dark mode.

Purpose: D-2 from CONTEXT.md — marketing should follow user theme preference. The existing App.css override (`App--marketing-page .main-content { background: white }`) blocks the css cascade in dark mode.

Output: App.css override block removed; LandingPage.css updated with dark surface overrides for hero gradient and page body.
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/phases/112-marketing-and-dashboard-ui-polish-ensure-consistent-light-and-dark-mode/112-CONTEXT.md
@.planning/phases/112-marketing-and-dashboard-ui-polish-ensure-consistent-light-and-dark-mode/112-UI-SPEC.md
</execution_context>

<context>
@banking_api_ui/src/App.css
@banking_api_ui/src/components/LandingPage.css
@banking_api_ui/src/styles/dashboard-theme.css

<interfaces>
<!-- App.css lines ~229-240 (the block to REMOVE) -->
```css
/* Signed-in /marketing: <main> must stay white when html[data-theme=dark] */
html[data-theme='dark'] .App--marketing-page .main-content {
  background: white;
  color: inherit;
}
/* (possibly more rules in this block) */
```

<!-- dashboard-theme.css dark tokens (use these in LandingPage.css overrides) -->
html[data-theme='dark'] {
  --dash-bg: #0c1018;        /* page background */
  --dash-surface: #141a24;   /* card surface */
  --dash-surface-2: #1a2230; /* secondary surface */
  --dash-text: #e8edf5;      /* body text */
  --dash-muted: #94a3b8;     /* secondary text */
  --dash-border: rgb(148 163 184 / 0.18);
}

<!-- LandingPage key CSS classes (from LandingPage.css) -->
.landing-hero        → hero gradient section
.landing-page        → page root
.landing-header      → top nav bar
.landing-features    → feature cards row
.landing-cta-section → call-to-action block
.landing-footer      → footer strip
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove App--marketing-page white override from App.css</name>
  <files>banking_api_ui/src/App.css</files>
  <read_first>
    - banking_api_ui/src/App.css (read section around line 229-245 to see full override block before removing)
  </read_first>
  <action>
Find and remove the `App--marketing-page` dark mode override block from App.css. The block looks like:

```css
/* Signed-in /marketing: <main> must stay white when html[data-theme=dark] */
html[data-theme='dark'] .App--marketing-page .main-content {
  background: white;
  color: inherit;
}
```

Remove the entire block including the comment. Do NOT remove any other rules nearby.

After removal, verify no other `App--marketing-page`+ dark mode rules remain in the file.
  </action>
  <verify>
    <automated>grep -n "App--marketing-page.*white\|background.*white.*marketing" banking_api_ui/src/App.css</automated>
  </verify>
  <done>No `html[data-theme='dark'] .App--marketing-page .main-content { background: white }` rule exists in App.css. Command above returns empty output.</done>
</task>

<task type="auto">
  <name>Task 2: Add dark mode overrides to LandingPage.css</name>
  <files>banking_api_ui/src/components/LandingPage.css</files>
  <read_first>
    - banking_api_ui/src/components/LandingPage.css (read the full file to identify hardcoded light colors and understand existing dark rules already present)
    - banking_api_ui/src/styles/dashboard-theme.css (dark token values)
  </read_first>
  <action>
LandingPage.css already has some dark rules (toggle button). Append a comprehensive dark mode block at the END of the file covering the main layout sections:

```css
/* ─── Dark Mode ──────────────────────────────────────────────────────────── */

html[data-theme='dark'] .landing-page {
  background: var(--dash-bg, #0c1018);
  color: var(--dash-text, #e8edf5);
}

html[data-theme='dark'] .landing-header {
  background: rgba(12, 16, 24, 0.95);
  border-bottom: 1px solid rgb(148 163 184 / 0.14);
}

html[data-theme='dark'] .landing-hero {
  background: linear-gradient(135deg, #0c1018 0%, #1a2230 60%, #172554 100%);
  color: var(--dash-text, #e8edf5);
}

html[data-theme='dark'] .landing-hero h1,
html[data-theme='dark'] .landing-hero h2,
html[data-theme='dark'] .landing-hero p {
  color: var(--dash-text, #e8edf5);
}

html[data-theme='dark'] .landing-features {
  background: var(--dash-bg, #0c1018);
}

html[data-theme='dark'] .landing-feature-card,
html[data-theme='dark'] .landing-card {
  background: var(--dash-surface, #141a24);
  border: 1px solid rgb(148 163 184 / 0.18);
  color: var(--dash-text, #e8edf5);
}

html[data-theme='dark'] .landing-feature-card h3,
html[data-theme='dark'] .landing-card h3 {
  color: var(--dash-text, #e8edf5);
}

html[data-theme='dark'] .landing-feature-card p,
html[data-theme='dark'] .landing-card p {
  color: var(--dash-muted, #94a3b8);
}

html[data-theme='dark'] .landing-cta-section {
  background: var(--dash-surface, #141a24);
  border-top: 1px solid rgb(148 163 184 / 0.14);
}

html[data-theme='dark'] .landing-footer {
  background: #0a0e16;
  color: var(--dash-muted, #94a3b8);
  border-top: 1px solid rgb(148 163 184 / 0.14);
}
```

Note: If specific class names in LandingPage.css differ from the ones above (e.g., `.landing-hero` vs `.hero-section`), read the file first and adjust the selectors to match what actually exists. The important classes to cover are: page root, header, hero, feature cards, CTA section, and footer.
  </action>
  <verify>
    <automated>grep -c "html\[data-theme='dark'\]" banking_api_ui/src/components/LandingPage.css</automated>
  </verify>
  <done>LandingPage.css has at least 8 html[data-theme='dark'] rule blocks covering: page, header, hero, hero text, features, feature cards, CTA, footer</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App.css CSS cascade | Removing a CSS rule — no security boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-02-01 | Tampering | CSS visual | accept | CSS-only change, no auth or data affected |
</threat_model>

<verification>
After both tasks:

```bash
# Should return empty (no forced white on marketing)
grep "App--marketing-page.*background.*white\|white.*App--marketing-page" banking_api_ui/src/App.css

# Should return >= 8
grep -c "html\[data-theme='dark'\]" banking_api_ui/src/components/LandingPage.css
```
</verification>

<success_criteria>
- App.css no longer forces white background on marketing path in dark mode
- LandingPage.css has dark overrides for all major sections (page, header, hero, cards, CTA, footer)
- LandingPage still renders correctly in light mode (no regressions to light styles)
- No build errors
</success_criteria>

<output>
After completion, create `.planning/phases/112-marketing-and-dashboard-ui-polish-ensure-consistent-light-and-dark-mode/112-02-SUMMARY.md`
</output>
