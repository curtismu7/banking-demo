---
phase: 113
plan: 02
name: LandingPage hero Chase styling + SideNav Chase refinement
type: auto
tdd: false
wave: 1
depends_on: []
requirements: []
---

# Plan 02: LandingPage Hero + SideNav Chase Styling

**Objective:** Apply Chase color tokens to LandingPage hero section (without HTML restructuring), and refine SideNav styling to use Chase navy.

**Context:** @113-CONTEXT.md

---

## Tasks

### Task 1: Update LandingPage hero styling

**Description:** Apply Chase navy background, white typography, and CTA button colors to the landing page hero section.

**Files:** `banking_api_ui/src/components/LandingPage.js`, `banking_api_ui/src/components/LandingPage.css`

**Implementation notes:**
- NO HTML changes — keep existing hero structure
- Update `.landing-hero` background to `background: linear-gradient(135deg, var(--chase-navy) 0%, var(--chase-navy-dark) 100%)` or solid `var(--chase-navy)`
- Update `.landing-hero-headline` to `color: white`
- Update `.landing-hero-subheadline` to `color: var(--chase-light-gray)` or `#f0f0f0`
- CTA primary button: `background: white`, `color: var(--chase-navy)` (inverted)
- CTA secondary button: `background: transparent`, `border: 2px solid white`, `color: white`
- Update `.landing-features` background to something light (white or `var(--chase-light-gray)`) for contrast
- Add `html[data-theme='dark']` overrides if needed to preserve readability in dark mode

### Task 2: Refine SideNav styling for Chase navy

**Description:** Update SideNav.css to consistently use Chase navy (`--chase-navy`) as the primary color.

**Files:** `banking_api_ui/src/components/SideNav.css`

**Implementation notes:**
- `.sidenav` background: `var(--chase-navy)` (or ensure it already is — Phase 85 may have done this)
- `.sidenav-brand-name`: `color: white`
- `.sidenav-nav-item`: `color: white`, hover: `background: rgba(255, 255, 255, 0.1)`, active: `background: rgba(255, 255, 255, 0.2)`
- `.sidenav-footer` (theme toggle): `color: white`
- Dark mode: SideNav should invert colors — `.sidenav` background stays navy, but text may need lightening or status colors adjust
- Icons/separators: use white or subtle grays for visibility on navy background

### Task 3: Verify Phase 112 dark mode doesn't break

**Description:** Ensure dark mode CSS rules from Phase 112 still work with new Chase colors.

**Implementation notes:**
- Check `html[data-theme='dark']` blocks in LandingPage.css and SideNav.css
- Make sure `--dash-bg`, `--dash-surface` tokens are used where appropriate (or converted to `--chase-*` equivalents)
- Test both light and dark modes on the landing page

---

## Verification

✅ **All checks:**
- `npm run build` in `banking_api_ui/` exits 0
- LandingPage hero displays with Chase navy background, white headline, contrasting buttons
- CTA buttons are clickable and styled correctly
- Dark mode toggle on LandingPage works; dark hero styling is legible
- SideNav (if visible in admin pages) shows Chase navy background with white text
- No CSS conflicts or broken layouts

---

## Success Criteria

- [ ] LandingPage hero uses Chase navy, white text, styled CTAs (no HTML changes)
- [ ] SideNav background is Chase navy with white text and proper hover states
- [ ] Dark mode rules don't break new Chase colors
- [ ] Build passes
- [ ] Commit: `feat(113-02): apply Chase styling to LandingPage hero + refine SideNav colors`

