---
phase: 114
plan: 02
name: Register IETFStandardsPanel in EducationPanelsHost and wire SideNav integration
type: auto
tdd: false
wave: 1
depends_on: [114-01]
requirements: [IETF-EDU-02]
files_modified: [banking_api_ui/src/components/education/educationIds.js, banking_api_ui/src/components/education/EducationPanelsHost.js, banking_api_ui/src/components/education/educationCommands.js, banking_api_ui/src/components/SideNav.js]
autonomous: true
---

# Plan 02: IETFStandardsPanel Registration & SideNav Integration

**Objective:** Register the IETFStandardsPanel component in the education system, add education command support, and surface it from SideNav so users can discover and open the IETF Standards drawer.

**Context:** @114-CONTEXT.md

**Depends on:** Plan 01 (IETFStandardsPanel.js component)

---

## Tasks

### Task 1: Add IETF_STANDARDS to educationIds.js

**Files:**
- `banking_api_ui/src/components/education/educationIds.js`

**Read first:**
- `banking_api_ui/src/components/education/educationIds.js` — Current ID pattern, naming convention

**Action:**
Add a new education ID constant for IETF Standards panel:

```javascript
// After existing IDs like BEST_PRACTICES, TOKEN_EXCHANGE, HUMAN_LOOP, etc.

IETF_STANDARDS: 'ietf-standards',
```

**Acceptance criteria:**
- ✅ File contains `IETF_STANDARDS: 'ietf-standards'` entry (exact string value)
- ✅ Entry placed after existing education IDs (alphabetical or chronological order respected)
- ✅ Syntax valid JavaScript (trailing comma if not last)

**Done:**
- Education ID registered and available for reference throughout app

---

### Task 2: Register IETFStandardsPanel in EducationPanelsHost.js

**Files:**
- `banking_api_ui/src/components/education/EducationPanelsHost.js`

**Read first:**
- `banking_api_ui/src/components/education/EducationPanelsHost.js` — Panel registration pattern, import and mapping structure
- `banking_api_ui/src/components/education/IETFStandardsPanel.js` — Component signature (from Plan 01)

**Action:**
1. Add import at top of file (after other panel imports):
   ```javascript
   import IETFStandardsPanel from './IETFStandardsPanel';
   ```

2. Add to panel registry/mapping (typically a `panelMap` or `panelRegistry` object). Example mapping:
   ```javascript
   const panels = {
     // ... existing panels
     [educationIds.IETF_STANDARDS]: {
       Component: IETFStandardsPanel,
       title: 'IETF Standards Stack for Agentic Identity',
       description: '7 IETF drafts where Ping Identity is co-author, mapped to IDC AI governance guardrails'
     }
   };
   ```

**Acceptance criteria:**
- ✅ `import IETFStandardsPanel from './IETFStandardsPanel'` present in imports section
- ✅ Panel mapped to `educationIds.IETF_STANDARDS` in registry
- ✅ Panel entry includes `Component`, `title`, `description` fields
- ✅ `npm run build` passes

**Done:**
- IETFStandardsPanel registered and discoverable by EducationPanelsHost

---

### Task 3: Add IETF Standards to educationCommands.js

**Files:**
- `banking_api_ui/src/components/education/educationCommands.js`

**Read first:**
- `banking_api_ui/src/components/education/educationCommands.js` — Command structure, command palette integration pattern

**Action:**
Add entry to education commands list. Example structure:
```javascript
// After existing commands like SHOW_TOKEN_EXCHANGE, SHOW_HUMAN_LOOP, etc.

{
  id: educationIds.IETF_STANDARDS,
  label: 'Show IETF Standards',
  description: 'Open the IETF agentic identity standards education drawer',
  keybinding: '?'  // or leave empty if no keybinding desired
}
```

**Acceptance criteria:**
- ✅ Command entry uses `educationIds.IETF_STANDARDS` as ID
- ✅ Label is "Show IETF Standards" (or similar descriptive text)
- ✅ Description explains what the command does
- ✅ Command appears in command palette when invoked

**Done:**
- Users can invoke IETF Standards panel via education command palette

---

### Task 4: Add IETF Standards entry to SideNav.js

**Files:**
- `banking_api_ui/src/components/SideNav.js`

**Read first:**
- `banking_api_ui/src/components/SideNav.js` — SideNav structure, education menu items pattern
- `banking_api_ui/src/components/education/educationIds.js` — Education ID constant

**Action:**
1. Import educationIds at top of file:
   ```javascript
   import { educationIds } from './education/educationIds';
   ```

2. Add menu entry in the education section of SideNav (typically in a dropdown or menu list). Example:
   ```jsx
   {
     icon: 'standards',  // or appropriate icon
     label: 'IETF Standards',
     onClick: () => {
       // Invoke education command to open IETFStandardsPanel
       onEducationCommand(educationIds.IETF_STANDARDS);
     }
   }
   ```
   
   Or if using command palette:
   ```jsx
   {
     id: educationIds.IETF_STANDARDS,
     label: 'IETF Standards',
     category: 'Education'
   }
   ```

3. Position it logically in SideNav education section (near other standards/reference materials)

**Acceptance criteria:**
- ✅ SideNav renders "IETF Standards" menu item or link
- ✅ Clicking the item opens IETFStandardsPanel (or routes to education command)
- ✅ No console errors when clicking
- ✅ `npm run build` passes

**Done:**
- Users can open IETFStandardsPanel directly from SideNav
- Visual path: SideNav → IETF Standards (button/link) → Opens education drawer

---

## Verification

- All 4 files modified without errors
- Build passes: `npm run build` exit code 0
- SideNav renders "IETF Standards" entry
- Clicking entry opens IETFStandardsPanel without errors
- Panel displays all 8 tabs as created in Plan 01

