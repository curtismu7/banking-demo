---
phase: 119-call-mcp-server-and-get-tools-without-authenticating-user
plan: 02
type: execute
wave: 2
depends_on: ["119-01"]
files_modified:
  - banking_api_server/__tests__/routes/mcpDiscoveryRoutes.test.js
  - banking_api_server/__tests__/integration/mcp-discovery.integration.test.js
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "GET /.well-known/mcp-server returns 200 with correct tool list"
    - "GET /api/mcp/tools returns 200 with identical tool list"
    - "Rate limit exactly 100 requests/min per IP enforces correctly"
    - "Response excludes all 4 banking tools (get_my_accounts, create_transfer, create_deposit, create_withdrawal)"
    - "Tool schemas serialize to valid JSON Schema format"
  artifacts:
    - path: "banking_api_server/__tests__/routes/mcpDiscoveryRoutes.test.js"
      provides: "Unit tests for discovery handler logic"
      min_lines: 80
    - path: "banking_api_server/__tests__/integration/mcp-discovery.integration.test.js"
      provides: "E2E tests for endpoints + rate limiting"
      min_lines: 60
  key_links:
    - from: "test suite"
      to: "mcpDiscoveryRoutes.js"
      via: "require() in test file"
      pattern: "require.*mcpDiscoveryRoutes|mcpDiscoveryRoutes\\.js"
---

<objective>
Test Phase 119 discovery endpoints (Unit + Integration) (Phase 119, Task 2 of 2).

Purpose: Validate that discovery endpoints work correctly, tool whitelist is enforced, and rate limiting prevents abuse.

Output:
- Unit tests for PUBLIC_TOOLS filter, schema conversion, response structure
- Integration tests for /.well-known/mcp-server and /api/mcp/tools endpoints
- Rate limit enforcement tests
- All tests passing
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/119-call-mcp-server-and-get-tools-without-authenticating-user/119-CONTEXT.md
@.planning/phases/119-call-mcp-server-and-get-tools-without-authenticating-user/119-RESEARCH.md
@.planning/phases/119-call-mcp-server-and-get-tools-without-authenticating-user/119-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unit tests for discovery route logic</name>
  <files>banking_api_server/__tests__/routes/mcpDiscoveryRoutes.test.js</files>
  <behavior>
    - Test 1: getPublicToolsList() returns only 2 tools (explain_topic and brave_search)
    - Test 2: getPublicToolsList() filters out all 4 banking tools (get_my_accounts, create_transfer, create_deposit, create_withdrawal)
    - Test 3: toolToJsonSchema() converts Zod object to { name, description, schema } with valid JSON schema properties
    - Test 4: Response has required fields: server.name, server.version, tools[], requiresAuthentication (false), rateLimit
    - Test 5: toolToJsonSchema() handles edge case where schema description is missing (uses default)
  </behavior>
  <action>
Create `banking_api_server/__tests__/routes/mcpDiscoveryRoutes.test.js`

**Test setup:**
```javascript
const express = require('express');
const request = require('supertest');
const mcpDiscoveryRoutes = require('../../routes/mcpDiscoveryRoutes');

describe('MCP Discovery Routes', () => {
  let app;
  
  beforeEach(() => {
    app = express();
    app.use('/.well-known/mcp-server', mcpDiscoveryRoutes);
  });

  // Test suite here
});
```

**Must-have tests:**

1. **Tool Whitelist Filter (Test 1)**
   - Arrange: Call getPublicToolsList()
   - Act: Filter result to PUBLIC_TOOLS constant
   - Assert: List contains exactly 2 tools with names 'explain_topic' and 'brave_search'

2. **Banking Tool Exclusion (Test 2)**
   - Arrange: Call getPublicToolsList()
   - Act: Extract tool names
   - Assert: Tool names do NOT include 'get_my_accounts', 'create_transfer', 'create_deposit', 'create_withdrawal'

3. **JSON Schema Conversion (Test 3)**
   - Arrange: Sample Zod schema from banking_api_server/utils/mcpToolRegistry.js
   - Act: Call toolToJsonSchema(tool)
   - Assert: Result has structure { name: string, description: string, schema: { type: 'object', properties: {...}, required: [...] } }

4. **Response Structure (Test 4)**
   - Arrange: Mock Express app with discovery routes
   - Act: GET /.well-known/mcp-server
   - Assert: Response has fields: server.name, server.version, tools (array), requiresAuthentication (false), rateLimit (with requestsPerMinute: 100)

5. **Missing Schema Description (Test 5)**
   - Arrange: Tool with undefined description
   - Act: Call toolToJsonSchema(tool)
   - Assert: Result uses default description or empty string (not null/undefined in JSON)

**Validation targets:**
- All tests pass: `npm test -- mcpDiscoveryRoutes.test.js`
- Coverage: >80% on mcpDiscoveryRoutes.js
- No console errors during test run
  </action>
  <verify>
    <automated>npm test -- mcpDiscoveryRoutes.test.js 2>&1 | grep -q "passed" && echo "✓ Unit tests passed"</automated>
  </verify>
  <done>
    - Unit test file created at __tests__/routes/mcpDiscoveryRoutes.test.js
    - 5 test cases implemented (whitelist, exclusion, schema conversion, response structure, edge cases)
    - All tests passing
    - ≥80% code coverage on mcpDiscoveryRoutes.js
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests for endpoints + rate limiting</name>
  <files>banking_api_server/__tests__/integration/mcp-discovery.integration.test.js</files>
  <behavior>
    - Test 1: GET /.well-known/mcp-server returns 200 with valid JSON
    - Test 2: GET /api/mcp/tools returns 200 with same tool list as /.well-known/mcp-server
    - Test 3: Both endpoints accept direct GET (no authentication required)
    - Test 4: Sending 101 requests in rapid succession results in 429 on requests 101+
    - Test 5: Rate limit resets after 60 seconds (or uses key expiry)
    - Test 6: Rate limit uses IP address as key (different client IPs get separate buckets)
  </behavior>
  <action>
Create `banking_api_server/__tests__/integration/mcp-discovery.integration.test.js`

**Test setup:**
```javascript
const request = require('supertest');
const app = require('../../server');

describe('MCP Discovery Integration Tests', () => {
  // Ensure app is running with discovery routes registered

  // Test suite here
});
```

**Must-have tests:**

1. **Well-Known Endpoint Health (Test 1)**
   - Act: GET /.well-known/mcp-server
   - Assert: Status 200, Content-Type: application/json, body has server.name, tools[]

2. **API Endpoint Health (Test 2)**
   - Act: GET /api/mcp/tools
   - Assert: Status 200, body.tools == body from /.well-known/mcp-server (deep equal)

3. **No Auth Required (Test 3)**
   - Act: GET /.well-known/mcp-server (no Authorization header)
   - Assert: Status 200 (not 401)
   - Act: GET /api/mcp/tools (no Authorization header)
   - Assert: Status 200 (not 401)

4. **Rate Limit Enforcement (Test 4)**
   - Arrange: Disable rate limit for other tests (set DISABLE_RATE_LIMIT=true); enable for this test
   - Act: Send 101 requests from same IP in rapid succession
   - Assert: Requests 1-100 get 200; requests 101+ get 429
   - Assert: Response includes Retry-After header

5. **Rate Limit Resets (Test 5)** [Optional - can skip if slow test interferes]
   - Arrange: Send 100 requests (hit limit)
   - Act: Wait 61 seconds
   - Act: Send 1 more request
   - Assert: Status 200 (limit reset)
   - Note: Mark as slow test with @slow or comment if needed

6. **IP-based Rate Limit Bucketing (Test 6)**
   - Arrange: Mock two different X-Forwarded-For headers
   - Act: Send 100 requests with header A, then 1 request with header B
   - Assert: 100 requests with A → 100th is 200, 1 request with B → 200 (separate bucket)

**Validation targets:**
- All tests pass: `npm test -- mcp-discovery.integration.test.js`
- Rate limit behavior matches 100 req/min per IP specification
- No console errors during test run
  </action>
  <verify>
    <automated>npm test -- mcp-discovery.integration.test.js 2>&1 | grep -q "passed" && echo "✓ Integration tests passed"</automated>
  </verify>
  <done>
    - Integration test file created at __tests__/integration/mcp-discovery.integration.test.js
    - 6 test cases implemented (endpoint health, dual endpoint parity, no-auth requirement, rate limiting, reset, IP bucketing)
    - All tests passing
    - Rate limit behavior validated against 100 req/min per IP spec
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite → Discovery handler | Tests verify that whitelist filter works correctly and rate limiter cannot be bypassed |
| Rate limit tests → Backend | Tests simulate multiple IPs to verify IP-based bucketing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-05 | Testing Coverage | Rate limiter | mitigate | Integration test sends 101 requests to verify 100 req/min enforcement works |
| T-119-06 | Testing Coverage | Tool whitelist | mitigate | Unit test explicitly checks that all 4 banking tools are excluded from PUBLIC_TOOLS |

</threat_model>

<verification>

**Test execution checklist:**

1. **Run all tests:**
   ```bash
   npm test -- mcpDiscoveryRoutes.test.js mcp-discovery.integration.test.js
   ```
   Expected: ✅ All tests passing

2. **Check coverage:**
   ```bash
   npm test -- --coverage mcpDiscoveryRoutes.test.js
   ```
   Expected: ≥80% coverage on mcpDiscoveryRoutes.js

3. **Verify no console noise:**
   ```bash
   npm test -- mcpDiscoveryRoutes.test.js 2>&1 | grep -i "error\|warning" | grep -v "expected\|assertion"
   ```
   Expected: No unexpected errors

</verification>

<success_criteria>

✅ mcpDiscoveryRoutes.test.js created with 5 unit tests  
✅ mcp-discovery.integration.test.js created with 6 integration tests  
✅ All 11 tests passing  
✅ Tool whitelist correctly filters (only 2 tools exposed)  
✅ Banking tools excluded (get_my_accounts, create_transfer, create_deposit, create_withdrawal not in response)  
✅ Rate limiting enforces 100 req/min per IP (429 on request 101)  
✅ Both endpoints (/.well-known/mcp-server and /api/mcp/tools) return identical tool list  
✅ No authentication required for discovery endpoints  
✅ JSON Schema conversion works correctly  

</success_criteria>

<output>
After completion, create `.planning/phases/119-call-mcp-server-and-get-tools-without-authenticating-user/119-02-SUMMARY.md`
</output>
