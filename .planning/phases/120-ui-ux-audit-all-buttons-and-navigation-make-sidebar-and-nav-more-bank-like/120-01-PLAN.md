---
phase: "120-ui-ux-audit-all-buttons-and-navigation-make-sidebar-and-nav-more-bank-like"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: 
  - "banking_api_ui/package.json"
  - "banking_api_ui/src/components/SideNav.js"
  - "banking_api_ui/src/components/SideNav.css"
autonomous: true
requirements: ["NAV-01"]
user_setup: []

must_haves:
  truths:
    - "All emoji icons in SideNav are replaced with React Icons line icons"
    - "Icon mapping is consistent (Font Awesome or Material Design set, not mixed)"
    - "Sidebar spacing is tightened to match banking app density (15-16 items visible above fold)"
    - "npm run build completes without errors"
  artifacts:
    - path: "banking_api_ui/package.json"
      provides: "react-icons dependency"
      contains: "react-icons"
    - path: "banking_api_ui/src/components/SideNav.js"
      provides: "Icon imports and replacement"
      exports: ["20+ icons via React Icons"]
    - path: "banking_api_ui/src/components/SideNav.css"
      provides: "Tightened spacing"
      contains: "padding: 12px 14px"
  key_links:
    - from: "SideNav.js"
      to: "react-icons package"
      via: "ES6 imports"
      pattern: "import.*from.*react-icons"
    - from: "SideNav.css"
      to: "icon rendering"
      via: "link padding + gap adjustments"
      pattern: ".sidenav-link.*padding"
---

<objective>
**Goal:** Replace all 20+ emoji icons with professional line icons and tighten sidebar spacing to match banking density standards.

**Purpose:** Foundation for banking-like visual refresh; unblocks Wave 2 (active state + colors) and Wave 3 (top nav) tasks.

**Output:** 
- React Icons integrated as dependency
- All emoji → line icons mapping complete
- Sidebar spacing reduced; 15-16 items now visible above fold
- Build passes with zero new errors
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/phases/120-*/120-CONTEXT.md
@.planning/phases/120-*/120-RESEARCH.md
@banking_api_ui/src/components/SideNav.js
@banking_api_ui/src/components/SideNav.css
</execution_context>

<context>
**Current State (from RESEARCH.md §2):**
- SideNav.js: 20+ emoji icons defining nav items (ADMIN_NAV + USER_NAV)
- SideNav.css: Loose padding (~16–20px), large group gaps (~20px)
- Display: ~12–13 items above fold (vs banking target: 15–16)

**Phase 120 Decisions Implemented:**
- **D-01:** Sidebar Visual Design — Replace emoji with line icons (React Icons)
- **D-02:** Sidebar Spacing — Tighten padding and gaps

**Icon Map (from RESEARCH.md §2):**
Use Font Awesome (FaBank, FaUniversity) + Material Design (MdLock, MdSettings, etc.) for consistency.

**Spacing Targets (from RESEARCH.md §3):**
- Link padding: 12px 14px (from ~16px 16px)
- Group margin: 12px 0 (from ~20px 0)
- Icon + label gap: 8px (from ~12px)
- Result: 43% increase in density
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install react-icons and set up icon imports</name>
  <files>banking_api_ui/package.json, banking_api_ui/src/components/SideNav.js</files>
  <action>
    1. Run `npm install react-icons@6.3.0` in banking_api_ui/ (install latest stable version)
    2. In SideNav.js, add import block for all required React Icons:
       - Font Awesome: FaBank, FaUniversity (banking)
       - Material Design: MdLock, MdSettings, MdSearch, MdPayment, MdConfig, MdDataUsage, MdDeploy, MdBook, MdPerson, MdExtension, MdDescription, MdListAlt, MdSwapHoriz, MdSwapCalls
       - Heroicons: HiOutlineUsers, HiOutlineChat, HiOutlineBarChart3 (as alternatives if preferred)
       - Example: `import { FaBank, MdLock } from 'react-icons/md';` (multiple imports from single source)
    3. Create icon mapping constant in SideNav.js:
       ```javascript
       const ICON_MAP = {
         '🏦': FaBank,
         '🔐': MdLock,
         '💬': HiOutlineChat,
         '👥': HiOutlineUsers,
         // ... 16+ more entries (see decision D-01 map in CONTEXT.md)
       };
       ```
    4. Verify no import errors by running `npm run build` (don't commit yet)
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -i "error" | wc -l</automated>
  </verify>
  <done>
    - react-icons is installed and available in package.json
    - All required icons are imported without errors
    - Icon mapping constant created with 20+ emoji → React Icon entries
    - npm run build shows zero import errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace emoji with React Icons in SideNav.js nav structure</name>
  <files>banking_api_ui/src/components/SideNav.js</files>
  <action>
    1. Replace emoji in ADMIN_NAV:
       - Update each nav item: change `icon: '🏦'` to `icon: 'FaBank'` (string reference)
       - Process all 5 groups: Overview, Management, Configuration, User Management, Developer Tools
       - Reference icon map created in Task 1
    2. Replace emoji in USER_NAV:
       - Update nav items for 2 groups: My Banking, Self Service
       - Ensure icons match user context (no admin-only icons like 🔧)
    3. Update icon rendering in JSX:
       - Find the line where nav items render icons (likely `<span>{item.icon}</span>` or similar)
       - Replace with: `{const Icon = ICON_MAP[item.icon]; return <Icon size={20} />}`
       - Add CSS class for icon sizing: `className="sidenav-icon"`
       - Ensure fallback if icon not in map (e.g., render text emoji as safety net)
    4. Test each nav group renders correctly (visual inspection)
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - All emoji in ADMIN_NAV replaced with React Icon strings
    - All emoji in USER_NAV replaced with React Icon strings
    - JSX rendering loop correctly instantiates Icon components
    - No console errors on page load
    - npm run build completes without errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Update SideNav.css spacing to tighten layout</name>
  <files>banking_api_ui/src/components/SideNav.css</files>
  <action>
    1. Identify current spacing selectors (likely .sidenav-link, .sidenav-group, .sidenav-item):
       - Use grep or visual inspection of SideNav.css
       - Note: May have inline styles in SideNav.js as well (check and consolidate to CSS)
    2. Update padding values:
       - `.sidenav-link`: Change from `padding: 16px 16px;` (or similar) to `padding: 12px 14px;`
       - Icon container (if separate): Ensure no extra padding; use margin for spacing icon + label
       - Group labels: Set padding to `10px 14px 6px 14px;` (top, right, bottom, left)
    3. Update margins:
       - `.sidenav-group`: Change from `margin: 20px 0;` to `margin: 12px 0;`
       - `.sidenav-group:first-child`: Set `margin-top: 0;` (no top gap on first group)
       - `.sidenav-group-label`: Add `margin-bottom: 8px;` (gap between label and items)
    4. Update icon + label gap:
       - Icon element: Ensure `margin-right: 8px;` (vs previous ~12px)
       - Verify alignment: icon + text on same line, vertically centered
    5. Test responsive behavior:
       - Sidebar should collapse gracefully on mobile (<768px)
       - Width should remain 220px (unchanged)
       - No horizontal scroll on 320px viewport
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "error|warning" | head -5</automated>
  </verify>
  <done>
    - .sidenav-link padding reduced to 12px 14px
    - .sidenav-group margin tightened to 12px 0
    - Icon + label gap reduced to 8px
    - Group labels properly spaced
    - Responsive behavior preserved (no layout breaks on mobile)
    - npm run build passes with zero CSS errors
    - Visual inspection: 15–16 items now visible above fold (vs. 12–13 previously)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - React Icons installed and integrated
    - All emoji icons replaced with professional line icons
    - Sidebar spacing tightened to match banking density
  </what-built>
  <how-to-verify>
    1. Start the dev server: `cd banking_api_ui && npm start`
    2. Log in as admin (navigate to /admin or /agent)
    3. Verify sidebar displays:
       - No emoji anywhere (all replaced with line icons)
       - Icons are clean, professional (from React Icons)
       - Group labels are visible and styled
       - More items visible above fold than before (~15–16 vs 12–13)
    4. Verify user nav by logging in as customer (if applicable)
    5. Hover over nav items — no console errors in DevTools
    6. Run `npm run build` in banking_api_ui/ — must exit code 0
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Icon display | Untrusted nav structure could inject malicious SVG (mitigated: React Icons pre-vetted) |
| CSS rendering | Missing icon might crash component (mitigated: fallback to emoji if icon not found) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-01 | Tampering | Icon rendering | accept | Icons sourced from trusted npm package (react-icons, 10M+ weekly downloads) |
| T-120-02 | Information | CSS spacing | accept | Purely UI; no security relevance |

</threat_model>

<verification>
**Build Verification:**
- `npm run build` in banking_api_ui/ must exit with code 0
- No CSS errors or import errors in console
- No React warnings about missing props or undefined values

**Functional Verification:**
- Admin sidebar loads without errors
- All 5 admin groups visible with correct icons
- User sidebar loads without errors
- All 2 user groups visible with correct icons
- Spacing visually matches banking app density (tight, professional)

**Regression Verification:**
- SideNav.js still exports correctly (no breaking changes to component signature)
- Navigation links still work (all `to` props functional)
- Admin-only items still respect role gates
- Collapsible behavior preserved (if implemented)
</verification>

<success_criteria>
1. All 20+ emoji icons replaced with React Icons (zero emoji remaining)
2. Icon set is consistent (primary: Font Awesome + Material Design, not mixed random sets)
3. Spacing is tightened: 15–16 items visible above fold (43% improvement from 12–13)
4. Build passes: `npm run build` exit code 0
5. No visual regressions: all nav routes still work, no broken links
6. Mobile responsive: sidebar works correctly on 320px–1920px
7. Wave 1 complete: ready to unblock Wave 2 (colors + active state) and Wave 3 (top nav)
</success_criteria>

<output>
After completion, create `.planning/phases/120-ui-ux-audit-all-buttons-and-navigation-make-sidebar-and-nav-more-bank-like/120-01-SUMMARY.md`
</output>
