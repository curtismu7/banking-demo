---
phase: 132-full-end-to-end-testing-of-pingone-test-page-verify-all-token-acquisition-token-exchange-config-assets-and-decoded-token-display
plan: 02
type: execute
wave: 2
depends_on: ["132-01"]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "Worker token card on /pingone-test shows a valid token and decoded panel"
    - "Authz token card shows 'passed' or 'Login required' correctly based on session state"
    - "Agent token card runs successfully and shows decoded claims"
    - "Exchange 1 (User→MCP) test runs and shows pass or a meaningful error message"
    - "Exchange 2 (User+Agent→MCP) test runs and shows pass or a meaningful error message"
    - "Exchange 3 (User→Agent→MCP) test runs and shows pass or a meaningful error message"
    - "Configuration section shows env var names, format hints, and inline amber fix messages for each failed config item"
    - "Resources/Assets section loads the app→resource→scope table without errors"
    - "Decoded token panels render properly when decoded data is present (no blank panels)"
  artifacts: []
  key_links: []
---

<objective>
Full end-to-end human verification of the /pingone-test page after the decodeJwtForDisplay fix lands.

Covers all 6 functional areas:
1. Worker token acquisition + decoded panel
2. Authz token (session-based) + decoded panel
3. Agent token (client credentials) + decoded panel
4. Token Exchange 1: User→MCP
5. Token Exchange 2: User+Agent→MCP (double exchange)
6. Token Exchange 3: User→Agent→MCP (chained)
7. Configuration section — inline env var names, format hints, amber fix messages
8. Assets/Resources — app→resource→scope rich table
9. Decoded token display for all acquired tokens

Purpose: Confirm the test page as a whole is fully functional as a diagnostic/testing tool for PingOne integration.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: End-to-end verification of /pingone-test page</name>
  <what-built>
    - Plan 01 added `decodeJwtForDisplay` helper to `pingoneTestRoutes.js`, fixing ReferenceError that caused all token endpoints to return `success: false` with no decoded data.
    - All 7 token endpoint call sites already reference the function — they now work as intended.
  </what-built>
  <how-to-verify>
    1. Ensure the API server is running (ports 3001 or 3002 per `REACT_APP_API_PORT`)
    2. Navigate to: http://localhost:4000/pingone-test (or https://api.pingdemo.com:4000/pingone-test)
    3. **Worker Token section**: Confirm it loads automatically — shows a token expiry timer and a Decoded panel with `header` (alg, kid) and `payload` (sub, exp, etc.) fields. Previously showed error `decodeJwtForDisplay is not defined`.
    4. **Authz Token card**: If logged in, click "Test Authorization Token" — should show 'passed' + decoded panel. If not logged in, should show a Login button (not a crash).
    5. **Agent Token card**: Click "Test Agent Token" — should acquire a client_credentials token and show decoded panel.
    6. **Exchange 1 (User→MCP)**: Click Test — should pass or show a meaningful PingOne error message (not a server crash / ReferenceError).
    7. **Exchange 2 (User+Agent→MCP)**: Click Test — same — pass or meaningful error.
    8. **Exchange 3 (Full chain)**: Click Test — same.
    9. **Configuration section**: Scroll down — each config item should show its env var name (e.g. `PINGONE_ENVIRONMENT_ID`) and format hint. Failed items show amber inline fix message. (Phase 131 feature)
    10. **Assets/Resources table**: Click "Verify Assets" — should load the app→resource→scope table from Phase 130.
    11. Check browser console — should be free of `decodeJwtForDisplay is not defined` errors.
    12. Check API server log (`cat .logs/banking-api.log | tail -20`) — no `decodeJwtForDisplay is not defined` entries.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 12 checks pass.
    Or describe any remaining issues (e.g. "Exchange 1 still fails with scope error") — these will be logged as follow-up items.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
None — this is a verification-only plan with no code changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-02 | N/A | Verification plan | accept | No attack surface introduced — read-only checkpoint |
</threat_model>

<verification>
Human checkpoint — see task above.
</verification>

<success_criteria>
- User types "approved" after completing all 12 verification steps
- OR user identifies specific remaining issues for targeted follow-up
</success_criteria>

<output>
After completion, create `.planning/phases/132-full-end-to-end-testing-of-pingone-test-page-verify-all-token-acquisition-token-exchange-config-assets-and-decoded-token-display/132-02-SUMMARY.md`
</output>
