---
phase: "136-token-chain-reliability-audit-and-hardening-make-foolproof"
plan: "03"
type: execute
wave: 2
depends_on: ["136-01", "136-02"]
files_modified:
  - "banking_api_ui/src/components/TokenChainDisplay.js"
  - "banking_api_ui/src/components/TokenChainDisplay.css"
autonomous: false
requirements: ["TC-05"]

must_haves:
  truths:
    - "Token chain shows real user session data after login (not placeholder)"
    - "Token chain updates after an AI agent tool call"
    - "Token chain shows friendly sub/act names (from identityHints fix in conflict resolution)"
    - "Token chain shows the empty state message (not generic placeholder) when signed in but no events"
    - "Server restart does not permanently break the chain — synthesis fallback kicks in"
  artifacts:
    - path: "banking_api_ui/src/components/TokenChainDisplay.js"
      provides: "All fixes from plans 01 and 02 working together"
  key_links:
    - from: "User login"
      to: "Token chain seeded"
      via: "useCurrentUserTokenEvent → setSessionToken OR synthesizeFromSession fallback"
      pattern: "useCurrentUserTokenEvent|synthesizeFromSession"
---

<objective>
Human verification checkpoint — confirm all token chain fixes from plans 01 and 02 are working correctly end-to-end in a running browser session.

Purpose: Catch any integration issues between server-side synthesis fallback and client-side seeding before marking phase done.
Output: Confirmed working chain or bug report for follow-up fix.
</objective>

<execution_context>
@.planning/ROADMAP.md
</execution_context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 01: Fixed oauth.js to use decoded.sub as userId key; added synthesizeFromSession fallback in /api/token-chain for post-restart resilience.
    Plan 02: Added useCurrentUserTokenEvent to UserDashboard; removed stale fetchSessionPreview guard; added tcd-empty-state visible message.
  </what-built>
  <how-to-verify>
    1. Start the server: `cd banking_api_server && node server.js`
    2. Start the UI: `cd banking_api_ui && npm start`
    3. Sign in as a **customer** user (not admin) — go to `/dashboard`
    4. Check the Token Chain rail on the right — it should show either:
       - A "User session token" event card (green/active), OR
       - The "Token chain loading…" empty state (not generic placeholder chains)
       It should NOT show the grey placeholder cards with "(placeholder)" labels.
    5. Make an AI Agent request (e.g. "list my accounts") — the chain should update with RFC 8693 exchange events.
    6. Restart the server (`Ctrl+C`, then `node server.js` again).
    7. Refresh the browser — the chain should still show something real (synthesis fallback), not placeholder.
    8. Check browser console — no red errors related to token chain or useCurrentUserTokenEvent.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues you see</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| human tester → browser | Manual verification step |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-05 | — | Verification only | accept | No code changes in this plan |
</threat_model>

<verification>
User approves checkpoint above.
</verification>

<success_criteria>
- Token chain shows real data (not placeholder) for customer dashboard
- Chain updates on agent tool call
- No console errors
- Server restart resilience confirmed
</success_criteria>

<output>
After completion, create `.planning/phases/136-token-chain-reliability-audit-and-hardening-make-foolproof/136-03-SUMMARY.md`
</output>
