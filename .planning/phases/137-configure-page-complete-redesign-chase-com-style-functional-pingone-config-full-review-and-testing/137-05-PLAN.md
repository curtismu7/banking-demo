---
phase: "137-configure-page-complete-redesign-chase-com-style-functional-pingone-config-full-review-and-testing"
plan: "05"
type: execute
wave: 3
depends_on: ["137-04"]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "Human confirms all 5 tabs show real content"
    - "Save + reload cycle preserves field values"
    - "Build is clean"
  artifacts:
    - path: "banking_api_ui/src/components/Configuration/UnifiedConfigurationPage.tsx"
      provides: "Verified complete implementation"
      contains: "generate-keypair"
  key_links:
    - from: "browser"
      to: "/configure"
      via: "React Router"
      pattern: "/configure"
---

<objective>
Human verification checkpoint: confirm the complete `/configure` page redesign works end-to-end.

Purpose: Visual + functional sign-off before marking Phase 137 complete.
Output: Approved or issues listed for a follow-up fix.
</objective>

<execution_context>
@.planning/ROADMAP.md
</execution_context>

<context>
@banking_api_ui/src/components/Configuration/UnifiedConfigurationPage.tsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete /configure page redesign:
    - Plan 01: `pingone-config` tab (4 sections) — region/envId/clientId/secret forms + Test Connection
    - Plan 02: `quick-start` tab (3 sections) — pingone-basics, demo-data-setup, industry tiles
    - Plan 03: `demo-management` (4 sections) + `agent-configuration` (4 sections) — dropdowns, toggles, textarea
    - Plan 04: `advanced` tab (4 sections) — vercel, worker-app, debug toggles, Generate Keypair
    - CSS: cfg-section, cfg-field-help, cfg-secret-wrap, cfg-industry-tiles, cfg-toggle, cfg-keypair-row
  </what-built>
  <how-to-verify>
    1. Open the app and navigate to /configure (must be logged in as admin)
    2. Tab: **Quick Start** — verify:
       - PingOne Basics shows Region dropdown + Environment ID field (no placeholder text)
       - Demo Data Setup shows Scenario dropdown
       - Industry Branding shows 5 tiles (Banking, Healthcare, Retail, Insurance, Government)
    3. Tab: **PingOne Config** — verify:
       - PingOne Connection shows Region, Environment ID, Admin Client ID, Test Connection button
       - OAuth Flows shows 6 fields (Admin & User client ID/secret/redirect)
       - MFA Settings shows MFA Policy ID, Step-Up Threshold, CIBA toggle
       - Token Exchange shows MCP Server URL, MCP Resource URI, Worker Client ID
    4. Tab: **Demo Management** — verify all 4 sections have real dropdowns (no placeholder)
    5. Tab: **Agent Configuration** — verify all 4 sections have real controls (no placeholder)
    6. Tab: **Advanced** — verify all 4 sections have real controls; api-keys has "Generate Keypair" button
    7. **No placeholder text**: Search the page for "Configuration section content for" — should not appear anywhere
    8. **Save test**: Fill in any Environment ID value → click Save → reload page → value persists
    9. **Test Connection**: Fill in valid environment details → click "Test Connection" → see success or meaningful error (not a crash)
    10. `cd banking_api_ui && npm run build` exits 0
  </how-to-verify>
  <resume-signal>
    Type "approved" if all checks pass, or describe any specific issues (which tab, which section, what you see).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human | Manual verification only — no new code in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-11 | N/A | Verification checkpoint | accept | No code changes in this plan |
</threat_model>

<verification>
Human approval via resume-signal.
</verification>

<success_criteria>
- Human types "approved"
- All 5 tabs confirmed working with real form content
- Build clean
</success_criteria>

<output>
After approval, create `.planning/phases/137-configure-page-complete-redesign-chase-com-style-functional-pingone-config-full-review-and-testing/137-05-SUMMARY.md`

Then update REGRESSION_PLAN.md §4 with entry:
```
| Phase 137 | UnifiedConfigurationPage.tsx | All 19 sections now have real form content; save/load wired; Test Connection + Generate Keypair functional |
```
</output>
