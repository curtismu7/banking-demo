---
phase: 138-audit-and-fix-all-placeholder-content-across-the-app-and-server-replace-with-real-functionality
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - banking_api_ui/src/components/UserDashboard.js
  - banking_api_ui/src/components/UserDashboard.css
autonomous: true
requirements:
  - D-02
  - D-04

must_haves:
  truths:
    - "UserDashboard has no 'Demo placeholder' pills"
    - "Insights pill links to Security Center"
    - "Payments hub pill links to /transactions"
    - "Goals pill is removed entirely"
    - "Hero, account cards, quick-action row use Chase colour palette"
  artifacts:
    - path: "banking_api_ui/src/components/UserDashboard.js"
      provides: "No demo placeholder elements, Chase-styled hero + cards"
    - path: "banking_api_ui/src/components/UserDashboard.css"
      provides: "Chase-consistent styles for hero, account cards, action row"
  key_links:
    - from: "UserDashboard ud-super-pills"
      to: "SecurityCenter page and /transactions route"
      via: "React Link or onClick with navigate"
      pattern: "SecurityCenter|/transactions"
---

<objective>
Fix UserDashboard placeholder pills (D-02) and apply Chase visual consistency to the hero section, account cards, and quick-action row (D-04 for UserDashboard).

Purpose: Eliminate "Demo placeholder" labels and bring the primary user-facing page to Chase visual standard.
Output: UserDashboard.js with real wired pills and Chase-styled sections; UserDashboard.css updated.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/138-audit-and-fix-all-placeholder-content-across-the-app-and-server-replace-with-real-functionality/138-CONTEXT.md

<interfaces>
From banking_api_ui/src/index.css (existing Chase CSS tokens — use these, do not redefine):
```css
--chase-navy: #004687;
--chase-navy-dark: #003DA5;
--chase-navy-light: #005FA3;
--chase-blue: #0066CC;
--chase-white: #FFFFFF;
--chase-light-gray: #F5F5F5;
--chase-medium-gray: #E0E0E0;
--chase-dark-gray: #333333;
--chase-success: #4CAF50;
--chase-warning: #FF9800;
--chase-error: #F44336;
```

From UserDashboard.js (placeholder location ~line 1301):
```jsx
<div className="ud-super-pills" aria-label="More capabilities (demo)">
  <span className="ud-super-pill" title="Demo placeholder">Insights</span>
  <span className="ud-super-pill" title="Demo placeholder">Goals</span>
  <span className="ud-super-pill" title="Demo placeholder">Payments hub</span>
</div>
```

Routing: UserDashboard uses `useNavigate` from react-router-dom. Route for SecurityCenter is `/security` (or check App.js for exact path). Route for transactions is `/transactions`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix "Demo placeholder" pills in UserDashboard</name>
  <files>banking_api_ui/src/components/UserDashboard.js</files>
  <action>
Per D-02: Find the `ud-super-pills` block around line 1301.

1. Check App.js for the exact Security Center route (`grep "SecurityCenter\|/security" banking_api_ui/src/App.js | head -5`)

2. Replace the three pills:
   - **Insights** → `<Link to="/security" className="ud-super-pill" aria-label="Security & Insights">Insights</Link>` (use React Router Link; route to security center which shows token/audit info)
   - **Goals** → remove entirely (delete the `<span>` line)
   - **Payments hub** → `<Link to="/transactions" className="ud-super-pill" aria-label="Payments & Transfers">Payments hub</Link>`

3. If `Link` not already imported in UserDashboard.js, add it to the `react-router-dom` import.

4. Remove `title="Demo placeholder"` from all remaining pills. Remove `aria-label="More capabilities (demo)"` from the container div — replace with `aria-label="Quick links"`.

No other changes to this task — save Chase visual redesign for Task 2.
  </action>
  <verify>
    <automated>grep -n "Demo placeholder\|demo)" banking_api_ui/src/components/UserDashboard.js | grep -v "//\|Goals\|Insights\|Payments"</automated>
  </verify>
  <done>
    - No `title="Demo placeholder"` in UserDashboard.js
    - Insights and Payments hub are `<Link>` elements with real routes
    - Goals span removed
    - Build still passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Chase visual pass — UserDashboard hero, account cards, action row</name>
  <files>banking_api_ui/src/components/UserDashboard.js, banking_api_ui/src/components/UserDashboard.css</files>
  <action>
Per D-04: Apply Chase visual consistency to UserDashboard. CSS/className/structure only — no new state or API calls.

**What to update in UserDashboard.js:**

1. **Hero/greeting section** — Find the top-of-dashboard greeting/hero area. Ensure it has className `ud-hero` or similar. If the hero background is inline `style={}`, move to CSS class.

2. **Account balance cards** — Find account card elements (`ud-account-card` or similar). Ensure each card has:
   - `className` that references a CSS class styled with navy background and white text for balance
   - Account type label styled as subtitle

3. **Quick-action buttons** (Send, Pay, Transfer, etc.) — Ensure buttons use `btn btn-primary` or `ud-action-btn` className consistently (not inline styles or mixed styles).

4. **Remove any inline `backgroundColor`, `color`, `fontWeight` styles** that duplicate what CSS should define. Move those to CSS classes.

**What to add/update in UserDashboard.css:**

Add or update these rules (using existing CSS tokens, NOT raw hex):

```css
/* Hero section */
.ud-hero {
  background: var(--chase-navy);
  color: var(--chase-white);
  padding: 1.5rem 2rem;
  border-radius: 0;
}
.ud-hero__greeting {
  font-size: 1.25rem;
  font-weight: 600;
  color: var(--chase-white);
}

/* Account balance cards */
.ud-account-card--primary {
  background: var(--chase-navy);
  color: var(--chase-white);
  border-radius: 8px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.12);
  padding: 1.25rem 1.5rem;
}
.ud-account-card--primary .ud-account-balance {
  font-size: 1.75rem;
  font-weight: 700;
  color: var(--chase-white);
}
.ud-account-card--primary .ud-account-type {
  font-size: 0.85rem;
  color: rgba(255,255,255,0.8);
  font-weight: 400;
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

/* Quick-action row */
.ud-action-btn {
  background: var(--chase-blue);
  color: var(--chase-white);
  border: none;
  border-radius: 8px;
  padding: 0.625rem 1.25rem;
  font-weight: 600;
  font-size: 0.9rem;
  cursor: pointer;
  transition: background 0.15s;
}
.ud-action-btn:hover {
  background: var(--chase-navy-light);
}

/* Super pills (quick links) */
.ud-super-pill {
  display: inline-block;
  padding: 0.375rem 0.875rem;
  border-radius: 20px;
  background: var(--chase-light-gray);
  color: var(--chase-navy);
  font-size: 0.875rem;
  font-weight: 600;
  text-decoration: none;
  border: 1px solid var(--chase-medium-gray);
  transition: background 0.15s, border-color 0.15s;
}
.ud-super-pill:hover {
  background: var(--chase-medium-gray);
  border-color: var(--chase-navy-light);
  color: var(--chase-navy);
  text-decoration: none;
}
```

Only add rules that don't already exist in UserDashboard.css. Check existing rules first with `grep -n "ud-account-card\|ud-hero\|ud-action-btn" banking_api_ui/src/components/UserDashboard.css` before adding.

Do NOT touch: token chain panel, education overlays, agent FAB, MFA modals.
  </action>
  <verify>
    <automated>cd banking_api_ui && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - npm run build exits 0
    - No remaining inline `backgroundColor` style props in the hero/card/action sections
    - CSS file has chase-token-based rules for hero, account cards, action buttons, super pills
    - No raw hex values directly in JSX style attributes for these sections
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→router | Link elements navigate to real routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-02-01 | Information Disclosure | Security Center link | accept | Link navigates to existing route — no new data exposure |
</threat_model>

<verification>
- `grep -n "Demo placeholder" banking_api_ui/src/components/UserDashboard.js` → no matches
- `grep -n "Goals" banking_api_ui/src/components/UserDashboard.js` → no pill element
- `npm run build` exits 0, no TS/JS errors
</verification>

<success_criteria>
- Zero "Demo placeholder" occurrences in UserDashboard.js
- Insights and Payments hub are real navigable links
- Chase visual pass applied to hero, account cards, action row
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/138-audit-and-fix-all-placeholder-content-across-the-app-and-server-replace-with-real-functionality/138-02-SUMMARY.md`
</output>
