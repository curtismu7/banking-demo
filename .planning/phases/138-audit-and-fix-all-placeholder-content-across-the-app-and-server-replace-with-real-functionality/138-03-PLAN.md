---
phase: 138-audit-and-fix-all-placeholder-content-across-the-app-and-server-replace-with-real-functionality
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - banking_api_ui/src/components/ChaseTopNav.js
  - banking_api_ui/src/components/ChaseTopNav.css
  - banking_api_ui/src/components/SideNav.js
  - banking_api_ui/src/components/SideNav.css
  - banking_api_ui/src/components/TopNav.js
  - banking_api_ui/src/components/TopNav.css
  - banking_api_ui/src/components/Login.js
  - banking_api_ui/src/components/Login.css
  - banking_api_ui/src/components/Transactions.js
  - banking_api_ui/src/components/Transactions.css
  - banking_api_ui/src/components/UserTransactions.js
  - banking_api_ui/src/components/UserTransactions.css
autonomous: true
requirements:
  - D-04

must_haves:
  truths:
    - "ChaseTopNav uses --chase-navy background consistently"
    - "SideNav and TopNav use chase token palette"
    - "Login page has Chase-branded card layout with PingOne badge"
    - "Transactions and UserTransactions pages use card layout, chase-styled filter/action bar"
  artifacts:
    - path: "banking_api_ui/src/components/Login.css"
      provides: "Chase login card with navy header, form body"
    - path: "banking_api_ui/src/components/Transactions.css"
      provides: "Chase table/card layout for transaction list"
  key_links:
    - from: "ChaseTopNav.css"
      to: "index.css --chase-navy"
      via: "var(--chase-navy)"
      pattern: "chase-navy"
---

<objective>
Apply Chase visual consistency to navigation components (ChaseTopNav, SideNav, TopNav), Login page, and Transactions pages (D-04 part 2).

Purpose: Ensure all navigation chrome and key entry-point pages match chase.com visual standards using existing CSS tokens.
Output: 6 components updated with Chase palette, consistent nav styling, login card, transaction table/card layout.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/138-audit-and-fix-all-placeholder-content-across-the-app-and-server-replace-with-real-functionality/138-CONTEXT.md

<interfaces>
Existing Chase CSS tokens (from src/index.css):
```css
--chase-navy: #004687;        /* nav/headers */
--chase-navy-dark: #003DA5;
--chase-navy-light: #005FA3;
--chase-blue: #0066CC;        /* CTAs */
--chase-white: #FFFFFF;
--chase-light-gray: #F5F5F5;  /* page bg */
--chase-medium-gray: #E0E0E0;
--chase-dark-gray: #333333;
--chase-success: #4CAF50;
```

ChaseTopNav.js already uses `chase-top-nav` classes and `var(--chase-navy, #004687)` — this component is close to spec. Focus on verifying consistency and fixing any gaps (white text on nav links, active indicator, consistent spacing).

SideNav.js: 278 lines — uses sidenav classes. Update background, active item, hover state to chase tokens.
TopNav.js: 173 lines — admin/secondary nav. Update to chase navy background.
Login.js: 192 lines — card-based login form.
Transactions.js: 168 lines — transaction list page.
UserTransactions.js: 591 lines — user-facing transaction page with filters.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chase nav pass — ChaseTopNav, SideNav, TopNav CSS</name>
  <files>banking_api_ui/src/components/ChaseTopNav.js, banking_api_ui/src/components/ChaseTopNav.css, banking_api_ui/src/components/SideNav.js, banking_api_ui/src/components/SideNav.css, banking_api_ui/src/components/TopNav.js, banking_api_ui/src/components/TopNav.css</files>
  <action>
Per D-04: CSS/className/structure only — no new state or API calls.

**ChaseTopNav.css — verify and fix:**
Read current CSS. Ensure:
- `.chase-top-nav` background is `var(--chase-navy)` (not hardcoded hex)
- `.chase-nav-link` color is `var(--chase-white)`, hover uses `rgba(255,255,255,0.15)` underline or background tint
- `.chase-nav-link--active` has a bottom border or weight indicator in white
- `.chase-nav-button` uses `var(--chase-white)` color
- Overall: no raw hex colour values — all referenced through CSS variables

Replace any hardcoded `#004687` / `#005EB8` / `#0066CC` with the corresponding `var(--chase-*)` token.

**SideNav.css — update:**
Read current CSS. Apply:
```css
.side-nav, .sidenav, [class*="side-nav"] {
  background: var(--chase-navy);
  color: var(--chase-white);
}
/* Active nav item */
.side-nav__item--active, .sidenav-item--active, [class*="nav-item"][class*="active"] {
  background: rgba(255,255,255,0.12);
  border-left: 3px solid var(--chase-white);
}
/* Hover */
.side-nav__item:hover, .sidenav-item:hover {
  background: rgba(255,255,255,0.08);
}
/* Links */
.side-nav a, .sidenav a {
  color: var(--chase-white);
  text-decoration: none;
}
```
Read SideNav.js to discover exact class names before writing CSS — match actual class names used.

**TopNav.css — update:**
Same pattern as SideNav: use `--chase-navy` for background, `--chase-white` for text. TopNav is the admin secondary navigation bar.

Do NOT add duplicate class declarations — check what already exists and only add/modify what differs.
  </action>
  <verify>
    <automated>grep -rn "#004687\|#005EB8\|#0066CC\|#003DA5" banking_api_ui/src/components/ChaseTopNav.css banking_api_ui/src/components/SideNav.css banking_api_ui/src/components/TopNav.css 2>/dev/null | wc -l</automated>
  </verify>
  <done>
    - ChaseTopNav.css, SideNav.css, TopNav.css contain zero raw hex colour values
    - All colours reference var(--chase-*) tokens
    - npm run build exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Chase Login card + Transactions layout</name>
  <files>banking_api_ui/src/components/Login.js, banking_api_ui/src/components/Login.css, banking_api_ui/src/components/Transactions.js, banking_api_ui/src/components/Transactions.css, banking_api_ui/src/components/UserTransactions.js, banking_api_ui/src/components/UserTransactions.css</files>
  <action>
Per D-04: CSS/className/structure only — no new state or API calls.

**Login.js + Login.css:**

Read Login.js first. The login form should be inside a card. Apply:
```css
.login-page {
  min-height: 100vh;
  background: var(--chase-light-gray);
  display: flex;
  align-items: center;
  justify-content: center;
}
.login-card {
  background: var(--chase-white);
  border-radius: 8px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.08);
  width: 100%;
  max-width: 420px;
  overflow: hidden;
}
.login-card__header {
  background: var(--chase-navy);
  color: var(--chase-white);
  padding: 1.5rem 2rem 1.25rem;
  text-align: center;
}
.login-card__header h1, .login-card__header h2 {
  color: var(--chase-white);
  font-size: 1.25rem;
  font-weight: 600;
  margin: 0;
}
.login-card__body {
  padding: 2rem;
}
.login-card__pingone-badge {
  text-align: center;
  margin-top: 1rem;
  font-size: 0.8rem;
  color: var(--chase-dark-gray);
}
```

In Login.js: ensure the outermost wrapper div has className `login-page`, the card div has `login-card`, the header area has `login-card__header`, the form body has `login-card__body`. If the existing structure is different, add classNames to match without restructuring the component logic.

**Transactions.js + Transactions.css:**

Read Transactions.js. It's a 168-line component — simpler. Apply Chase card + table style:
```css
.transactions-page {
  background: var(--chase-light-gray);
  min-height: 100vh;
  padding: 1.5rem;
}
.transactions-card {
  background: var(--chase-white);
  border-radius: 8px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.08);
  padding: 1.5rem;
  margin-bottom: 1rem;
}
.transactions-header {
  background: var(--chase-navy);
  color: var(--chase-white);
  padding: 1rem 1.5rem;
  border-radius: 8px 8px 0 0;
  font-size: 1.1rem;
  font-weight: 600;
}
```

**UserTransactions.js + UserTransactions.css:**

Read UserTransactions.js. It's 591 lines with a filter bar and transaction list. Apply:
- Page background: `var(--chase-light-gray)`
- Card wrappers for filter bar and transaction list: `background: var(--chase-white); border-radius: 8px; box-shadow: 0 2px 8px rgba(0,0,0,0.08)`
- Action buttons (Send, Pay): `background: var(--chase-blue); color: var(--chase-white); border-radius: 8px`
- Filter bar inputs: standard form-input style with `border: 1px solid var(--chase-medium-gray); border-radius: 4px`

Read UserTransactions.css first to discover class names before adding rules. Only add/update what's needed — do not delete existing working rules.
  </action>
  <verify>
    <automated>cd banking_api_ui && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Login page has chase-navy header card, white body card
    - Transactions and UserTransactions pages use chase-light-gray background, white cards
    - npm run build exits 0, no TypeScript/JSX errors
    - No raw hex colour values in Login.css, Transactions.css, UserTransactions.css
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→Login | Login form renders user credentials in browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-03-01 | Information Disclosure | Login card | accept | CSS-only change — no auth logic touched |
</threat_model>

<verification>
- `grep -rn "^  background.*#\|^  color.*#\|^  background-color.*#" banking_api_ui/src/components/Login.css` → only variable references
- `npm run build` exits 0
- No inline style changes to auth-related JSX attributes
</verification>

<success_criteria>
- Nav components use var(--chase-*) tokens exclusively (no raw hex)
- Login page has chase-branded card layout
- Transactions pages use page-bg + white card layout
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/138-audit-and-fix-all-placeholder-content-across-the-app-and-server-replace-with-real-functionality/138-03-SUMMARY.md`
</output>
