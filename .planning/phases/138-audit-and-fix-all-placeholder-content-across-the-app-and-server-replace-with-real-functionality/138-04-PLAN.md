---
phase: 138-audit-and-fix-all-placeholder-content-across-the-app-and-server-replace-with-real-functionality
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - banking_api_ui/src/components/Profile.js
  - banking_api_ui/src/components/Profile.css
  - banking_api_ui/src/components/SecurityCenter.js
  - banking_api_ui/src/components/SecurityCenter.css
  - banking_api_ui/src/components/AdminSubPageShell.js
  - banking_api_ui/src/components/AdminSubPageShell.css
  - banking_api_ui/src/components/BankingAdminOps.js
  - banking_api_ui/src/components/BankingAdminOps.css
autonomous: true
requirements:
  - D-04

must_haves:
  truths:
    - "Profile page uses Chase card layout with section headers"
    - "Security Center uses card-based layout matching admin mock"
    - "Admin pages (AdminSubPageShell, BankingAdminOps) match admin-dashboard-2026 look"
  artifacts:
    - path: "banking_api_ui/src/components/Profile.css"
      provides: "Chase card layout for profile sections"
    - path: "banking_api_ui/src/components/SecurityCenter.css"
      provides: "Card-based layout for security info panels"
    - path: "banking_api_ui/src/components/BankingAdminOps.css"
      provides: "Admin dashboard card layout"
  key_links:
    - from: "Profile.css sections"
      to: "index.css --chase-navy/--chase-light-gray"
      via: "var(--chase-*)"
      pattern: "chase-navy|chase-light-gray"
---

<objective>
Apply Chase visual consistency to Profile, Security Center, and Admin pages (D-04 part 3 — final UI pages).

Purpose: Complete the Chase visual pass for all non-configure pages as specified in D-04.
Output: Profile, SecurityCenter, AdminSubPageShell, BankingAdminOps updated with Chase card layout and palette.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/138-audit-and-fix-all-placeholder-content-across-the-app-and-server-replace-with-real-functionality/138-CONTEXT.md

<interfaces>
Existing Chase CSS tokens (from src/index.css):
```css
--chase-navy: #004687;
--chase-blue: #0066CC;
--chase-white: #FFFFFF;
--chase-light-gray: #F5F5F5;
--chase-medium-gray: #E0E0E0;
--chase-dark-gray: #333333;
```

Reference mocks:
- `banking_api_ui/public/design/admin-dashboard-2026.html` — card grid, navy section headers
- `banking_api_ui/public/design/customer-dashboard-2026.html` — account overview, clean card layout

Profile.js: 398 lines — user settings/profile form
SecurityCenter.js: 767 lines — security alerts, MFA status, session info
AdminSubPageShell.js: 33 lines — simple admin shell wrapper
BankingAdminOps.js: 361 lines — admin operations panel
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chase pass — Profile + SecurityCenter</name>
  <files>banking_api_ui/src/components/Profile.js, banking_api_ui/src/components/Profile.css, banking_api_ui/src/components/SecurityCenter.js, banking_api_ui/src/components/SecurityCenter.css</files>
  <action>
Per D-04: CSS/className/structure only — no new state or API calls.

**Profile.js + Profile.css:**

Read Profile.js to understand structure. Apply card-with-header pattern to each section:
```css
.profile-page {
  background: var(--chase-light-gray);
  min-height: 100vh;
  padding: 1.5rem;
}
.profile-section {
  background: var(--chase-white);
  border-radius: 8px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.08);
  margin-bottom: 1.5rem;
  overflow: hidden;
}
.profile-section__header {
  background: var(--chase-navy);
  color: var(--chase-white);
  padding: 0.875rem 1.5rem;
  font-size: 0.95rem;
  font-weight: 600;
}
.profile-section__body {
  padding: 1.5rem;
}
/* Form rows inside profile */
.profile-field-label {
  font-weight: 600;
  color: var(--chase-dark-gray);
  font-size: 0.875rem;
}
```

In Profile.js: if sections exist without the above class names, add `className="profile-section"` to each section wrapper and `className="profile-section__header"` to each header. Match actual classNames in the file — do not restructure component logic.

**SecurityCenter.js + SecurityCenter.css:**

Read SecurityCenter.js. The security center has sections for MFA status, active sessions, token audit. Apply:
```css
.security-center {
  background: var(--chase-light-gray);
  min-height: 100vh;
  padding: 1.5rem;
}
.security-card {
  background: var(--chase-white);
  border-radius: 8px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.08);
  margin-bottom: 1.5rem;
  overflow: hidden;
}
.security-card__header {
  background: var(--chase-navy);
  color: var(--chase-white);
  padding: 0.875rem 1.5rem;
  display: flex;
  align-items: center;
  justify-content: space-between;
}
.security-card__header h2, .security-card__header h3 {
  color: var(--chase-white);
  margin: 0;
  font-size: 0.95rem;
  font-weight: 600;
}
.security-card__body {
  padding: 1.5rem;
}
/* Status indicators */
.security-status--ok {
  color: var(--chase-success);
  font-weight: 600;
}
.security-status--warn {
  color: var(--chase-warning);
  font-weight: 600;
}
.security-status--error {
  color: var(--chase-error);
  font-weight: 600;
}
```

Read SecurityCenter.css first before adding rules. Only add what's missing or inconsistent.
  </action>
  <verify>
    <automated>cd banking_api_ui && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Profile page has card-per-section layout with chase-navy section headers
    - SecurityCenter has card-based layout with chase navy headers
    - npm run build exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Chase pass — AdminSubPageShell + BankingAdminOps</name>
  <files>banking_api_ui/src/components/AdminSubPageShell.js, banking_api_ui/src/components/AdminSubPageShell.css, banking_api_ui/src/components/BankingAdminOps.js, banking_api_ui/src/components/BankingAdminOps.css</files>
  <action>
Per D-04: CSS/className/structure only. Reference: `public/design/admin-dashboard-2026.html` for layout intent.

**AdminSubPageShell.js + AdminSubPageShell.css:**

Read AdminSubPageShell.js (33 lines — a simple wrapper). Apply:
```css
.admin-sub-page-shell {
  background: var(--chase-light-gray);
  min-height: 100vh;
  padding: 1.5rem;
}
.admin-sub-page-shell__content {
  background: var(--chase-white);
  border-radius: 8px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.08);
  padding: 1.5rem;
}
```

Check AdminSubPageShell.js for existing classNames and add/match to CSS. If `.css` file doesn't exist, create `AdminSubPageShell.css` with the above rules and add `import './AdminSubPageShell.css';` to the JS file.

**BankingAdminOps.js + BankingAdminOps.css:**

Read BankingAdminOps.js (361 lines). This is the admin operations panel (user management, scenario reset, config actions). Apply:
```css
.banking-admin-ops {
  background: var(--chase-light-gray);
  min-height: 100vh;
  padding: 1.5rem;
}
.admin-ops-section {
  background: var(--chase-white);
  border-radius: 8px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.08);
  margin-bottom: 1.5rem;
  overflow: hidden;
}
.admin-ops-section__header {
  background: var(--chase-navy);
  color: var(--chase-white);
  padding: 0.875rem 1.5rem;
  font-size: 0.95rem;
  font-weight: 600;
}
.admin-ops-section__body {
  padding: 1.5rem;
}
.admin-ops-btn {
  background: var(--chase-blue);
  color: var(--chase-white);
  border: none;
  border-radius: 8px;
  padding: 0.5rem 1.125rem;
  font-weight: 600;
  font-size: 0.875rem;
  cursor: pointer;
  transition: background 0.15s;
  margin-right: 0.5rem;
  margin-bottom: 0.5rem;
}
.admin-ops-btn:hover {
  background: var(--chase-navy-light);
}
.admin-ops-btn--danger {
  background: var(--chase-error);
}
.admin-ops-btn--danger:hover {
  background: #c62828;
}
```

In BankingAdminOps.js: check section wrapper divs — add `className="admin-ops-section"`, section headers add `className="admin-ops-section__header"`, section bodies `className="admin-ops-section__body"`. Add `import './BankingAdminOps.css'` if not already present.

Read BankingAdminOps.css first to avoid duplicating existing rules.

After all CSS changes, run the build to verify.
  </action>
  <verify>
    <automated>cd banking_api_ui && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - AdminSubPageShell has chase-light-gray page background with white content card
    - BankingAdminOps has card-per-section layout with chase-navy headers, chase-blue action buttons
    - npm run build exits 0, no errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→Admin pages | Admin pages are protected by auth middleware — no change to auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-04-01 | Elevation of Privilege | Admin pages | accept | CSS-only changes — auth guards not touched |
</threat_model>

<verification>
- `npm run build` exits 0
- `grep -rn "^.*background.*#\|^.*color.*#" banking_api_ui/src/components/Profile.css banking_api_ui/src/components/SecurityCenter.css banking_api_ui/src/components/BankingAdminOps.css 2>/dev/null | grep -v "rgba\|#fff\|#000\|#F\|gradient" | wc -l` → close to 0 (only vars)
</verification>

<success_criteria>
- Profile, SecurityCenter, AdminSubPageShell, BankingAdminOps all use Chase card layout
- All new CSS uses var(--chase-*) tokens
- Build passes
- No auth/session behaviour changed
</success_criteria>

<output>
After completion, create `.planning/phases/138-audit-and-fix-all-placeholder-content-across-the-app-and-server-replace-with-real-functionality/138-04-SUMMARY.md`
</output>
