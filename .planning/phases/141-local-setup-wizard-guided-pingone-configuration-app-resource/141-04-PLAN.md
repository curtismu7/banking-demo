---
phase: 141-local-setup-wizard-guided-pingone-configuration-app-resource
plan: 04
type: execute
wave: 3
depends_on:
  - "141-03"
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "User can navigate to /setup/wizard and see the accordion wizard"
    - "All 7 accordion steps are visible; Step 1 is expanded by default"
    - "Credential fields have eye-toggle for workerClientSecret"
    - "SideNav shows Setup Wizard link under Configuration group"
    - "Backend provision pipeline step list includes mcp-exchanger-app, schema-attr, spel-claim"
  artifacts:
    - path: "banking_api_ui/src/components/SetupWizard.js"
      provides: "Renders without console errors at /setup/wizard"
  key_links:
    - from: "SideNav Setup Wizard link"
      to: "/setup/wizard"
      via: "React Router Link"
      pattern: "setup/wizard"
---

<objective>
Human verification that the accordion wizard renders correctly and the backend provision pipeline has been extended with the new steps. Visual check of layout, step circles, credentials form, and nav link.

Purpose: Confirm the full feature looks correct before phase 141 is marked complete.

Output: Phase marked complete; REGRESSION_PLAN.md §4 updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 141 complete — local dev PingOne setup wizard at /setup/wizard:
    - Accordion wizard (7 steps, all visible) at /setup/wizard
    - Step 1: Credentials form with eye-toggle on workerClientSecret
    - Steps 2-6: Provisioning accordion panels (shown pre-run with inactive state)
    - Step 7: Completion panel (shows only after run completes)
    - SideNav "Setup Wizard" link under Configuration group
    - Backend: pingoneProvisionService extended with mcp-exchanger, schema-attr, spel-claim steps
  </what-built>
  <how-to-verify>
    1. Open https://api.pingdemo.com:4000/setup/wizard
    2. Confirm page title "BX Finance — PingOne Setup" appears
    3. Confirm 7 accordion steps visible with correct titles:
       - 1. PingOne Credentials ← expanded/active (blue circle)
       - 2. Create Application
       - 3. Create Resource Server
       - 4. Create Scopes
       - 5. SPEL Attribute Mapping
       - 6. Generate .env
       - 7. Complete
    4. Expand Step 1 — confirm fields: Environment ID, Worker Client ID, Worker Client Secret (with eye toggle), Region dropdown, App URL, ACR Value
    5. Click the eye icon on Worker Client Secret — confirm text toggles between ••••••• and visible
    6. Open SideNav — confirm "Setup Wizard" appears under Configuration group
    7. Check browser console for React errors — should be none
    8. (Optional — only if you have a test PingOne environment) Enter credentials and click "Verify Connection" — confirm validation fires and Step 2 panel becomes active

    Backend step check (run in terminal):
    ```
    grep -c "mcp-exchanger-app\|schema-attr\|spel-claim" banking_api_server/services/pingoneProvisionService.js
    ```
    Should return 3 or more.
  </how-to-verify>
  <resume-signal>Type "approved" if the wizard renders correctly, or describe any issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manual verification | No new attack surface introduced in verification step |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-09 | N/A | Checkpoint only | accept | No code changes in this plan |
</threat_model>

<verification>
Human visual check per task instructions above.
</verification>

<success_criteria>
- Wizard renders at /setup/wizard with correct accordion structure
- Step 1 credentials form with eye toggle works
- No React console errors
- SideNav link present
- Backend has mcp-exchanger, schema-attr, spel-claim step keys
</success_criteria>

<output>
After approval, create `.planning/phases/141-local-setup-wizard-guided-pingone-configuration-app-resource/141-04-SUMMARY.md`

Then update:
1. REGRESSION_PLAN.md §4 — add entry: "Phase 141: SetupWizard at /setup/wizard; pingoneProvisionService extended with mcp_exchanger + SPEL claim steps"
2. ROADMAP.md phase 141 — mark all plans complete
</output>
