---
phase: 143
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [banking_api_ui/src/components/TokenChainDisplay.js, banking_api_ui/src/components/BankingAgent.js, banking_api_ui/src/services/tokenChainService.js]
autonomous: true
requirements: [TOKEN-01, TOKEN-02]
must_haves:
  truths:
    - "Token chain panel shows exchange path label (1-exchange vs 2-exchange)"
    - "Agent chat displays real-time progress steps: Getting tokens → Exchanging → Calling agent → Complete"
    - "Progress steps appear as separate messages in chat history"
    - "User sees which token exchange path was used after each agent action"
    - "Agent action completes with toast notification + transaction appears in dashboard"
  artifacts:
    - path: "banking_api_ui/src/components/TokenChainDisplay.js"
      provides: "Token chain panel with exchange path label (1-exchange / 2-exchange)"
      min_lines: 50
    - path: "banking_api_ui/src/services/tokenChainService.js"
      provides: "Token chain history + exchange path detection logic"
      exports: ["recordTokenChainEvent", "detectExchangePath"]
    - path: "banking_api_ui/src/components/BankingAgent.js"
      provides: "Real-time progress feedback streaming in agent chat"
      min_lines: 50
  key_links:
    - from: "agentMcpTokenService.resolveMcpToken()"
      to: "TokenChainDisplay exchange path label"
      via: "token metadata returned includes exchange_path"
      pattern: "exchange_path|1-exchange|2-exchange"
    - from: "BankingAgent_progress_steps"
      to: "agent_chat_messages"
      via: "stream progress as intermediate messages"
      pattern: "Getting tokens|Exchanging|Calling agent|Complete"
    - from: "TokenChainDisplay"
      to: "tokenChainService"
      via: "recordTokenChainEvent(event)"
      pattern: "recordTokenChainEvent"
---

<objective>
Display token exchange paths in the token chain panel (1-exchange vs 2-exchange label). Stream real-time progress feedback in agent chat as agent performs token exchange and tool calls.

Purpose: Educate user on RFC 8693 patterns in real time. User sees exactly which exchange path is being used and the step-by-step agent flow.
Output: TokenChainDisplay with exchange path label + agent chat progress streaming
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/phases/143-ux-agent-banking-actions-via-mcp-server-new-user-token-scoped-to-agent-with-aud-claim-then-1-token-or-2-token-exchange-for-mcp-token-ff-showing-pattern-evolution-from-standard-authz-to-agentic-authz/143-CONTEXT.md
@banking_api_ui/src/components/TokenChainDisplay.js
@banking_api_ui/src/components/BankingAgent.js
@banking_api_server/services/agentMcpTokenService.js
</execution_context>

<context>
**Key Decisions (from CONTEXT.md):**
- D-03: 1-token default with FF for 2-token exchange
- D-04: Show exchange path label in token chain panel (1-exchange vs 2-exchange)
- D-08: Real-time feedback with progress steps in agent chat

**Existing Components:**
- TokenChainDisplay.js: Shows decoded JWT tokens, act/may_act claims
- BankingAgent.js: Agent chat interface
- agentMcpTokenService.js: Returns MCP token metadata (includes which exchange path was used)
- tokenChainService.js (existing): localStorage-backed history of token events

**Token Exchange Return Format (from agentMcpTokenService):**
- Should include { mcp_token, exchange_path: "1-exchange" | "2-exchange", exchanged_at, ... }
- Phase 144 will add more details; this phase just displays the path label
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add exchange path label to TokenChainDisplay panel</name>
  <files>banking_api_ui/src/components/TokenChainDisplay.js</files>
  <action>
Enhance TokenChainDisplay to show exchange path after each agent operation.

Add new row/section to token chain viz:
- "Exchange Path" label with badge: **"1-exchange"** or **"2-exchange"**
- Styling: small badge, positioned near the MCP token section
- Appears after agent tool call completes successfully

Implementation:
1. Update tokenChainService.recordTokenChainEvent() to include exchange_path field
2. In TokenChainDisplay, after mcp_token is displayed, add conditional:
   ```javascript
   {lastEvent?.exchange_path && (
     <div className="token-chain-row">
       <label>Exchange Path</label>
       <badge className={lastEvent.exchange_path === "1-exchange" ? "badge-blue" : "badge-purple"}>
         {lastEvent.exchange_path}
       </badge>
     </div>
   )}
   ```
3. Badge colors: 1-exchange = blue, 2-exchange = purple (visual diff for demo)

Data flow:
- After agentMcpTokenService.resolveMcpToken() returns, response includes { exchange_path: "..." }
- BankingAgent calls tokenChainService.recordTokenChainEvent({ exchange_path, mcp_token, ... })
- TokenChainDisplay subscribes to tokenChainService updates and re-renders with exchange path badge

Rationale: User (esp. at conference demo) immediately understands which RFC 8693 path was used in real time.
  </action>
  <verify>
    <automated>
      npm run build -- --production 2>&1 | grep -q "success"
      grep -n "exchange_path\|1-exchange\|2-exchange" banking_api_ui/src/components/TokenChainDisplay.js | head -3
      # Manual: Trigger agent action, token chain panel displays exchange path label (blue badge "1-exchange" or purple badge "2-exchange")
    </automated>
  </verify>
  <done>
    - Exchange path badge appears in TokenChainDisplay after agent tool call
    - Badge displays correct exchange path (1-exchange or 2-exchange based on agentMcpTokenService response)
    - Badge styling distinct: 1-exchange = blue, 2-exchange = purple
    - tokenChainService.recordTokenChainEvent() includes exchange_path field
    - npm run build exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement real-time progress feedback streaming in BankingAgent chat</name>
  <files>banking_api_ui/src/components/BankingAgent.js</files>
  <action>
Add progress step messages to agent chat as agent performs token exchange and tool calls.

When agent initiates a banking action (Transfer/Deposit/Withdraw):
1. Show progress message: "🔄 Getting tokens..." (message rendered immediately, no awaiting)
2. After token exchange completes: "🔄 Exchanging..." (update or new message)
3. After tool call sent to MCP: "🔄 Calling agent..." (update or new message)
4. After banking API responds: "✓ Transfer complete" (final message, success emoji)
5. If error at any step: "✗ Transfer failed: [error user message]" (error emoji + user-friendly message)

Implementation approach:
- Create ProgressFeedback.js component: renders a sequence of steps with icons and text
- Steps: { id, text, status: 'pending' | 'in-progress' | 'complete' | 'error', icon: emoji }
- In BankingAgent, wrap agentMcpTokenService call:
  ```javascript
  const steps = [
    { id: 1, text: 'Getting tokens...', status: 'in-progress', icon: '🔄' },
    { id: 2, text: 'Exchanging...', status: 'pending', icon: '🔄' },
    { id: 3, text: 'Calling agent...', status: 'pending', icon: '🔄' },
    { id: 4, text: 'Complete', status: 'pending', icon: '✓' }
  ];
  
  // On each step completion, update steps array and re-render
  agentMcpTokenService.on('step', (step) => {
    updateSteps(step.name, 'complete');
  });
  ```

- Add emit hooks to agentMcpTokenService (or wrap in BankingAgent itself)
- Each progress message appears as a separate chat entry with timestamp
- Messages persist in chat history (user can scroll back and see the sequence)

Rationale: Per D-08, user sees agent working in real time. Builds confidence + educational (shows RFC 8693 steps).
  </action>
  <verify>
    <automated>
      npm run build -- --production 2>&1 | grep -q "success"
      grep -n "Getting tokens\|Exchanging\|Calling agent\|Complete" banking_api_ui/src/components/BankingAgent.js | wc -l | grep -q "^[4-9]"
      # Manual: Request agent transfer, verify chat shows step-by-step messages: 1. Getting tokens 2. Exchanging 3. Calling agent 4. Complete
      # Test error: break token exchange, verify error message appears at step 2
    </automated>
  </verify>
  <done>
    - ProgressFeedback.js component exists and renders steps with icons + status
    - BankingAgent.js emits progress messages at each stage of agent action
    - Chat history shows all progress steps (getting tokens → exchanging → calling agent → complete)
    - Error at any step updates the final message with error icon + user-friendly error
    - Step messages appear with timestamps
    - npm run build exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TokenChainService → localStorage | Token metadata stored locally; exchange path is not sensitive |
| ProgressFeedback messages → User | Messages may leak that token exchange is happening; acceptable for demo (educational) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-07 | Information Disclosure | Progress messages reveal agent token flow | Accept | Rationale: This demo is educationally opaque on purpose. Progress messages show RFC 8693 implementation to learners. No credentials or PII leaked. |
| T-143-08 | Tampering | Exchange path label modified client-side | Accept | Exchange path comes from MCP token (server-validated). Client-side display is UI only; cannot affect backend behavior. |

</threat_model>

<verification>
1. BankingAgent.js builds without errors: `npm run build -- --production` → exit 0
2. Token chain panel displays exchange path badge after agent action (1-exchange or 2-exchange)
3. Agent chat shows real-time progress messages in sequence: Getting tokens → Exchanging → Calling agent → Complete
4. Error at any step updates final message with error status
5. Step messages persist in chat history
</verification>

<success_criteria>
1. ✓ Exchange path badge appears in TokenChainDisplay with correct label (1-exchange or 2-exchange)
2. ✓ Agent chat displays progress steps in real time as agent performs action
3. ✓ All 4 progress steps visible: Getting tokens, Exchanging, Calling agent, Complete
4. ✓ Error handling shows error message at appropriate step
5. ✓ Progress messages persist in chat history; user can scroll back to see sequence
6. ✓ npm run build exits 0; no console errors
</success_criteria>

<output>
After completion, create `.planning/phases/143-*/143-02-SUMMARY.md`
</output>
