---
phase: 143
plan: 04
type: execute
wave: 3
depends_on: [143-01, 143-02, 143-03]
files_modified: []
autonomous: false
requirements: [TOKEN-01, TOKEN-02, AGENT-ACTIVITY-01]
must_haves:
  truths:
    - "Agent can initiate a transfer, request approval (if > threshold), and user can approve"
    - "Token chain shows exchange path label (1-exchange or 2-exchange)"
    - "Agent chat shows real-time progress steps during action"
    - "Dashboard Agent Activity tab shows transaction with 🤖 Agent badge"
    - "Admin can toggle FF to switch exchange paths"
    - "Next agent action uses selected exchange path"
    - "Error handling displays user-friendly + technical message + recovery link"
  artifacts:
    - path: "Full agent banking action flow"
      provides: "End-to-end verification of Phases 143-01 through 143-03"
      min_lines: 0
  key_links:
    - from: "BankingAgent chat input"
      to: "Dashboard transaction with badge"
      via: "full agent flow: input → approval → token exchange → tool call → dashboard update"
      pattern: "complete end-to-end flow"
---

<objective>
Comprehensive end-to-end verification that all agent banking action components work together: chat interface → approval → token exchange → real-time feedback → dashboard display with badge.

Purpose: QA checkpoint before Phase 143 is marked complete. Verify all 8 locked decisions (D-01 through D-08) are implemented correctly.
Output: Manual verification results; go/no-go decision for Phase 143 completion
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/phases/143-ux-agent-banking-actions-via-mcp-server-new-user-token-scoped-to-agent-with-aud-claim-then-1-token-or-2-token-exchange-for-mcp-token-ff-showing-pattern-evolution-from-standard-authz-to-agentic-authz/143-CONTEXT.md
</execution_context>

<context>
**All 8 Locked Decisions (Phase 143 CONTEXT.md):**
1. D-01: Agent actions via chat interface (no dashboard buttons)
2. D-02: Approval threshold for large transactions (small auto, large requires approval)
3. D-03: 1-token default with FF toggle for 2-token
4. D-04: Show exchange path label in token chain
5. D-05: All MCP tools available
6. D-06: Separate "Agent Activity" tab in transaction history
7. D-07: Error handling with user-friendly + technical + troubleshooting
8. D-08: Real-time progress feedback in agent chat

**Verification must confirm all 8 decisions implemented and working.**
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - Approval threshold modal in BankingAgent (Plan 01, Task 1)
    - Error handler service with dual-layer messages (Plan 01, Task 2)
    - Exchange path badge in TokenChainDisplay (Plan 02, Task 1)
    - Real-time progress feedback in agent chat (Plan 02, Task 2)
    - Agent Activity tab + badge in transaction history (Plan 03, Task 1)
    - Feature flag toggle for token exchange path (Plan 03, Task 2)
  </what-built>
  <how-to-verify>
    **TEST 1: Agent Transfer (< threshold) — Auto-run without approval**
    1. Open BankingAgent chat interface
    2. Request: "Transfer $100 to savings account"
    3. Expected: No approval modal appears; agent proceeds directly
    4. Verify: Token chain shows exchange path badge (1-exchange or 2-exchange)
    5. Verify: Agent chat shows progress steps: Getting tokens → Exchanging → Calling agent → Complete
    6. Verify: Dashboard shows new transaction in Agent Activity tab with 🤖 Agent badge

    **TEST 2: Agent Transfer (>= threshold) — Requires approval**
    1. Request: "Transfer $1000 to savings account"
    2. Expected: Approval modal appears with amount ($1000), destination (savings), authorized by [Agent Name], Approve/Reject buttons
    3. Click "Approve"
    4. Verify: Agent continues; progress steps show in chat
    5. Verify: Transaction appears in Agent Activity tab with 🤖 Agent badge
    
    **TEST 3: Test Rejection**
    1. Request: "Transfer $2000 to checking"
    2. Approval modal appears
    3. Click "Reject"
    4. Expected: Error message in chat "Agent action cancelled by user"
    5. Verify: No transaction appears in dashboard

    **TEST 4: Error Handling**
    1. Intentionally trigger an error (e.g., insufficient funds, invalid destination)
    2. Expected: Error modal appears with:
       - User-friendly message: "Agent couldn't complete this transfer. Check your token configuration or try manually."
       - Technical detail section (expandable): "(Token exchange error: ... OR MCP tool error: ...)"
       - Troubleshooting link: "How to fix agent action errors"
    3. Verify: User can understand next steps from the message

    **TEST 5: Exchange Path Toggle**
    1. Admin config page → "Token Exchange Path" toggle
    2. Verify current path is displayed (1-exchange or 2-exchange)
    3. Toggle to opposite path
    4. Request new agent transfer (< threshold)
    5. Verify: Token chain badge shows new exchange path (toggle was successful)
    6. Toggle back to original path and verify badge updates again

    **TEST 6: Agent Activity Tab**
    1. Dashboard shows two tabs: "All Transactions" and "Agent Activity"
    2. "Agent Activity" tab displays only transactions with 🤖 Agent badge
    3. "All Transactions" tab shows both manual and agent transactions
    4. Verify badge styling is consistent and visible

    **TEST 7: Dashboard Integration**
    1. Perform a successful agent transfer
    2. Dashboard automatically updates (no page refresh needed)
    3. New transaction appears in transaction history with 🤖 Agent badge
    4. Agent Activity tab shows the transaction
    5. Agent name (if available) displayed in transaction detail

    **TEST 8: Console/Build Validation**
    1. npm run build → exit code 0 (no errors)
    2. Open browser console: no console.error or unhandled exceptions during agent flow
    3. No broken links in error recovery messages / troubleshooting docs
  </how-to-verify>
  <resume-signal>
    Write "approved" if all 8 tests pass.
    Write "issues found:" and list specific failures (e.g., "TEST 3 failed: error message unclear", "TEST 5 failed: toggle doesn't persist").
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User approval intent | User clicks Approve/Reject; no external validation needed for this checkpoint |
| Frontend visual display | All components render correctly; backend security (token validation) tested in Phase 144 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-12 | Testing/Verification | Manual QA of full flow | Accept | This checkpoint does not validate security; it validates UX + integration. Security validation (token exchange correctness, agent permissions) belongs to Phase 144. |

</threat_model>

<verification>
Manual verification via 8 test scenarios.
</verification>

<success_criteria>
All 8 decision implemented and verified:
- ✓ D-01: Agent actions via chat (TEST 1-2)
- ✓ D-02: Approval threshold (TEST 2-3)
- ✓ D-03: 1-token default with FF toggle (TEST 5)
- ✓ D-04: Exchange path label (TEST 1, TEST 5)
- ✓ D-05: All MCP tools available (TEST 1-2 demo Transfer; others deferred to full test)
- ✓ D-06: Agent Activity tab (TEST 6)
- ✓ D-07: Error handling (TEST 4)
- ✓ D-08: Real-time feedback (TEST 1-2)
- ✓ npm run build exits 0 (TEST 8)

</success_criteria>

<output>
After verification completion (approved or issues resolved), create `.planning/phases/143-*/143-04-SUMMARY.md` with results.
</output>
