---
phase: 20-postman-collections-fix-1-exchange-utilities-and-build-industry-standard-2-exchange-collection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/BX-Finance-1-Exchange-Step-by-Step.postman_collection.json
  - docs/BX Finance — 1-Exchange Delegated Chain — pi.flow.postman_collection.json
  - docs/BX Finance — 2-Exchange Delegated Chain — pi.flow.postman_collection.json
  - docs/BX-Finance-Shared.postman_environment.json
autonomous: true
requirements: []
must_haves:
  truths:
    - BX-Finance-1-Exchange-Step-by-Step.postman_collection.json exists in docs/ with info.name = "BX Finance — 1-Exchange Step-by-Step (RFC 8693)"
    - BX-Finance-MayAct-Chain.postman_collection.json no longer exists in docs/
    - BX-Finance-Shared.postman_environment.json exists with UPPERCASE variable keys matching .env.example canonical names
    - Both pi.flow collections use UPPERCASE variable names (PINGONE_ENVIRONMENT_ID not env_id, TEST_USERNAME not username, etc.)
    - Deprecated files (sub-steps collection, two deprecated env files) have been deleted from docs/
  artifacts:
    - docs/BX-Finance-1-Exchange-Step-by-Step.postman_collection.json
    - docs/BX Finance — 1-Exchange Delegated Chain — pi.flow.postman_collection.json
    - docs/BX Finance — 2-Exchange Delegated Chain — pi.flow.postman_collection.json
    - docs/BX-Finance-Shared.postman_environment.json
  key_links:
    - Shared env variables use UPPERCASE names that exactly match canonical names in banking_api_server/.env.example
    - All {{variable}} references inside pi.flow collection request bodies + pre-request scripts updated to match renamed vars
---

<objective>
Restructure the docs/ Postman collection files: rename the MayAct-Chain step-by-step collection, upgrade variable naming in both pi.flow collections from lowercase to UPPERCASE canonical names (D-05 mapping from CONTEXT.md), create a unified shared environment file, and delete the deprecated files.

Purpose: Foundation for all other Phase 20 work — all four audience-ready collections share one env file with consistent, .env.example-aligned variable names.
Output:
- `docs/BX-Finance-1-Exchange-Step-by-Step.postman_collection.json` (renamed from MayAct-Chain, info.name updated)
- `docs/BX Finance — 1-Exchange Delegated Chain — pi.flow.postman_collection.json` (lowercase vars → UPPERCASE)
- `docs/BX Finance — 2-Exchange Delegated Chain — pi.flow.postman_collection.json` (lowercase vars → UPPERCASE)
- `docs/BX-Finance-Shared.postman_environment.json` (new unified env)
- 3 deprecated files deleted from docs/
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-postman-collections-fix-1-exchange-utilities-and-build-industry-standard-2-exchange-collection/20-CONTEXT.md

Canonical variable name authority:
@banking_api_server/.env.example

Existing collections to modify:
@docs/BX-Finance-MayAct-Chain.postman_collection.json
@docs/BX Finance — 1-Exchange Delegated Chain — pi.flow.postman_collection.json
@docs/BX Finance — 2-Exchange Delegated Chain — pi.flow.postman_collection.json

Existing env files to merge:
@docs/BX-Finance-MayAct-Chain.postman_environment.json
@docs/BX Finance — 2-Exchange Delegated Chain.postman_environment.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename MayAct-Chain collection to 1-Exchange-Step-by-Step</name>
  <files>docs/BX-Finance-1-Exchange-Step-by-Step.postman_collection.json</files>
  <read_first>
    docs/BX-Finance-MayAct-Chain.postman_collection.json
    .planning/phases/20-postman-collections-fix-1-exchange-utilities-and-build-industry-standard-2-exchange-collection/20-CONTEXT.md (decisions D-01, D-02)
  </read_first>
  <action>
    Read docs/BX-Finance-MayAct-Chain.postman_collection.json. Parse the JSON. Make the following changes:
    1. Update `info.name` from its current value to: `BX Finance — 1-Exchange Step-by-Step (RFC 8693)`
    2. Update `info.description` (at the collection level, if present) to explain this is the step-by-step learner collection for the 1-exchange delegated chain, pairing with BX-Finance-Shared.postman_environment.json.
    3. The variable names inside this collection already use UPPERCASE (PINGONE_ENVIRONMENT_ID, PINGONE_CORE_USER_CLIENT_ID, etc.) because it was the canonical step-by-step collection — verify this is the case before writing. Do NOT rename any variables in this collection (they're already correct).
    4. Write the updated JSON to: `docs/BX-Finance-1-Exchange-Step-by-Step.postman_collection.json`
    5. Do NOT yet delete `docs/BX-Finance-MayAct-Chain.postman_collection.json` — deletion is handled in Task 5.

    The output file must be valid Postman collection JSON (Postman Collection Schema v2.1). Pretty-print with 2-space indent.
  </action>
  <verify>
    python3 -c "import json; d=json.load(open('docs/BX-Finance-1-Exchange-Step-by-Step.postman_collection.json')); print(d['info']['name'])"
  </verify>
  <acceptance_criteria>
    - docs/BX-Finance-1-Exchange-Step-by-Step.postman_collection.json exists
    - info.name in that file is exactly: "BX Finance — 1-Exchange Step-by-Step (RFC 8693)"
    - File parses as valid JSON: python3 -c "import json; json.load(open('docs/BX-Finance-1-Exchange-Step-by-Step.postman_collection.json'))" exits 0
    - Collection contains Step 1–4 items (same as MayAct-Chain source) plus utilities
  </acceptance_criteria>
  <done>File exists with correct info.name and parses as valid JSON</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade 1-exchange pi.flow collection — rename lowercase vars to UPPERCASE</name>
  <files>docs/BX Finance — 1-Exchange Delegated Chain — pi.flow.postman_collection.json</files>
  <read_first>
    docs/BX Finance — 1-Exchange Delegated Chain — pi.flow.postman_collection.json
    banking_api_server/.env.example (lines 14–50 — canonical UPPERCASE var names)
    .planning/phases/20-postman-collections-fix-1-exchange-utilities-and-build-industry-standard-2-exchange-collection/20-CONTEXT.md (decision D-05 — the exact rename table)
  </read_first>
  <action>
    Read the existing `docs/BX Finance — 1-Exchange Delegated Chain — pi.flow.postman_collection.json` and apply the D-05 variable rename table to ALL occurrences in the JSON — both the `variable` array (collection-level variable definitions) and every `{{varname}}` reference inside request URLs, headers, body content, and pre-request/test scripts.

    D-05 rename mapping (old → new):
    - env_id           → PINGONE_ENVIRONMENT_ID
    - base_url         → PINGONE_BASE_URL
    - client_id        → PINGONE_CORE_USER_CLIENT_ID
    - client_secret    → PINGONE_CORE_USER_CLIENT_SECRET
    - ai_agent_client_id     → PINGONE_CORE_CLIENT_ID
    - ai_agent_client_secret → PINGONE_CORE_CLIENT_SECRET
    - mcp_client_id    → MCP_CLIENT_ID   (likely already correct — verify first)
    - mcp_client_secret → MCP_CLIENT_SECRET  (likely already correct — verify first)
    - username         → TEST_USERNAME
    - password         → TEST_PASSWORD
    - redirect_uri     → PINGONE_CORE_USER_REDIRECT_URI

    Additional vars NOT in D-05 table that need attention:
    - resource: This is the audience parameter for the authorization request. In the 1-exchange flow, `resource` = the AI Agent audience URI (value: `https://ai-agent.pingdemo.com`). Rename `resource` → `ENDUSER_AUDIENCE` in the collection-level variable definition AND all `{{resource}}` references.
    - scope: Keep as `scope` in collection variables (it's a flow-specific runtime value, not a static config var). Do NOT rename scope.
    - Runtime vars (state, code_verifier, code_challenge, flow_id, flow_href, resume_url, auth_code, access_token, id_token, refresh_token, subject_token, user_sub, mcp_token, etc.): Keep all runtime vars as-is — these are populated by test scripts and must not be renamed as they're internal to the flow.

    Rename strategy: do a global string replacement on the JSON text for each mapping (e.g., replace all `{{env_id}}` with `{{PINGONE_ENVIRONMENT_ID}}`), then update the `variable` array keys. Be careful NOT to rename runtime/internal vars listed above that are not in the D-05 table.

    After renaming, pretty-print with 2-space indent and write back to the SAME file: `docs/BX Finance — 1-Exchange Delegated Chain — pi.flow.postman_collection.json`
  </action>
  <verify>
    python3 -c "
import json
d = json.load(open('docs/BX Finance — 1-Exchange Delegated Chain — pi.flow.postman_collection.json'))
keys = [v['key'] for v in d.get('variable', [])]
print('Keys:', keys)
text = json.dumps(d)
old_vars = ['env_id', 'base_url', '{{client_id}}', '{{client_secret}}', '{{username}}', '{{password}}', '{{redirect_uri}}']
for v in old_vars:
    if v in text:
        print('STILL HAS OLD VAR:', v)
    else:
        print('OK - renamed:', v)
"
  </verify>
  <acceptance_criteria>
    - File parses as valid JSON after modification
    - Collection variable array contains PINGONE_ENVIRONMENT_ID (not env_id)
    - Collection variable array contains PINGONE_BASE_URL (not base_url)
    - Collection variable array contains PINGONE_CORE_USER_CLIENT_ID (not client_id)
    - Collection variable array contains TEST_USERNAME (not username)
    - Collection variable array contains PINGONE_CORE_USER_REDIRECT_URI (not redirect_uri)
    - Collection variable array contains ENDUSER_AUDIENCE (not resource)
    - No raw references to {{env_id}}, {{base_url}}, {{client_id}}, {{username}}, {{redirect_uri}}, {{resource}} remain in the JSON
  </acceptance_criteria>
  <done>All D-05 vars renamed; no old lowercase names remain in collection JSON</done>
</task>

<task type="auto">
  <name>Task 3: Upgrade 2-exchange pi.flow collection — rename lowercase vars to UPPERCASE</name>
  <files>docs/BX Finance — 2-Exchange Delegated Chain — pi.flow.postman_collection.json</files>
  <read_first>
    docs/BX Finance — 2-Exchange Delegated Chain — pi.flow.postman_collection.json
    banking_api_server/.env.example (lines 14–50, 127–135 — UPPERCASE var names including MCP_SERVER_RESOURCE_URI)
    .planning/phases/20-postman-collections-fix-1-exchange-utilities-and-build-industry-standard-2-exchange-collection/20-CONTEXT.md (decision D-05)
  </read_first>
  <action>
    Apply the same D-05 rename table as Task 2 to `docs/BX Finance — 2-Exchange Delegated Chain — pi.flow.postman_collection.json`.

    D-05 rename mapping (same as Task 2):
    - env_id           → PINGONE_ENVIRONMENT_ID
    - base_url         → PINGONE_BASE_URL
    - client_id        → PINGONE_CORE_USER_CLIENT_ID
    - client_secret    → PINGONE_CORE_USER_CLIENT_SECRET
    - ai_agent_client_id     → PINGONE_CORE_CLIENT_ID
    - ai_agent_client_secret → PINGONE_CORE_CLIENT_SECRET
    - mcp_client_id    → MCP_CLIENT_ID
    - mcp_client_secret → MCP_CLIENT_SECRET
    - username         → TEST_USERNAME
    - password         → TEST_PASSWORD
    - redirect_uri     → PINGONE_CORE_USER_REDIRECT_URI

    Additional vars for the 2-exchange collection:
    - resource: Used for the authorization request audience. In this collection `resource` = `https://ai-agent.pingdemo.com` (the AI Agent audience). Rename `resource` → `ENDUSER_AUDIENCE`.
      However, the 2-exchange chain also exchanges the agent token for an MCP token (Exchange #2, step 6b). If the pre-request script for step 6b uses a hardcoded MCP resource URI or a separate variable, add `MCP_RESOURCE_URI` to the collection variables (set value to empty string placeholder) and update those references. Read the step 6b pre-request script carefully to check.
    - scope, pingone_api_token, and other runtime vars: Keep as-is (do NOT rename).
    - Runtime token vars (agent_actor_token, agent_exchanged_token, mcp_actor_token, mcp_exchanged_token, etc.): Keep as-is.

    Write back to the SAME file: `docs/BX Finance — 2-Exchange Delegated Chain — pi.flow.postman_collection.json`
  </action>
  <verify>
    python3 -c "
import json
d = json.load(open('docs/BX Finance — 2-Exchange Delegated Chain — pi.flow.postman_collection.json'))
keys = [v['key'] for v in d.get('variable', [])]
print('Keys:', keys)
"
  </verify>
  <acceptance_criteria>
    - File parses as valid JSON after modification
    - Collection variable array contains PINGONE_ENVIRONMENT_ID (not env_id)
    - Collection variable array contains PINGONE_CORE_CLIENT_ID (not ai_agent_client_id)
    - Collection variable array contains TEST_USERNAME (not username)
    - Collection variable array contains ENDUSER_AUDIENCE (not resource)
    - No raw references to {{env_id}}, {{base_url}}, {{ai_agent_client_id}}, {{username}} remain in the JSON
  </acceptance_criteria>
  <done>All D-05 vars renamed in 2-exchange collection; no old lowercase names remain</done>
</task>

<task type="auto">
  <name>Task 4: Create BX-Finance-Shared.postman_environment.json</name>
  <files>docs/BX-Finance-Shared.postman_environment.json</files>
  <read_first>
    docs/BX-Finance-MayAct-Chain.postman_environment.json (existing UPPERCASE env — use as base for static config vars)
    docs/BX Finance — 2-Exchange Delegated Chain.postman_environment.json (existing env — reference for any additional vars)
    banking_api_server/.env.example (canonical var name authority)
    .planning/phases/20-postman-collections-fix-1-exchange-utilities-and-build-industry-standard-2-exchange-collection/20-CONTEXT.md (D-04, D-05)
  </read_first>
  <action>
    Create `docs/BX-Finance-Shared.postman_environment.json` — a single Postman environment file that works for all four collections.

    The file must use Postman environment JSON format v2.1:
    ```json
    {
      "id": "<generate a new UUID v4>",
      "name": "BX Finance — Shared",
      "values": [
        { "key": "VAR_NAME", "value": "placeholder or empty string", "type": "default", "enabled": true }
      ],
      "_postman_variable_scope": "environment",
      "_postman_exported_at": "<current ISO date>",
      "_postman_exported_using": "Postman/GSD-Phase-20"
    }
    ```

    Include ONLY static configuration variables (values the user fills in before running — credentials, IDs, URLs). Do NOT put runtime token vars here (those are managed by test scripts as collection variables).

    Static config variable list (UPPERCASE, canonical names from .env.example):
    - PINGONE_ENVIRONMENT_ID         — empty string placeholder
    - PINGONE_BASE_URL               — value: "https://auth.pingone.com" (IMPORTANT: this is not in .env.example but is the PingOne auth base URL needed by pi.flow collections; leave a comment-adjacent note in the _postman description; NOTE Postman envs have no inline comments, so add it to the description field of the value if the schema supports it)
    - PINGONE_REGION                 — empty string placeholder (e.g., com, eu, ca, asia)
    - PINGONE_CORE_USER_CLIENT_ID    — empty string placeholder
    - PINGONE_CORE_USER_CLIENT_SECRET — empty string placeholder (mark as type "secret")
    - PINGONE_CORE_USER_REDIRECT_URI — empty string placeholder
    - PINGONE_CORE_CLIENT_ID         — empty string placeholder (staff/BFF/worker app)
    - PINGONE_CORE_CLIENT_SECRET     — empty string placeholder (mark as type "secret")
    - MCP_CLIENT_ID                  — empty string placeholder
    - MCP_CLIENT_SECRET              — empty string placeholder (mark as type "secret")
    - TEST_USERNAME                  — empty string placeholder
    - TEST_PASSWORD                  — empty string placeholder (mark as type "secret")
    - ENDUSER_AUDIENCE               — empty string placeholder (AI Agent resource URI, e.g. https://ai-agent.pingdemo.com)
    - MCP_RESOURCE_URI               — empty string placeholder (MCP Server resource URI)
    - PINGONE_API_AUDIENCE           — empty string placeholder (PingOne Management API audience)
    - PAZ_DECISION_ENDPOINT_ID       — empty string placeholder (PingOne Authorize decision endpoint UUID from PingOne Admin; required for Advanced Utilities PAZ request)

    For "secret" type vars (CLIENT_SECRET, TEST_PASSWORD, MCP_CLIENT_SECRET): use `"type": "secret"` in the value object.
    For all others: use `"type": "default"`.

    Write to: `docs/BX-Finance-Shared.postman_environment.json`
    Pretty-print with 2-space indent.
  </action>
  <verify>
    python3 -c "
import json
d = json.load(open('docs/BX-Finance-Shared.postman_environment.json'))
keys = [v['key'] for v in d['values']]
print('Name:', d['name'])
print('Keys:', keys)
req = ['PINGONE_ENVIRONMENT_ID','PINGONE_BASE_URL','PINGONE_CORE_USER_CLIENT_ID','TEST_USERNAME','ENDUSER_AUDIENCE','PAZ_DECISION_ENDPOINT_ID']
for k in req:
    print(k, 'PRESENT' if k in keys else 'MISSING')
"
  </verify>
  <acceptance_criteria>
    - docs/BX-Finance-Shared.postman_environment.json exists and parses as valid JSON
    - name field is "BX Finance — Shared"
    - values array contains PINGONE_ENVIRONMENT_ID, PINGONE_BASE_URL, PINGONE_CORE_USER_CLIENT_ID, PINGONE_CORE_CLIENT_ID, MCP_CLIENT_ID, TEST_USERNAME, ENDUSER_AUDIENCE, MCP_RESOURCE_URI, PAZ_DECISION_ENDPOINT_ID
    - Secret-type vars (PINGONE_CORE_USER_CLIENT_SECRET, PINGONE_CORE_CLIENT_SECRET, MCP_CLIENT_SECRET, TEST_PASSWORD) have type "secret"
  </acceptance_criteria>
  <done>Shared env file created with all required static config vars and correct types</done>
</task>

<task type="auto">
  <name>Task 5: Delete deprecated collection and environment files</name>
  <files></files>
  <read_first>
    .planning/phases/20-postman-collections-fix-1-exchange-utilities-and-build-industry-standard-2-exchange-collection/20-CONTEXT.md (section under canonical_refs "Collections to Delete" and decisions D-02, D-04)
  </read_first>
  <action>
    Delete the following deprecated files from docs/ using git rm (so the deletion is tracked in git):

    1. `docs/BX Finance — 1-Exchange Delegated Chain (sub-steps).postman_collection.json`
       — superseded by BX-Finance-1-Exchange-Step-by-Step (D-02: retired)
    2. `docs/BX-Finance-MayAct-Chain.postman_collection.json`
       — has been replaced by BX-Finance-1-Exchange-Step-by-Step created in Task 1 (D-01)
    3. `docs/BX-Finance-MayAct-Chain.postman_environment.json`
       — replaced by BX-Finance-Shared.postman_environment.json (D-04)
    4. `docs/BX Finance — 2-Exchange Delegated Chain.postman_environment.json`
       — replaced by BX-Finance-Shared.postman_environment.json (D-04)

    Commands:
    ```bash
    git rm "docs/BX Finance — 1-Exchange Delegated Chain (sub-steps).postman_collection.json"
    git rm "docs/BX-Finance-MayAct-Chain.postman_collection.json"
    git rm "docs/BX-Finance-MayAct-Chain.postman_environment.json"
    git rm "docs/BX Finance — 2-Exchange Delegated Chain.postman_environment.json"
    ```

    Do NOT delete: `AI-IAM-CORE Webinar.postman_collection.json` (stays in root as raw dev artifact per D-11).
    Do NOT delete: `docs/BX Finance — 1-Exchange Delegated Chain — pi.flow.postman_collection.json` (kept, audience = demo runners).
    Do NOT delete: `docs/BX Finance — 2-Exchange Delegated Chain — pi.flow.postman_collection.json` (kept).
  </action>
  <verify>
    ls docs/*.postman_* 2>/dev/null
  </verify>
  <acceptance_criteria>
    - docs/BX Finance — 1-Exchange Delegated Chain (sub-steps).postman_collection.json does NOT exist
    - docs/BX-Finance-MayAct-Chain.postman_collection.json does NOT exist
    - docs/BX-Finance-MayAct-Chain.postman_environment.json does NOT exist
    - docs/BX Finance — 2-Exchange Delegated Chain.postman_environment.json does NOT exist
    - docs/BX-Finance-1-Exchange-Step-by-Step.postman_collection.json DOES exist
    - docs/BX-Finance-Shared.postman_environment.json DOES exist
    - docs/BX Finance — 1-Exchange Delegated Chain — pi.flow.postman_collection.json DOES exist
    - docs/BX Finance — 2-Exchange Delegated Chain — pi.flow.postman_collection.json DOES exist
  </acceptance_criteria>
  <done>All 4 deprecated files removed via git rm; 4 surviving collection/env files present</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] python3 -c "import json; json.load(open('docs/BX-Finance-1-Exchange-Step-by-Step.postman_collection.json'))" exits 0
- [ ] python3 -c "import json; json.load(open('docs/BX Finance — 1-Exchange Delegated Chain — pi.flow.postman_collection.json'))" exits 0
- [ ] python3 -c "import json; json.load(open('docs/BX Finance — 2-Exchange Delegated Chain — pi.flow.postman_collection.json'))" exits 0
- [ ] python3 -c "import json; json.load(open('docs/BX-Finance-Shared.postman_environment.json'))" exits 0
- [ ] ls docs/*.postman_* shows exactly: Step-by-Step collection, 1-exchange pi.flow, 2-exchange pi.flow, Shared env (4 files total from this plan; 5th collection BX-Finance-Advanced-Utilities from plan 20-02 may or may not be present yet)
- [ ] grep -r "{{env_id}}\|{{base_url}}\|{{client_id}}\|{{username}}\|{{ai_agent_client_id}}" docs/ returns no matches in the pi.flow collections
</verification>

<success_criteria>
- All 5 tasks completed
- BX-Finance-1-Exchange-Step-by-Step.postman_collection.json exists with correct info.name
- Both pi.flow collections use UPPERCASE variable names (D-05 mapping applied)
- BX-Finance-Shared.postman_environment.json covers all 4 collections' static config vars
- 4 deprecated files removed via git rm
- All JSON files parse without error
</success_criteria>

<output>
After completion, create `.planning/phases/20-postman-collections-fix-1-exchange-utilities-and-build-industry-standard-2-exchange-collection/20-01-SUMMARY.md`
</output>
