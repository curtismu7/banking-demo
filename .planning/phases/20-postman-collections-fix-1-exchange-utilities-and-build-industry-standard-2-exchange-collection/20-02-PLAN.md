---
phase: 20-postman-collections-fix-1-exchange-utilities-and-build-industry-standard-2-exchange-collection
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/BX-Finance-Advanced-Utilities.postman_collection.json
autonomous: true
requirements: []
must_haves:
  truths:
    - docs/BX-Finance-Advanced-Utilities.postman_collection.json exists with info.name = "BX Finance — Advanced Utilities"
    - Collection contains exactly 2 requests — "PAZ Policy Decision" and "Token Revocation"
    - PAZ request URL uses {{PAZ_DECISION_ENDPOINT_ID}} (not the hardcoded UUID cc26aaa9-9b27-4409-bc60-f8a5f98d6296)
    - All variable references use UPPERCASE naming (PINGONE_ENVIRONMENT_ID, PINGONE_BASE_URL, MCP_CLIENT_ID, etc.)
    - Webinar source file (AI-IAM-CORE Webinar.postman_collection.json) is not modified or deleted
  artifacts:
    - docs/BX-Finance-Advanced-Utilities.postman_collection.json
  key_links:
    - Advanced Utilities collection variables align with BX-Finance-Shared.postman_environment.json (same UPPERCASE naming convention)
    - PAZ_DECISION_ENDPOINT_ID is a collection variable (user fills it from PingOne Admin) and also present in BX-Finance-Shared env
---

<objective>
Create the `docs/BX-Finance-Advanced-Utilities.postman_collection.json` by extracting the two unique, valuable requests from the `AI-IAM-CORE Webinar.postman_collection.json` — the PAZ Policy Decision request and the Token Revocation request — and promoting them to a clean, production-ready collection with UPPERCASE vars, clear descriptions, and the PAZ endpoint UUID extracted to a portable variable.

Purpose: Provide an "engineer" collection for advanced PingOne operations (PAZ policy testing, token revocation) that pairs with the shared env file.
Output: `docs/BX-Finance-Advanced-Utilities.postman_collection.json`
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-postman-collections-fix-1-exchange-utilities-and-build-industry-standard-2-exchange-collection/20-CONTEXT.md

Source collection (read-only, do NOT modify):
@AI-IAM-CORE Webinar.postman_collection.json

Canonical variable names:
@banking_api_server/.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build BX-Finance-Advanced-Utilities collection from Webinar source</name>
  <files>docs/BX-Finance-Advanced-Utilities.postman_collection.json</files>
  <read_first>
    AI-IAM-CORE Webinar.postman_collection.json (source — extract PAZ + Revocation requests; do NOT modify this file)
    banking_api_server/.env.example (canonical var names for UPPERCASE alignment)
    .planning/phases/20-postman-collections-fix-1-exchange-utilities-and-build-industry-standard-2-exchange-collection/20-CONTEXT.md (decisions D-07, D-08, D-09, D-10)
  </read_first>
  <action>
    Read `AI-IAM-CORE Webinar.postman_collection.json`. Extract exactly TWO requests:
    1. "Call PAZ Copy 2" — the PAZ policy decision request
    2. "Token Revocation Copy" — the token revocation request

    Build a NEW Postman collection file at `docs/BX-Finance-Advanced-Utilities.postman_collection.json` with this structure:

    Collection-level:
    - `info.name`: "BX Finance — Advanced Utilities"
    - `info.schema`: "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
    - `info.description`: Multi-line description explaining:
      - "Advanced utility requests for BX Finance delegated token flows."
      - "Requires: BX-Finance-Shared.postman_environment.json imported and active."
      - "PAZ Policy Decision: Tests the PingOne Authorize (PAZ) policy engine. Set PAZ_DECISION_ENDPOINT_ID in the shared env (find it in PingOne Admin → Authorize → Decision Endpoints). Requires an MCP token in {{mcp_exchanged_token}} — run a 2-exchange pi.flow collection first."
      - "Token Revocation: Revokes an MCP token using client credentials. Set the token to revoke in {{token_to_revoke}} before running."

    Collection-level `variable` array — only include PAZ-specific and runtime vars not present in shared env:
    - PAZ_DECISION_ENDPOINT_ID: value "", type "default" (static config — must be filled from PingOne Admin; also in shared env)
    - mcp_exchanged_token: value "", type "default" (runtime; populated by 2-exchange pi.flow test scripts)
    - token_to_revoke: value "", type "default" (runtime; user pastes token to revoke here)
    Note: PINGONE_ENVIRONMENT_ID, PINGONE_BASE_URL, MCP_CLIENT_ID, MCP_CLIENT_SECRET come from the shared env at runtime.

    Request 1 — PAZ Policy Decision:
    - name: "PAZ Policy Decision"
    - method: POST
    - URL: `{{PINGONE_BASE_URL}}/v1/environments/{{PINGONE_ENVIRONMENT_ID}}/decisionEndpoints/{{PAZ_DECISION_ENDPOINT_ID}}`
      Note: The Webinar collection URL uses `{{APIPath}}/environments/{{envID}}/decisionEndpoints/cc26aaa9-9b27-4409-bc60-f8a5f98d6296` — extract the hardcoded UUID to `{{PAZ_DECISION_ENDPOINT_ID}}` and rename `{{APIPath}}` → `{{PINGONE_BASE_URL}}` (without the trailing /v1; keep /v1 in the path), `{{envID}}` → `{{PINGONE_ENVIRONMENT_ID}}`.
    - headers: Content-Type: application/json; Authorization: Bearer {{mcp_exchanged_token}}
    - body (raw JSON, keep the structure from the Webinar collection, replace hardcoded `inMCPTokenJWT` value with `{{mcp_exchanged_token}}`):
      ```json
      {
        "parameters": {
          "inConditionAmountCheck": "false",
          "inConditionCheckAgent": "false",
          "inConditionAgentCheckAmount": "AgentCheckAmount",
          "inAgentAmount": "50",
          "inMCPTokenJWT": "{{mcp_exchanged_token}}"
        },
        "resources": {
          "environmentId": "{{PINGONE_ENVIRONMENT_ID}}"
        }
      }
      ```
    - description: "Calls the PingOne Authorize (PAZ) decision endpoint to test policy evaluation. The MCP token is passed both as the Bearer token (for auth) and as inMCPTokenJWT (for policy evaluation against the token claims). Expected response: 200 with a 'decision' field (PERMIT or DENY). Prerequisites: Run a 2-exchange pi.flow first to populate {{mcp_exchanged_token}}. Fill PAZ_DECISION_ENDPOINT_ID in the shared env (PingOne Admin → Authorize → Decision Endpoints → copy the UUID from the endpoint URL)."
    - pre-request script: Check that `mcp_exchanged_token` is set. If not, throw error: "[PAZ Policy Decision] mcp_exchanged_token is not set — run the 2-exchange pi.flow collection first (Step 6b saves the token)."
    - test script: Log the response status and body. If status is 200, log the `decision` field value.

    Request 2 — Token Revocation:
    - name: "Token Revocation"
    - method: POST
    - URL: `{{PINGONE_BASE_URL}}/{{PINGONE_ENVIRONMENT_ID}}/as/revoke`
      Note: The Webinar URL uses `{{authPath}}/{{envID}}/as/revoke` — rename `{{authPath}}` → `{{PINGONE_BASE_URL}}`, `{{envID}}` → `{{PINGONE_ENVIRONMENT_ID}}`.
    - body (x-www-form-urlencoded, NOT JSON):
      - token: `{{token_to_revoke}}`
      - client_id: `{{MCP_CLIENT_ID}}`
      - client_secret: `{{MCP_CLIENT_SECRET}}`
    - description: "Revokes an MCP token using the MCP service client credentials. Set the token you want to revoke in the {{token_to_revoke}} collection variable before running. Expected response: 200 with empty body (token revoked successfully). The token is no longer valid after this call — subsequent introspection will show 'active: false'."
    - pre-request script: Check that `token_to_revoke` is set. If not, throw error: "[Token Revocation] token_to_revoke is not set — paste the token you want to revoke into the token_to_revoke collection variable."
    - test script: Log the response status. If status is 200, log "Token revoked successfully."

    Write the complete collection to `docs/BX-Finance-Advanced-Utilities.postman_collection.json` with 2-space indent.

    CRITICAL: Do NOT modify `AI-IAM-CORE Webinar.postman_collection.json` — read it only.
  </action>
  <verify>
    python3 -c "
import json
d = json.load(open('docs/BX-Finance-Advanced-Utilities.postman_collection.json'))
print('Name:', d['info']['name'])
print('Items:', [i['name'] for i in d['item']])
vars = [v['key'] for v in d.get('variable', [])]
print('Collection vars:', vars)
import re
text = json.dumps(d)
old = ['{{envID}}', '{{APIPath}}', '{{authPath}}', 'cc26aaa9-9b27-4409-bc60-f8a5f98d6296']
for v in old:
    print(v, 'STILL PRESENT' if v in text else 'OK - replaced')
"
  </verify>
  <acceptance_criteria>
    - docs/BX-Finance-Advanced-Utilities.postman_collection.json exists and parses as valid JSON
    - info.name is "BX Finance — Advanced Utilities"
    - Collection contains exactly 2 items: "PAZ Policy Decision" and "Token Revocation"
    - Hardcoded UUID cc26aaa9-9b27-4409-bc60-f8a5f98d6296 does NOT appear anywhere in the file (replaced by {{PAZ_DECISION_ENDPOINT_ID}})
    - {{envID}} does NOT appear in the file (replaced by {{PINGONE_ENVIRONMENT_ID}})
    - {{APIPath}} does NOT appear in the file (replaced by {{PINGONE_BASE_URL}})
    - {{authPath}} does NOT appear in the file (replaced by {{PINGONE_BASE_URL}})
    - PAZ_DECISION_ENDPOINT_ID appears in collection variable array
    - AI-IAM-CORE Webinar.postman_collection.json is unchanged (git diff shows no changes to it)
  </acceptance_criteria>
  <done>BX-Finance-Advanced-Utilities.postman_collection.json created with 2 clean requests; source Webinar file unmodified</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] python3 -c "import json; json.load(open('docs/BX-Finance-Advanced-Utilities.postman_collection.json'))" exits 0
- [ ] python3 -c "import json; d=json.load(open('docs/BX-Finance-Advanced-Utilities.postman_collection.json')); assert d['info']['name']=='BX Finance — Advanced Utilities'; print('OK')"
- [ ] grep "cc26aaa9" "docs/BX-Finance-Advanced-Utilities.postman_collection.json" returns no output (UUID extracted to variable)
- [ ] git diff "AI-IAM-CORE Webinar.postman_collection.json" is empty (source file unchanged)
</verification>

<success_criteria>
- Task 1 completed
- BX-Finance-Advanced-Utilities.postman_collection.json exists with correct name and 2 clean requests
- PAZ UUID extracted to PAZ_DECISION_ENDPOINT_ID collection variable
- All variable references use UPPERCASE convention aligned with shared env
- Webinar source file is untouched
</success_criteria>

<output>
After completion, create `.planning/phases/20-postman-collections-fix-1-exchange-utilities-and-build-industry-standard-2-exchange-collection/20-02-SUMMARY.md`
</output>
