---
phase: 22-agent-capability-audit-enterprise-grade-tools-full-account-data-brave-search-routing-groq-nlu-exhaustive-chip-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - banking_api_ui/src/utils/agentToolSteps.js
  - banking_mcp_server/src/tools/BankingToolRegistry.ts
  - banking_api_server/routes/accounts.js
autonomous: true
requirements:
  - AGENT-01
  - AGENT-02
must_haves:
  truths:
    - agentToolSteps.js returns a chip for every tool in BankingToolRegistry (no tool has zero chip coverage)
    - query_user_by_email tool has a chip (action type 'query_user')
    - accounts API returns all expected fields (id, type, name, balance, currency)
    - transactions API returns all expected fields (id, type, amount, description, status, createdAt)
    - Groq NLU groqConfigured flag accurately reflects whether GROQ_API_KEY is set
  artifacts:
    - path: banking_api_ui/src/utils/agentToolSteps.js
      provides: Chip definitions for all 7 MCP tools
    - path: banking_api_server/routes/accounts.js
      provides: Full account and transaction data fields
  key_links:
    - from: banking_api_ui/src/utils/agentToolSteps.js
      to: banking_mcp_server/src/tools/BankingToolRegistry.ts
      via: Tool name strings must match exactly
---

<objective>
Audit all MCP tool chip definitions against the BankingToolRegistry, add the missing `query_user_by_email` chip, and verify that account and transaction data routes return complete field sets.

Purpose: Ensure every registered MCP tool has a corresponding UI chip so users can see exactly which tools the agent called. Close the gap between 7 registered tools and 6 chip-covered actions.
Output:
- `banking_api_ui/src/utils/agentToolSteps.js` — all 7 tools covered
- Verified: accounts + transactions routes return complete data
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md

BankingToolRegistry (source of truth for all MCP tools):
@banking_mcp_server/src/tools/BankingToolRegistry.ts

Current chip definitions (to be extended):
@banking_api_ui/src/utils/agentToolSteps.js

BankingAgent chip rendering (how chips are displayed):
@banking_api_ui/src/components/BankingAgent.js

Accounts route (verify full data):
@banking_api_server/routes/accounts.js

<interfaces>
<!-- BankingToolRegistry exports these 7 tool names: -->
<!-- get_my_accounts, get_account_balance, get_my_transactions -->
<!-- create_deposit, create_withdrawal, create_transfer, query_user_by_email -->

<!-- agentToolSteps.js currently maps: -->
<!-- 'accounts' → [{name: 'get_my_accounts'}] -->
<!-- 'transactions' → [{name: 'get_my_transactions'}] -->
<!-- 'balance' → [{name: 'get_account_balance'}] -->
<!-- 'deposit' → [{name: 'create_deposit'}] -->
<!-- 'withdraw' → [{name: 'create_withdrawal'}] -->
<!-- 'transfer' → [{name: 'create_transfer'}] -->
<!-- 'mcp_tools' → [] (no chips!) -->
<!-- MISSING: 'query_user' → [{name: 'query_user_by_email'}] -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add query_user chip + fix mcp_tools case in agentToolSteps.js</name>
  <files>banking_api_ui/src/utils/agentToolSteps.js</files>
  <action>
    Read `banking_api_ui/src/utils/agentToolSteps.js`. Make these changes:

    1. Add a new `case 'query_user':` returning `[row('query_user_by_email')]`
    2. Update `case 'mcp_tools':` — currently returns `[]`. Change to return `[]` with a comment
       `// mcp_tools is a meta-action; individual tool calls show their own chips via BFF response`
       (leave as empty array — this is correct, it's a passthrough)
    3. Add a new `case 'web_search':` returning `[row('brave_search')]` — pre-wire the chip for Phase 22 plan 02

    No other changes. Do NOT remove any existing cases.

    Final switch should cover: accounts, transactions, balance, deposit, withdraw, transfer, mcp_tools, query_user, web_search.
  </action>
  <verify>
    `grep -n "query_user\|web_search\|brave_search" banking_api_ui/src/utils/agentToolSteps.js`
    Should show all three new entries.
  </verify>
  <done>
    agentToolSteps.js has case entries for query_user (→ query_user_by_email) and web_search (→ brave_search). The mcp_tools case has an explanatory comment.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify full account and transaction data fields</name>
  <files>banking_api_server/routes/accounts.js</files>
  <action>
    Read `banking_api_server/routes/accounts.js`. Audit the GET accounts and GET transactions responses.

    **For accounts (`GET /api/accounts/my` or equivalent):**
    The response must include for each account: `id`, `type` (checking/savings), `name` or `accountName`, `balance`, `currency` (or default to USD). If `currency` is missing from the response mapping, add it with value `'USD'` as default.

    **For transactions (`GET /api/accounts/transactions` or equivalent):**
    The response must include for each transaction: `id`, `type` (debit/credit/transfer/deposit/withdrawal), `amount`, `description`, `status` (completed/pending), `createdAt` or `created_at`.
    Also verify: `accountId` is included on each transaction so the UI can associate it with an account.

    If any field is missing from the serialization/mapping, add it. If the field exists on the underlying data model but isn't being returned, include it in the response object.

    Read the route file carefully before making any changes. Only modify if a field is genuinely missing.
    Do NOT change route paths, middleware, or authentication logic.
  </action>
  <verify>
    No syntax errors: `cd banking_api_server && node -e "require('./routes/accounts.js')" 2>&1 | head -5`
    (If the require check passes without throwing, the syntax is valid. Ignore warnings about express app context.)
  </verify>
  <done>
    GET accounts response includes id, type, name, balance, currency. GET transactions response includes id, type, amount, description, status, createdAt, accountId. No other changes made to the route.
  </done>
</task>

</tasks>

<verification>
Chip coverage audit:
```
grep -A2 "case '" banking_api_ui/src/utils/agentToolSteps.js
```
Should show 9 cases covering all 7 MCP tools plus mcp_tools and web_search.

Account data field verification:
```
grep -n "currency\|accountId\|created_at\|createdAt" banking_api_server/routes/accounts.js | head -20
```
</verification>

<success_criteria>
- `agentToolSteps.js` has `case 'query_user':` returning `query_user_by_email` chip
- `agentToolSteps.js` has `case 'web_search':` returning `brave_search` chip (pre-wired for plan-02)
- Account route returns complete field set including currency
- Transaction route returns complete field set including accountId
- All 7 tool names in BankingToolRegistry have a corresponding chip entry
</success_criteria>

<output>
After completion, create `.planning/phases/22-agent-capability-audit-enterprise-grade-tools-full-account-data-brave-search-routing-groq-nlu-exhaustive-chip-coverage/22-01-SUMMARY.md`
</output>
