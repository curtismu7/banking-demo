---
phase: 24-agent-builder-landscape-langchain-open-source-and-commercial-frameworks-vendor-comparison
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - banking_api_ui/src/components/education/EducationPanelsHost.js
  - banking_api_ui/src/components/education/educationCommands.js
autonomous: true
requirements: []
must_haves:
  truths:
    - EducationPanelsHost.js renders AgentBuilderLandscapePanel with isOpen={panel === EDU.AGENT_BUILDER_LANDSCAPE}
    - educationCommands.js has an entry with id 'agent-builders' pointing to panel EDU.AGENT_BUILDER_LANDSCAPE, tab 'langchain'
    - educationCommands.js has an entry with id 'agent-builder-compare' pointing to tab 'comparison'
    - npm run build in banking_api_ui exits 0
  artifacts:
    - banking_api_ui/src/components/education/EducationPanelsHost.js
    - banking_api_ui/src/components/education/educationCommands.js
  key_links:
    - AgentBuilderLandscapePanel reachable from the Education Bar and Banking Agent via the new commands
---

<objective>
Wire `AgentBuilderLandscapePanel` into the running application: register it in `EducationPanelsHost.js` and add two commands to `educationCommands.js`. Verify the React build passes.

Purpose: The panel created in plan 24-01 is unreachable from the UI until this plan connects it.
Output: Panel surfaced via Education Bar and Banking Agent; build passes.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@banking_api_ui/src/components/education/EducationPanelsHost.js
@banking_api_ui/src/components/education/educationCommands.js
@banking_api_ui/src/components/education/educationIds.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register AgentBuilderLandscapePanel in EducationPanelsHost.js</name>
  <files>banking_api_ui/src/components/education/EducationPanelsHost.js</files>
  <read_first>banking_api_ui/src/components/education/EducationPanelsHost.js</read_first>
  <action>
    Two targeted additions to `EducationPanelsHost.js`:

    1. Add the import after the `Oidc21Panel` import line:
       ```js
       import AgentBuilderLandscapePanel from './AgentBuilderLandscapePanel';
       ```

    2. Add the render inside the returned fragment, after the `<Oidc21Panel ...>` line:
       ```jsx
       <AgentBuilderLandscapePanel isOpen={panel === EDU.AGENT_BUILDER_LANDSCAPE} onClose={close} initialTabId={tab} />
       ```

    Do not modify any other lines.
  </action>
  <verify>grep "AgentBuilderLandscapePanel" banking_api_ui/src/components/education/EducationPanelsHost.js</verify>
  <acceptance_criteria>
    - EducationPanelsHost.js contains `import AgentBuilderLandscapePanel from './AgentBuilderLandscapePanel'`
    - EducationPanelsHost.js contains `panel === EDU.AGENT_BUILDER_LANDSCAPE`
    - All existing panel registrations are unchanged
  </acceptance_criteria>
  <done>AgentBuilderLandscapePanel imported and rendered in EducationPanelsHost</done>
</task>

<task type="auto">
  <name>Task 2: Add agent builder commands to educationCommands.js</name>
  <files>banking_api_ui/src/components/education/educationCommands.js</files>
  <read_first>banking_api_ui/src/components/education/educationCommands.js</read_first>
  <action>
    Add two entries to the `EDUCATION_COMMANDS` array after the existing last entry (`agentic-maturity`):

    ```js
    { id: 'agent-builders', label: '🤖 Agent Builder Landscape', panel: EDU.AGENT_BUILDER_LANDSCAPE, tab: 'langchain' },
    { id: 'agent-builder-compare', label: '📊 Agent Builder Comparison', panel: EDU.AGENT_BUILDER_LANDSCAPE, tab: 'comparison' },
    ```

    The first opens to the LangChain tab (most relevant to this demo). The second jumps to the comparison matrix.

    Do not modify any existing entries.
  </action>
  <verify>grep "agent-builders\|agent-builder-compare" banking_api_ui/src/components/education/educationCommands.js</verify>
  <acceptance_criteria>
    - educationCommands.js contains an entry with id: 'agent-builders' and tab: 'langchain'
    - educationCommands.js contains an entry with id: 'agent-builder-compare' and tab: 'comparison'
    - All existing entries are unchanged
  </acceptance_criteria>
  <done>Two agent builder commands added to EDUCATION_COMMANDS</done>
</task>

<task type="auto">
  <name>Task 3: Verify React build passes</name>
  <files></files>
  <read_first></read_first>
  <action>
    Run the production build:

    ```bash
    cd banking_api_ui && npm run build 2>&1 | tail -20
    ```

    If it fails, read the error and fix:
    - Missing `React` import in `AgentBuilderLandscapePanel.js`
    - Unclosed JSX tags in content components
    - `EDU.AGENT_BUILDER_LANDSCAPE` not exported from educationIds.js
    - Import path typo in EducationPanelsHost.js

    Fix inline and re-run until exit code 0.
  </action>
  <verify>cd banking_api_ui && npm run build 2>&1 | grep -E "Compiled successfully|webpack compiled|Failed to compile|error" | tail -5</verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - Build output contains "Compiled successfully" or "webpack compiled"
    - No "Module not found" or "Failed to compile" in output
  </acceptance_criteria>
  <done>npm run build exits 0</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] grep "AgentBuilderLandscapePanel" banking_api_ui/src/components/education/EducationPanelsHost.js
- [ ] grep "agent-builders" banking_api_ui/src/components/education/educationCommands.js
- [ ] cd banking_api_ui && npm run build exits 0
</verification>

<success_criteria>
- All 3 tasks completed
- AgentBuilderLandscapePanel registered in EducationPanelsHost
- Two commands added (LangChain tab + comparison tab)
- npm run build passes with exit code 0
</success_criteria>

<output>
After completion, create `.planning/phases/24-agent-builder-landscape-langchain-open-source-and-commercial-frameworks-vendor-comparison/24-02-SUMMARY.md`
</output>
