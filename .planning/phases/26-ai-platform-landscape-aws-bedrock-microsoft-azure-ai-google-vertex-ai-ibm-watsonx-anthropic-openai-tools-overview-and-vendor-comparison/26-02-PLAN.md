---
phase: 26-ai-platform-landscape-aws-bedrock-microsoft-azure-ai-google-vertex-ai-ibm-watsonx-anthropic-openai-tools-overview-and-vendor-comparison
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - banking_api_ui/src/components/education/EducationPanelsHost.js
  - banking_api_ui/src/components/education/educationCommands.js
autonomous: true
requirements: []
must_haves:
  truths:
    - EducationPanelsHost.js renders AiPlatformLandscapePanel with isOpen={panel === EDU.AI_PLATFORM_LANDSCAPE}
    - educationCommands.js includes an entry with panel: EDU.AI_PLATFORM_LANDSCAPE pointing to tab 'aws'
    - npm run build in banking_api_ui exits 0
  artifacts:
    - banking_api_ui/src/components/education/EducationPanelsHost.js
    - banking_api_ui/src/components/education/educationCommands.js
  key_links:
    - AiPlatformLandscapePanel wired into EducationPanelsHost so it renders when EDU.AI_PLATFORM_LANDSCAPE is the active panel
    - educationCommands entry allows the Banking Agent and Education Bar to surface the panel by command
---

<objective>
Wire `AiPlatformLandscapePanel` into the running application: register it in `EducationPanelsHost.js` (so it renders when triggered) and add it to `educationCommands.js` (so the Banking Agent and Education Bar surface it). Then verify the React build passes.

Purpose: The panel created in plan 26-01 is currently unreachable from the UI — this plan connects it to the app's education system.
Output: Panel surfaced via Education Bar and Banking Agent; build passes.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@banking_api_ui/src/components/education/EducationPanelsHost.js
@banking_api_ui/src/components/education/educationCommands.js
@banking_api_ui/src/components/education/educationIds.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register AiPlatformLandscapePanel in EducationPanelsHost.js</name>
  <files>banking_api_ui/src/components/education/EducationPanelsHost.js</files>
  <read_first>banking_api_ui/src/components/education/EducationPanelsHost.js</read_first>
  <action>
    Make two targeted additions to `EducationPanelsHost.js`:

    1. Add the import after the existing `Oidc21Panel` import line:
       ```js
       import AiPlatformLandscapePanel from './AiPlatformLandscapePanel';
       ```

    2. Add the panel render inside the returned fragment, after the `<Oidc21Panel ...>` line:
       ```jsx
       <AiPlatformLandscapePanel isOpen={panel === EDU.AI_PLATFORM_LANDSCAPE} onClose={close} initialTabId={tab} />
       ```

    Do not modify any other lines. The `EDU.AI_PLATFORM_LANDSCAPE` key was added to `educationIds.js` in plan 26-01.
  </action>
  <verify>
    grep "AiPlatformLandscapePanel" banking_api_ui/src/components/education/EducationPanelsHost.js
  </verify>
  <acceptance_criteria>
    - EducationPanelsHost.js contains `import AiPlatformLandscapePanel from './AiPlatformLandscapePanel'`
    - EducationPanelsHost.js contains `panel === EDU.AI_PLATFORM_LANDSCAPE`
    - All existing panel registrations are still present (verify Oidc21Panel, AgenticMaturityPanel lines unchanged)
  </acceptance_criteria>
  <done>AiPlatformLandscapePanel imported and rendered in EducationPanelsHost</done>
</task>

<task type="auto">
  <name>Task 2: Add AI platform commands to educationCommands.js</name>
  <files>banking_api_ui/src/components/education/educationCommands.js</files>
  <read_first>banking_api_ui/src/components/education/educationCommands.js</read_first>
  <action>
    Add two entries to the `EDUCATION_COMMANDS` array in `educationCommands.js`, after the existing last entry (`agentic-maturity`):

    ```js
    { id: 'ai-platforms', label: '🌐 AI Platform Landscape', panel: EDU.AI_PLATFORM_LANDSCAPE, tab: 'aws' },
    { id: 'ai-platform-compare', label: '📊 AI Platform Comparison', panel: EDU.AI_PLATFORM_LANDSCAPE, tab: 'comparison' },
    ```

    The first command opens directly to the AWS tab (first overview). The second jumps straight to the comparison tab — useful when a user just wants the feature matrix.

    Do not modify any existing command entries.
  </action>
  <verify>
    grep "AI_PLATFORM_LANDSCAPE\|ai-platforms\|ai-platform-compare" banking_api_ui/src/components/education/educationCommands.js
  </verify>
  <acceptance_criteria>
    - educationCommands.js contains an entry with id: 'ai-platforms' and panel: EDU.AI_PLATFORM_LANDSCAPE
    - educationCommands.js contains an entry with id: 'ai-platform-compare' pointing to tab: 'comparison'
    - All existing EDUCATION_COMMANDS entries are unchanged
  </acceptance_criteria>
  <done>Two AI platform commands added to EDUCATION_COMMANDS array</done>
</task>

<task type="auto">
  <name>Task 3: Verify React build passes</name>
  <files></files>
  <read_first></read_first>
  <action>
    Run the production build to verify no syntax errors or missing imports were introduced:

    ```bash
    cd banking_api_ui && npm run build 2>&1 | tail -20
    ```

    If the build fails:
    - Read the error output carefully
    - Fix the specific error (missing import, syntax error, undefined variable, etc.)
    - Re-run until the build exits 0

    Common issues to check if build fails:
    - `AiPlatformLandscapePanel.js` missing a `React` import (required for JSX)
    - Unclosed JSX tags in one of the content components
    - `EDU.AI_PLATFORM_LANDSCAPE` not exported from educationIds.js
    - Import path typo in EducationPanelsHost.js
  </action>
  <verify>
    cd banking_api_ui && npm run build 2>&1 | grep -E "Compiled|error|Error|warning" | tail -10
  </verify>
  <acceptance_criteria>
    - `npm run build` in banking_api_ui exits with code 0
    - Output contains "Compiled successfully" or "webpack compiled"
    - No "Module not found" errors in build output
    - No "Failed to compile" in build output
  </acceptance_criteria>
  <done>npm run build exits 0 with no errors</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] grep "AiPlatformLandscapePanel" banking_api_ui/src/components/education/EducationPanelsHost.js
- [ ] grep "ai-platforms" banking_api_ui/src/components/education/educationCommands.js
- [ ] cd banking_api_ui && npm run build exits 0
</verification>

<success_criteria>
- All 3 tasks completed
- AiPlatformLandscapePanel registered in EducationPanelsHost
- Two commands added to EDUCATION_COMMANDS (overview + comparison jump)
- npm run build passes with exit code 0
</success_criteria>

<output>
After completion, create `.planning/phases/26-ai-platform-landscape-aws-bedrock-microsoft-azure-ai-google-vertex-ai-ibm-watsonx-anthropic-openai-tools-overview-and-vendor-comparison/26-02-SUMMARY.md`
</output>
