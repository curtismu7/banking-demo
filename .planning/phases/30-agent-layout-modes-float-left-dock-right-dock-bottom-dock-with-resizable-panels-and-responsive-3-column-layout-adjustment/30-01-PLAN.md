---
phase: 30-agent-layout-modes-float-left-dock-right-dock-bottom-dock-with-resizable-panels-and-responsive-3-column-layout-adjustment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - banking_api_ui/src/context/AgentUiModeContext.js
  - banking_api_ui/src/context/__tests__/AgentUiModeContext.test.js
autonomous: true
requirements:
  - LAYOUT-01
  - LAYOUT-02

must_haves:
  truths:
    - "AgentUiState.placement accepts 'left-dock' and 'right-dock' without throwing"
    - "readState() correctly round-trips 'left-dock'/'right-dock' through localStorage"
    - "syncLegacyString handles new values (maps to 'both' for legacy compatibility)"
    - "setAgentUi({ placement: 'left-dock', fab: true }) updates state and localStorage correctly"
  artifacts:
    - path: "banking_api_ui/src/context/AgentUiModeContext.js"
      provides: "Extended AgentUiState type with left-dock/right-dock placements"
      contains: "left-dock"
    - path: "banking_api_ui/src/context/__tests__/AgentUiModeContext.test.js"
      provides: "Tests for new placement values"
      exports: ["readState left-dock", "syncLegacyString left-dock"]
  key_links:
    - from: "AgentUiModeContext.js"
      to: "localStorage STORAGE_KEY_V2"
      via: "readState + setAgentUi"
      pattern: "left-dock|right-dock"
---

<objective>
Extend AgentUiModeContext to support 'left-dock' and 'right-dock' placement values alongside the existing 'middle', 'bottom', and 'none' values.

Purpose: Foundation for the side dock UI. All consumers of useAgentUiMode need the new values to be valid before any rendering can use them.
Output: Updated AgentUiModeContext.js + tests proving round-trip storage works for new values.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/ROADMAP.md
</execution_context>

<context>
@banking_api_ui/src/context/AgentUiModeContext.js

<interfaces>
<!-- Current AgentUiState type (from AgentUiModeContext.js line 8-11): -->
```js
/**
 * @typedef {object} AgentUiState
 * @property {'middle' | 'bottom' | 'none'} placement
 * @property {boolean} fab
 */
```

<!-- readState() validation guard (line 63-70): -->
```js
if (
  (p === 'middle' || p === 'bottom' || p === 'none') &&
  typeof fab === 'boolean'
) {
  if (p === 'none' && !fab) {
    return { placement: 'none', fab: true };
  }
  return { placement: p, fab };
}
```

<!-- syncLegacyString (line 31-53): maps placement to legacy string values -->
<!-- setAgentUi (line 107-130): placement guard: if placement === 'none', force fab = true -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend placement type and storage guards</name>
  <files>banking_api_ui/src/context/AgentUiModeContext.js</files>
  <action>
    Make the following targeted changes to AgentUiModeContext.js:

    1. **JSDoc typedef** (line 9): Change placement type string to:
       `'middle' | 'bottom' | 'none' | 'left-dock' | 'right-dock'`

    2. **readState() validation guard** (around line 63): Extend the `||` chain to include the two new values:
       `(p === 'middle' || p === 'bottom' || p === 'none' || p === 'left-dock' || p === 'right-dock')`
       Keep the `'none' && !fab` safety rule unchanged. Add analogous rule for new docks: if p is 'left-dock' or 'right-dock' and fab is undefined/non-boolean, default fab to true.

    3. **syncLegacyString** (around line 31): Add two cases before the final fallback:
       ```js
       if (state.placement === 'left-dock') {
         localStorage.setItem(STORAGE_KEY_LEGACY, 'both');
         return;
       }
       if (state.placement === 'right-dock') {
         localStorage.setItem(STORAGE_KEY_LEGACY, 'both');
         return;
       }
       ```
       (Legacy key only supports 'floating'/'embedded'/'both' — 'both' is the least-surprising fallback for anything with an embedded agent)

    4. **setAgentUi callback** (around line 111): The existing `if (placement === 'none') { fab = true; }` guard is correct for new values (left-dock and right-dock should still allow fab to be toggled). No change needed here — just verify the guard is placement-specific.

    5. **Update JSDoc comment** above AgentUiModeProvider (around line 90):
       Add: "Left-dock — agent in collapsible left sidebar (width-resizable). Right-dock — agent in collapsible right sidebar (width-resizable)."
  </action>
  <verify>grep -n "left-dock.*right-dock\|right-dock.*left-dock" banking_api_ui/src/context/AgentUiModeContext.js</verify>
  <done>Both 'left-dock' and 'right-dock' appear in the typedef, readState guard, and syncLegacyString</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for new placement values</name>
  <files>banking_api_ui/src/context/__tests__/AgentUiModeContext.test.js</files>
  <behavior>
    - Test: readState() with localStorage containing { placement: 'left-dock', fab: true } returns { placement: 'left-dock', fab: true }
    - Test: readState() with localStorage containing { placement: 'right-dock', fab: false } returns { placement: 'right-dock', fab: false }
    - Test: readState() with { placement: 'left-dock', fab: undefined } falls back to { placement: 'left-dock', fab: true }
    - Test: legacy storage value 'floating' still yields { placement: 'none', fab: true } (no regression)
    - Test: syncLegacyString({ placement: 'left-dock', fab: true }) writes 'both' to STORAGE_KEY_LEGACY
    - Test: syncLegacyString({ placement: 'right-dock', fab: false }) writes 'both' to STORAGE_KEY_LEGACY
  </behavior>
  <action>
    Read the existing test file first to understand the mock localStorage setup and test patterns.
    Add the 6 new test cases to the existing describe block(s). Do not remove existing tests.
    Follow the exact same mock pattern already used in the file (jest mock for localStorage or direct storage manipulation — match existing style).
  </action>
  <verify>
    <automated>cd banking_api_ui && npx react-scripts test --watchAll=false --testPathPattern="AgentUiModeContext" 2>&1 | tail -20</automated>
  </verify>
  <done>All tests pass (existing + new). 6 new test cases covering left-dock/right-dock round-trip and legacy sync.</done>
</task>

</tasks>

<verification>
- `grep "left-dock" banking_api_ui/src/context/AgentUiModeContext.js` — 3+ matches (typedef, readState guard, syncLegacyString)
- Tests pass: `npx react-scripts test --watchAll=false --testPathPattern="AgentUiModeContext"`
- Build still passes: `cd banking_api_ui && npm run build` → exit 0
</verification>

<success_criteria>
AgentUiModeContext.js accepts and persists 'left-dock' and 'right-dock' placement values. Unit tests prove round-trip storage and legacy sync. No regressions in existing tests.
</success_criteria>

<output>
After completion, create `.planning/phases/30-agent-layout-modes-float-left-dock-right-dock-bottom-dock-with-resizable-panels-and-responsive-3-column-layout-adjustment/30-01-SUMMARY.md`
</output>
