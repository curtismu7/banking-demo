---
phase: 36-postman-collections-and-environments-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/BX-Finance-Shared.postman_environment.json
  - docs/AI-IAM-CORE Webinar.postman_collection.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "BX-Finance-Shared.postman_environment.json has 19 variables (16 original + BANKING_API_BASE_URL + MCP_SERVER_URL + BANKING_SENSITIVE_SCOPE)"
    - "No Postman files exist at the repo root"
    - "docs/AI-IAM-CORE Webinar.postman_collection.json exists at that path"
  artifacts:
    - path: "docs/BX-Finance-Shared.postman_environment.json"
      provides: "Shared Postman environment"
      contains: "BANKING_API_BASE_URL"
    - path: "docs/AI-IAM-CORE Webinar.postman_collection.json"
      provides: "Webinar reference collection in docs/"
      contains: "Agent Exchange Token"
  key_links:
    - from: "docs/BX-Finance-Shared.postman_environment.json"
      to: "docs/BX-Finance-BFF-API.postman_collection.json"
      via: "BANKING_API_BASE_URL variable consumed by new BFF collection (plan 36-03)"
      pattern: "BANKING_API_BASE_URL"
    - from: "docs/BX-Finance-Shared.postman_environment.json"
      to: "docs/BX-Finance-MCP-Tools.postman_collection.json"
      via: "MCP_SERVER_URL variable consumed by new MCP collection (plan 36-03)"
      pattern: "MCP_SERVER_URL"
---

<objective>
Organize repo-root Postman stray files into docs/ and add 3 new environment variables
to the shared Postman environment. This creates the foundation that the new collections
in plans 36-02 and 36-03 build on.

Purpose: No Postman files at root; shared env has the BFF and MCP URL variables needed by new collections.
Output: Updated BX-Finance-Shared.postman_environment.json (19 vars), AI-IAM-CORE Webinar moved to docs/, root duplicate deleted.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-postman-collections-and-environments-audit-update-all-collections-and-environments-for-any-missing-or-changed-api-routes-auth-flows-and-mcp-endpoints/36-CONTEXT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move stray root Postman files into docs/</name>
  <files>docs/AI-IAM-CORE Webinar.postman_collection.json</files>
  <action>
    1. Run `git mv "AI-IAM-CORE Webinar.postman_collection.json" "docs/AI-IAM-CORE Webinar.postman_collection.json"` to relocate the Webinar collection into docs/.

    2. For the root-level duplicate: compare `PingOne Authentication v4 - MFA included.postman_collection.json` (root) against `docs/PingOne Authentication v4 - MFA included.postman_collection.json` using a JSON diff or `diff` — if they are identical, run `git rm "PingOne Authentication v4 - MFA included.postman_collection.json"` to delete the root copy. If they differ, merge/keep only the `docs/` version (the docs/ one is authoritative) and then `git rm` the root copy.

    Do NOT use `cp` or `mv` without `git` — this must be a git-tracked rename/delete.
  </action>
  <verify>
    <automated>test ! -f "AI-IAM-CORE Webinar.postman_collection.json" &amp;&amp; test ! -f "PingOne Authentication v4 - MFA included.postman_collection.json" &amp;&amp; test -f "docs/AI-IAM-CORE Webinar.postman_collection.json" &amp;&amp; echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - No Postman files remain at repo root
    - docs/AI-IAM-CORE Webinar.postman_collection.json exists and is a valid JSON Postman collection
    - Root duplicate of PingOne Auth v4 is deleted via git rm
  </done>
</task>

<task type="auto">
  <name>Task 2: Add 3 new variables to BX-Finance-Shared.postman_environment.json</name>
  <files>docs/BX-Finance-Shared.postman_environment.json</files>
  <action>
    Read docs/BX-Finance-Shared.postman_environment.json. It has 16 existing variables in its `values` array. Append the following 3 entries to the end of the `values` array (use the same object shape as existing entries):

    ```json
    {
      "key": "BANKING_API_BASE_URL",
      "value": "http://localhost:3001",
      "type": "default",
      "enabled": true
    },
    {
      "key": "MCP_SERVER_URL",
      "value": "http://localhost:8080",
      "type": "default",
      "enabled": true
    },
    {
      "key": "BANKING_SENSITIVE_SCOPE",
      "value": "banking:sensitive:read",
      "type": "default",
      "enabled": true
    }
    ```

    Do NOT change the `name`, `_postman_id`, `_exporter_id`, or any existing variable entries.
    The resulting `values` array must have exactly 19 entries.
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('docs/BX-Finance-Shared.postman_environment.json')); keys=[v['key'] for v in d['values']]; assert 'BANKING_API_BASE_URL' in keys and 'MCP_SERVER_URL' in keys and 'BANKING_SENSITIVE_SCOPE' in keys and len(d['values'])==19, f'FAIL: {len(d[\"values\"])} vars, keys={keys}'; print('PASS:', len(d['values']), 'vars')"</automated>
  </verify>
  <done>
    - BX-Finance-Shared.postman_environment.json has exactly 19 variables
    - BANKING_API_BASE_URL = http://localhost:3001 (default, not secret)
    - MCP_SERVER_URL = http://localhost:8080 (default, not secret)
    - BANKING_SENSITIVE_SCOPE = banking:sensitive:read (default, not secret)
    - All 16 original variables are unchanged
  </done>
</task>

</tasks>

<verification>
1. `ls *.postman*` at repo root returns nothing
2. `python3 -c "import json; d=json.load(open('docs/BX-Finance-Shared.postman_environment.json')); print(len(d['values']), [v['key'] for v in d['values']][-3:])"` → 19, ["BANKING_API_BASE_URL", "MCP_SERVER_URL", "BANKING_SENSITIVE_SCOPE"]
3. `test -f "docs/AI-IAM-CORE Webinar.postman_collection.json"` → exits 0
</verification>

<success_criteria>
- docs/ contains all Postman files (none at root)
- Shared environment has 19 variables including the 3 new BFF/MCP/sensitive-scope ones
- All changes are git-tracked (moves via git mv, deletes via git rm)
</success_criteria>

<output>
After completion, create `.planning/phases/36-postman-collections-and-environments-audit-update-all-collections-and-environments-for-any-missing-or-changed-api-routes-auth-flows-and-mcp-endpoints/36-01-SUMMARY.md`
</output>
