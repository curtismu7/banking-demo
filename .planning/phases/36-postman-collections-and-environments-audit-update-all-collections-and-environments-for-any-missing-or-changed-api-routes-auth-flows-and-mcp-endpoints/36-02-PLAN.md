---
phase: 36-postman-collections-and-environments-audit
plan: 02
type: execute
wave: 2
depends_on:
  - 36-01
files_modified:
  - docs/BX-Finance-1-Exchange-Step-by-Step.postman_collection.json
  - "docs/BX Finance — 1-Exchange Delegated Chain — pi.flow.postman_collection.json"
  - "docs/BX Finance — 2-Exchange Delegated Chain — pi.flow.postman_collection.json"
  - docs/BX-Finance-Advanced-Utilities.postman_collection.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "2-exchange collection has no hardcoded pingdemo.com URLs — all audience params use {{VARIABLE}} references"
    - "All 4 existing BX Finance collections load without schema errors"
    - "BX-Finance-Advanced-Utilities has ≥5 requests (was 2, adding ≥3 new BFF utility calls)"
    - "The Webinar collection todo is resolved (study complete, corrections applied)"
  artifacts:
    - path: "docs/BX Finance — 2-Exchange Delegated Chain — pi.flow.postman_collection.json"
      provides: "Corrected 2-exchange token chain"
      contains: "MCP_RESOURCE_URI"
    - path: "docs/BX-Finance-Advanced-Utilities.postman_collection.json"
      provides: "BFF utility requests"
      contains: "BANKING_API_BASE_URL"
  key_links:
    - from: "docs/BX Finance — 2-Exchange Delegated Chain — pi.flow.postman_collection.json"
      to: "banking_api_server/services/agentMcpTokenService.js"
      via: "Audience values in token exchange steps match BFF configuration (MCP_RESOURCE_URI, ENDUSER_AUDIENCE)"
      pattern: "MCP_RESOURCE_URI"
    - from: "docs/BX-Finance-Advanced-Utilities.postman_collection.json"
      to: "docs/BX-Finance-Shared.postman_environment.json"
      via: "New requests use {{BANKING_API_BASE_URL}} from shared env (added in 36-01)"
      pattern: "BANKING_API_BASE_URL"
---

<objective>
Full staleness audit of all 4 existing BX Finance Postman collections, plus two focused
corrections: replace hardcoded pingdemo.com audience URLs in the 2-exchange collection with
proper {{VARIABLE}} references, and add new BFF utility requests to Advanced-Utilities.

Purpose: Existing collections are accurate and up-to-date; 2-exchange flow reflects the BFF implementation; Advanced-Utilities covers new BFF utility endpoints.
Output: 4 updated collection files with corrected request bodies, accurate variable usage, and expanded Advanced-Utilities coverage.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-postman-collections-and-environments-audit-update-all-collections-and-environments-for-any-missing-or-changed-api-routes-auth-flows-and-mcp-endpoints/36-CONTEXT.md
@.planning/STATE.md

<interfaces>
<!-- Key files for this plan -->

From docs/BX-Finance-Shared.postman_environment.json (after plan 36-01):
Variables: PINGONE_ENVIRONMENT_ID, PINGONE_BASE_URL, PINGONE_REGION,
  PINGONE_CORE_USER_CLIENT_ID, PINGONE_CORE_USER_CLIENT_SECRET, PINGONE_CORE_USER_REDIRECT_URI,
  PINGONE_CORE_CLIENT_ID, PINGONE_CORE_CLIENT_SECRET,
  MCP_CLIENT_ID, MCP_CLIENT_SECRET,
  TEST_USERNAME, TEST_PASSWORD,
  ENDUSER_AUDIENCE, MCP_RESOURCE_URI, PINGONE_API_AUDIENCE, PAZ_DECISION_ENDPOINT_ID,
  BANKING_API_BASE_URL, MCP_SERVER_URL, BANKING_SENSITIVE_SCOPE

From banking_api_server/services/agentMcpTokenService.js:
  - mcp_resource_uri (= {{MCP_RESOURCE_URI}}) is the audience for RFC 8693 exchange
  - AGENT_OAUTH_CLIENT_ID = the AI agent/BFF actor client (maps to {{PINGONE_CORE_CLIENT_ID}})
  - MCP_CLIENT_ID / MCP_CLIENT_SECRET = MCP exchanger client (maps to {{MCP_CLIENT_ID}})
  - ENDUSER_AUDIENCE = audience for the user's initial authorization code flow (maps to {{ENDUSER_AUDIENCE}})

From banking_api_server/routes/mcpAudit.js:
  GET / — accepts query params: agentId, operation, eventType, outcome, limit
  Hosted at /api/mcp/audit (via server.js app.use)

From banking_api_server/routes/mcpExchangeMode.js:
  GET /exchange-mode — returns current exchange mode
  POST /exchange-mode — body: { mode: "1-exchange" | "2-exchange" }
  Hosted at /api/mcp (via server.js app.use('/api/mcp', mcpExchangeMode))
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full staleness audit of all 4 existing BX Finance collections</name>
  <files>
    docs/BX-Finance-1-Exchange-Step-by-Step.postman_collection.json,
    docs/BX Finance — 1-Exchange Delegated Chain — pi.flow.postman_collection.json,
    docs/BX Finance — 2-Exchange Delegated Chain — pi.flow.postman_collection.json,
    docs/BX-Finance-Advanced-Utilities.postman_collection.json
  </files>
  <action>
    Read all 4 BX Finance collection files. Audit each request for:

    1. **URL staleness** — Any URL containing hardcoded values (non-{{VARIABLE}}), especially `pingdemo.com` hostnames. Flag and replace with the correct `{{VARIABLE_NAME}}` from the shared environment.

    2. **Variable naming** — Any lowercase variable references (e.g., `{{envID}}`, `{{authCode}}`, `{{flowID}}`). These are Webinar-era names — replace with UPPERCASE equivalents from the shared environment context above:
       - `{{envID}}` → `{{PINGONE_ENVIRONMENT_ID}}`
       - `{{authCode}}` or `{{auth_code}}` → `{{auth_code}}` (collection-scoped variable, OK to leave)
       - `{{flowID}}` → `{{flow_id}}` or `{{flowID}}` — leave collection-scoped vars as-is

    3. **Credential exposure** — Any hardcoded client_id/client_secret VALUES (not variable refs). Replace with the appropriate `{{VARIABLE}}`. Per Phase 20 decisions, hardcoded credentials were already removed — verify they're still gone.

    4. **Auth type accuracy** — Check `auth.type` on each request. PingOne `/as/token` calls that use `client_secret_post` should have `auth.type: "noauth"` (credentials in body). Calls using `client_secret_basic` should have `auth.type: "basic"` with `{{VARIABLE}}` username/password.

    5. **Description accuracy** — If any request has a description that refers to old naming, update it.

    Apply fixes. Do NOT change request structure, only fix stale values.
  </action>
  <verify>
    <automated>python3 -c "
import json, glob, sys
errors = []
for f in ['docs/BX-Finance-1-Exchange-Step-by-Step.postman_collection.json',
          'docs/BX Finance \u2014 1-Exchange Delegated Chain \u2014 pi.flow.postman_collection.json',
          'docs/BX Finance \u2014 2-Exchange Delegated Chain \u2014 pi.flow.postman_collection.json',
          'docs/BX-Finance-Advanced-Utilities.postman_collection.json']:
    try:
        d = json.load(open(f))
        text = json.dumps(d)
        if 'pingdemo.com' in text:
            errors.append(f + ': hardcoded pingdemo.com found')
        if 'lIW-QkCx2' in text or '7bb93320-3c96' in text:
            errors.append(f + ': hardcoded credentials found')
    except Exception as e:
        errors.append(f + ': JSON parse error: ' + str(e))
if errors:
    print('FAIL:', errors); sys.exit(1)
print('PASS: all collections clean')
"</automated>
  </verify>
  <done>
    - All 4 collections are valid JSON
    - No hardcoded pingdemo.com URLs remain
    - No hardcoded client credentials remain
    - All identified stale values replaced with {{VARIABLE}} references
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct 2-exchange audience params + expand Advanced-Utilities with new BFF requests</name>
  <files>
    docs/BX Finance — 2-Exchange Delegated Chain — pi.flow.postman_collection.json,
    docs/BX-Finance-Advanced-Utilities.postman_collection.json
  </files>
  <action>
    **Part A — 2-exchange audience correction (per D-06):**

    Read `banking_api_server/services/agentMcpTokenService.js` to understand the BFF's actual RFC 8693 audience values — specifically what `mcp_resource_uri` maps to and how `ENDUSER_AUDIENCE` is used.

    In `docs/BX Finance — 2-Exchange Delegated Chain — pi.flow.postman_collection.json`, find and fix these hardcoded audience values in the `urlencoded` body params:

    - Step 5a (AI Agent Actor Token CC): `audience=https://agent-gateway.pingdemo.com`
      → Replace with `audience={{ENDUSER_AUDIENCE}}`
      (The agent client credentials call should target the user-facing resource — ENDUSER_AUDIENCE is the correct audience)

    - Step 5b (Exchange #1): `audience=https://mcp-server.pingdemo.com`
      → Replace with `audience={{MCP_RESOURCE_URI}}`
      (The RFC 8693 exchange requests a token scoped to the MCP resource — MCP_RESOURCE_URI)

    - Step 6a (MCP Actor Token CC): `audience=https://mcp-gateway.pingdemo.com`
      → Replace with `audience={{MCP_RESOURCE_URI}}`
      (MCP exchanger client credentials also target the MCP resource URI)

    - Step 6b (Exchange #2): `audience=https://resource-server.pingdemo.com`
      → Replace with `audience={{MCP_RESOURCE_URI}}`
      (Second exchange also targets the MCP resource)

    If the BFF code reveals different mappings than the above after reading agentMcpTokenService.js, use the BFF's actual values instead. Document any deviations in the SUMMARY.md.

    **Part B — Expand BX-Finance-Advanced-Utilities.postman_collection.json (per D-03):**

    Add the following 3 new requests to the `item` array. Follow the same request object structure as existing items. Use Collection v2.1 format with `urlencoded` body format where applicable:

    **New request 1 — BFF MCP Audit**
    ```
    name: "BFF — GET /api/mcp/audit (tool-call events)"
    method: GET
    url: {{BANKING_API_BASE_URL}}/api/mcp/audit?limit=20
    auth: noauth
    description: "Returns recent MCP tool-call audit events. Optional query params: agentId, operation, eventType (tool_call|auth_challenge|consent_request|session), outcome (success|failure|challenge), limit."
    ```

    **New request 2 — Exchange Mode GET**
    ```
    name: "BFF — GET /api/mcp/exchange-mode"
    method: GET
    url: {{BANKING_API_BASE_URL}}/api/mcp/exchange-mode
    auth: noauth
    description: "Returns current token exchange mode (1-exchange or 2-exchange)."
    ```

    **New request 3 — Exchange Mode SET**
    ```
    name: "BFF — POST /api/mcp/exchange-mode (set mode)"
    method: POST
    url: {{BANKING_API_BASE_URL}}/api/mcp/exchange-mode
    auth: noauth
    body mode: raw (JSON)
    body: {"mode": "1-exchange"}
    description: "Sets token exchange mode. mode: '1-exchange' | '2-exchange'."
    ```

    Do NOT modify the existing 2 Advanced-Utilities requests (PAZ policy decision + token revocation).
  </action>
  <verify>
    <automated>python3 -c "
import json, sys
# Check 2-exchange: no pingdemo.com
bx2 = json.load(open('docs/BX Finance \u2014 2-Exchange Delegated Chain \u2014 pi.flow.postman_collection.json'))
if 'pingdemo.com' in json.dumps(bx2):
    print('FAIL: pingdemo.com still in 2-exchange collection'); sys.exit(1)
# Check Advanced-Utilities: has 5 requests
def count_reqs(items):
    n = 0
    for i in items:
        if 'request' in i: n += 1
        if 'item' in i: n += count_reqs(i['item'])
    return n
utils = json.load(open('docs/BX-Finance-Advanced-Utilities.postman_collection.json'))
n = count_reqs(utils.get('item', []))
if n < 5:
    print(f'FAIL: Advanced-Utilities has {n} requests, need >=5'); sys.exit(1)
if 'BANKING_API_BASE_URL' not in json.dumps(utils):
    print('FAIL: BANKING_API_BASE_URL not in Advanced-Utilities'); sys.exit(1)
print(f'PASS: 2-exchange clean, Advanced-Utilities has {n} requests')
"</automated>
  </verify>
  <done>
    - 2-exchange collection has 0 hardcoded pingdemo.com audience values
    - All audience params use {{MCP_RESOURCE_URI}} or {{ENDUSER_AUDIENCE}} as appropriate
    - BX-Finance-Advanced-Utilities has ≥5 requests (original 2 + 3 new BFF utilities)
    - New requests use {{BANKING_API_BASE_URL}} from shared env
  </done>
</task>

</tasks>

<verification>
1. No `pingdemo.com` in any BX Finance collection: `grep -r "pingdemo.com" docs/BX-Finance*.json docs/BX\ Finance*.json` → no output
2. Advanced-Utilities request count ≥ 5
3. `python3 -c "import json; d=json.load(open('docs/BX Finance \u2014 2-Exchange Delegated Chain \u2014 pi.flow.postman_collection.json')); print('MCP_RESOURCE_URI' in json.dumps(d))"` → True
</verification>

<success_criteria>
- All 4 existing BX Finance collections load cleanly in Postman (valid JSON, no hardcoded credentials or pingdemo.com URLs)
- 2-exchange collection audience params use {{VARIABLE}} references matching BFF configuration
- Advanced-Utilities has new BFF audit and exchange-mode requests using {{BANKING_API_BASE_URL}}
</success_criteria>

<output>
After completion, create `.planning/phases/36-postman-collections-and-environments-audit-update-all-collections-and-environments-for-any-missing-or-changed-api-routes-auth-flows-and-mcp-endpoints/36-02-SUMMARY.md`
</output>
