---
phase: 36-postman-collections-and-environments-audit
plan: 03
type: execute
wave: 2
depends_on:
  - 36-01
files_modified:
  - docs/BX-Finance-MCP-Tools.postman_collection.json
  - docs/BX-Finance-BFF-API.postman_collection.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "docs/BX-Finance-MCP-Tools.postman_collection.json exists with ≥2 requests covering MCP-server-direct endpoints"
    - "docs/BX-Finance-BFF-API.postman_collection.json exists with ≥5 requests covering new BFF routes"
    - "Both new collections reference only {{VARIABLE}} names from the shared environment"
    - "BFF-API collection covers /api/mcp/audit, /api/mcp/inspector/invoke, /api/rfc9728, and the sensitive scope scenario"
  artifacts:
    - path: "docs/BX-Finance-MCP-Tools.postman_collection.json"
      provides: "MCP server direct HTTP requests"
      contains: "well-known"
    - path: "docs/BX-Finance-BFF-API.postman_collection.json"
      provides: "Authenticated BFF API requests"
      contains: "mcp/audit"
  key_links:
    - from: "docs/BX-Finance-MCP-Tools.postman_collection.json"
      to: "docs/BX-Finance-Shared.postman_environment.json"
      via: "MCP_SERVER_URL variable (added in plan 36-01)"
      pattern: "MCP_SERVER_URL"
    - from: "docs/BX-Finance-BFF-API.postman_collection.json"
      to: "docs/BX-Finance-Shared.postman_environment.json"
      via: "BANKING_API_BASE_URL variable (added in plan 36-01)"
      pattern: "BANKING_API_BASE_URL"
---

<objective>
Create two new Postman collections covering MCP-server-direct endpoints and new BFF API
routes added in phases 29–34 that currently have zero Postman coverage.

Purpose: Developers and testers can exercise the MCP server and new BFF endpoints without building requests from scratch.
Output: docs/BX-Finance-MCP-Tools.postman_collection.json (MCP HTTP endpoints) and docs/BX-Finance-BFF-API.postman_collection.json (new authenticated BFF routes).
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-postman-collections-and-environments-audit-update-all-collections-and-environments-for-any-missing-or-changed-api-routes-auth-flows-and-mcp-endpoints/36-CONTEXT.md
@.planning/STATE.md

<interfaces>
<!-- Key BFF routes to cover in BFF-API collection -->

From banking_api_server/routes/mcpAudit.js (mounted at /api/mcp/audit):
  GET / — query params: agentId, operation, eventType (tool_call|auth_challenge|consent_request|session),
            outcome (success|failure|challenge), limit (default 50, max 200)
  Returns: { events: AuditEvent[], total: number, source: string }

From banking_api_server/routes/mcpExchangeMode.js (mounted at /api/mcp):
  GET /exchange-mode — returns { mode: "1-exchange" | "2-exchange" }
  POST /exchange-mode — body: { mode: "1-exchange" | "2-exchange" } — no auth required

From banking_api_server/routes/mcpInspector.js (mounted at /api/mcp/inspector):
  POST /invoke — body: { toolName: string, args: object, token?: string }
  Returns tool execution result. Requires MCP server to be reachable.

From banking_api_server/server.js — GET /api/rfc9728:
  Protected resource metadata (RFC 9728)
  No auth required. Returns JSON-LD metadata about the BFF as a protected resource.

Phase 29 — get_sensitive_account_details:
  This is an MCP tool call. Use POST /api/mcp/tool (if exists) or note as
  "invoke via MCP Inspector: toolName=get_sensitive_account_details, requires banking:sensitive:read scope"

<!-- Key MCP server HTTP endpoints to cover in MCP-Tools collection -->

From banking_mcp_server/src/server/HttpMCPTransport.ts:
  GET /.well-known/mcp-server — public discovery manifest
    Returns: { serverInfo, capabilities, tools[], oauth_metadata_url }
    No auth required. Use {{MCP_SERVER_URL}}/.well-known/mcp-server

  GET /audit — recent tool-call audit events
    Query params: agentId, operation, outcome, eventType, limit
    Returns: { events: AuditEvent[], total: number }
    Use {{MCP_SERVER_URL}}/audit

Shared environment variables available (after plan 36-01):
  {{BANKING_API_BASE_URL}} = http://localhost:3001
  {{MCP_SERVER_URL}} = http://localhost:8080
  {{BANKING_SENSITIVE_SCOPE}} = banking:sensitive:read
  {{MCP_CLIENT_ID}}, {{MCP_CLIENT_SECRET}} — for any auth needed
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/BX-Finance-MCP-Tools.postman_collection.json</name>
  <files>docs/BX-Finance-MCP-Tools.postman_collection.json</files>
  <action>
    Create a new Postman Collection v2.1 JSON file at `docs/BX-Finance-MCP-Tools.postman_collection.json`.

    Use this structure outline:
    ```json
    {
      "info": {
        "_postman_id": "<generate UUID v4>",
        "name": "BX Finance — MCP Server Tools",
        "description": "Direct HTTP calls to the BX Finance MCP server (banking_mcp_server). Use MCP_SERVER_URL in the shared BX-Finance environment.",
        "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
      },
      "variable": [],
      "item": [ ... ]
    }
    ```

    Include the following requests in the `item` array:

    **Request 1 — MCP Server Discovery**
    ```
    name: "1. MCP Server Discovery (/.well-known/mcp-server)"
    method: GET
    url: {{MCP_SERVER_URL}}/.well-known/mcp-server
    auth: noauth
    description: "Public MCP server discovery manifest. Returns serverInfo, capabilities, tools list, and oauth_metadata_url. No authentication required. Spec: MCP Protocol / RFC /.well-known discovery."
    ```

    **Request 2 — MCP Audit Trail**
    ```
    name: "2. MCP Audit Trail (GET /audit)"
    method: GET
    url: {{MCP_SERVER_URL}}/audit?limit=20
    auth: noauth
    description: "Returns recent MCP tool-call audit events from Redis (Upstash). Optional query params: agentId, operation, outcome (success|failure|challenge), eventType (tool_call|auth_challenge|consent_request|session), limit (max 200). Added in Phase 34."
    ```

    Add these as the 2 items. Use the same request object schema as in the existing BX Finance collections (url as object with raw+host+path, or as string — follow the pattern in docs/BX-Finance-Advanced-Utilities.postman_collection.json for reference).
  </action>
  <verify>
    <automated>python3 -c "
import json, sys
d = json.load(open('docs/BX-Finance-MCP-Tools.postman_collection.json'))
assert d['info']['schema'].startswith('https://schema.getpostman.com'), 'FAIL: wrong schema'
def count(items):
    n = 0
    for i in items:
        if 'request' in i: n += 1
        if 'item' in i: n += count(i['item'])
    return n
n = count(d.get('item', []))
assert n >= 2, f'FAIL: only {n} requests'
text = json.dumps(d)
assert 'MCP_SERVER_URL' in text, 'FAIL: MCP_SERVER_URL not used'
assert 'well-known' in text, 'FAIL: well-known request missing'
print(f'PASS: {n} requests, MCP_SERVER_URL used')
"</automated>
  </verify>
  <done>
    - docs/BX-Finance-MCP-Tools.postman_collection.json is valid Postman Collection v2.1
    - Contains ≥2 requests: discovery and audit trail
    - All URLs use {{MCP_SERVER_URL}} — no hardcoded ports or hostnames
  </done>
</task>

<task type="auto">
  <name>Task 2: Create docs/BX-Finance-BFF-API.postman_collection.json</name>
  <files>docs/BX-Finance-BFF-API.postman_collection.json</files>
  <action>
    Create a new Postman Collection v2.1 JSON file at `docs/BX-Finance-BFF-API.postman_collection.json`.

    Collection info:
    ```json
    {
      "info": {
        "_postman_id": "<generate UUID v4>",
        "name": "BX Finance — BFF API",
        "description": "Direct calls to the BX Finance BFF (banking_api_server). Covers new endpoints added in phases 29–34. Use BANKING_API_BASE_URL in the shared BX-Finance environment. Most endpoints require an active session cookie — run a login flow first.",
        "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
      },
      "item": [ ... ]
    }
    ```

    Include these 5 requests:

    **Request 1 — MCP Audit (BFF proxy)**
    ```
    name: "1. GET /api/mcp/audit (BFF audit proxy)"
    method: GET
    url: {{BANKING_API_BASE_URL}}/api/mcp/audit?limit=20
    auth: noauth
    description: "BFF proxy to MCP server /audit. Returns recent MCP tool-call audit events. Query params: agentId, operation, eventType (tool_call|auth_challenge|consent_request|session), outcome (success|failure|challenge), limit. Returns empty events array when MCP server is unreachable. Added Phase 34."
    ```

    **Request 2 — Get Exchange Mode**
    ```
    name: "2. GET /api/mcp/exchange-mode"
    method: GET
    url: {{BANKING_API_BASE_URL}}/api/mcp/exchange-mode
    auth: noauth
    description: "Returns current token exchange mode. Response: { mode: '1-exchange' | '2-exchange' }."
    ```

    **Request 3 — Set Exchange Mode**
    ```
    name: "3. POST /api/mcp/exchange-mode (set mode)"
    method: POST
    url: {{BANKING_API_BASE_URL}}/api/mcp/exchange-mode
    auth: noauth
    body: raw JSON {"mode": "1-exchange"}
    description: "Sets token exchange mode. Accepted values: '1-exchange' (single RFC 8693 exchange) or '2-exchange' (chained delegation). No session required."
    ```

    **Request 4 — RFC 9728 Protected Resource Metadata**
    ```
    name: "4. GET /api/rfc9728 (protected resource metadata)"
    method: GET
    url: {{BANKING_API_BASE_URL}}/api/rfc9728
    auth: noauth
    description: "RFC 9728 protected resource metadata for the BFF. Returns JSON-LD metadata describing the BFF as a protected resource including resource_indicators, scopes_supported, and authorization_servers. No auth required."
    ```

    **Request 5 — MCP Inspector Invoke Tool**
    ```
    name: "5. POST /api/mcp/inspector/invoke (invoke MCP tool)"
    method: POST
    url: {{BANKING_API_BASE_URL}}/api/mcp/inspector/invoke
    auth: noauth
    body: raw JSON {"toolName": "get_account_balance", "args": {"accountType": "checking"}}
    description: "Invokes an MCP tool via the BFF inspector. Requires MCP server running at MCP_SERVER_URL. For the sensitive data tool, use toolName='get_sensitive_account_details' and ensure the bearer token has banking:sensitive:read scope ({{BANKING_SENSITIVE_SCOPE}}). Session cookie required."
    ```

    Follow Postman Collection v2.1 request object format. For raw JSON bodies, use `body.mode = "raw"` and `body.options.raw.language = "json"`.
  </action>
  <verify>
    <automated>python3 -c "
import json, sys
d = json.load(open('docs/BX-Finance-BFF-API.postman_collection.json'))
assert d['info']['schema'].startswith('https://schema.getpostman.com'), 'FAIL: wrong schema'
def count(items):
    n = 0
    for i in items:
        if 'request' in i: n += 1
        if 'item' in i: n += count(i['item'])
    return n
n = count(d.get('item', []))
assert n >= 5, f'FAIL: only {n} requests'
text = json.dumps(d)
assert 'BANKING_API_BASE_URL' in text, 'FAIL: BANKING_API_BASE_URL not used'
assert 'mcp/audit' in text, 'FAIL: mcp/audit request missing'
assert 'rfc9728' in text, 'FAIL: rfc9728 request missing'
assert 'inspector' in text, 'FAIL: inspector invoke missing'
print(f'PASS: {n} requests, all key endpoints present')
"</automated>
  </verify>
  <done>
    - docs/BX-Finance-BFF-API.postman_collection.json is valid Postman Collection v2.1
    - Contains ≥5 requests covering: /api/mcp/audit, exchange-mode GET/POST, /api/rfc9728, /api/mcp/inspector/invoke
    - All URLs use {{BANKING_API_BASE_URL}} — no hardcoded hosts
    - Inspector invoke example body references {{BANKING_SENSITIVE_SCOPE}} in description
  </done>
</task>

</tasks>

<verification>
1. `python3 -c "import json; d=json.load(open('docs/BX-Finance-MCP-Tools.postman_collection.json')); print(d['info']['name'])"` → "BX Finance — MCP Server Tools"
2. `python3 -c "import json; d=json.load(open('docs/BX-Finance-BFF-API.postman_collection.json')); print(d['info']['name'])"` → "BX Finance — BFF API"
3. `grep -c "BANKING_API_BASE_URL" docs/BX-Finance-BFF-API.postman_collection.json` → ≥5
4. `grep -c "MCP_SERVER_URL" docs/BX-Finance-MCP-Tools.postman_collection.json` → ≥2
</verification>

<success_criteria>
- Two new valid Postman collections committed to docs/
- MCP-Tools covers /.well-known/mcp-server and /audit (MCP server direct)
- BFF-API covers /api/mcp/audit, exchange-mode, /api/rfc9728, /api/mcp/inspector/invoke
- All variable references resolve from the BX-Finance-Shared environment
- No hardcoded hostnames or credentials in either collection
</success_criteria>

<output>
After completion, create `.planning/phases/36-postman-collections-and-environments-audit-update-all-collections-and-environments-for-any-missing-or-changed-api-routes-auth-flows-and-mcp-endpoints/36-03-SUMMARY.md`
</output>
