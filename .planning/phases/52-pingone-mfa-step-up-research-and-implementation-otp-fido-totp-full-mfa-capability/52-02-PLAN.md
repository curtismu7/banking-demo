---
phase: 52-pingone-mfa-step-up-research-and-implementation-otp-fido-totp-full-mfa-capability
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - banking_api_server/config/runtimeSettings.js
  - banking_api_server/services/mcpLocalTools.js
  - banking_api_server/routes/oauthUser.js
  - banking_api_server/routes/ciba.js
  - banking_api_ui/src/components/SecuritySettings.js
autonomous: true
requirements:
  - MFA-02
  - MFA-03
  - MFA-04
  - MFA-06

must_haves:
  truths:
    - "Default step-up threshold is $0 (all write operations require step-up unless disabled)"
    - "Setting threshold to 0 in Security Settings UI is accepted (no min:1 blocking)"
    - "OTP initiate-otp BFF route returns { email } not { maskedEmail }"
    - "CIBA poll approved → req.session.stepUpVerified = true set before response"
    - "Re-fired agent tool call after CIBA approval passes checkLocalStepUp first time"
  artifacts:
    - path: banking_api_server/config/runtimeSettings.js
      provides: Default threshold 0
      contains: "|| 0"
    - path: banking_api_server/services/mcpLocalTools.js
      provides: Correct threshold gate with nullish coalescing
      contains: "?? 0"
    - path: banking_api_server/routes/ciba.js
      provides: stepUpVerified set on CIBA approval
      contains: "stepUpVerified = true"
    - path: banking_api_server/routes/oauthUser.js
      provides: Full email field in initiate-otp response
      contains: "email:"
  key_links:
    - from: ciba.js poll route approval branch
      to: req.session.stepUpVerified
      via: "req.session.stepUpVerified = true before req.session.save()"
    - from: mcpLocalTools.js checkLocalStepUp
      to: runtimeSettings threshold
      via: "runtimeSettings.get('stepUpAmountThreshold') ?? 0"
---

<objective>
Four targeted BFF fixes in one plan — all independent one-to-five line changes.
Plus SecuritySettings.js min constraint fix to allow threshold=0 in the Admin UI.

Purpose:
1. Make step-up always-on by default (D-02)
2. Fix the || bug that makes threshold=0 ineffective (D-02)
3. Remove OTP email masking — send full address to client (D-06)
4. Wire CIBA approval → stepUpVerified so re-fired tool calls pass the gate (D-03)
5. Allow threshold=0 in SecuritySettings Admin UI (D-02)

D-04 (silent SSO toast) is verified here — App.js already handles sso_silent=1 param,
oauthUser.js already appends it. No code changes needed for D-04.

Output: 5 small targeted edits; no new files
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/52-pingone-mfa-step-up-research-and-implementation-otp-fido-totp-full-mfa-capability/52-CONTEXT.md
@.planning/phases/52-pingone-mfa-step-up-research-and-implementation-otp-fido-totp-full-mfa-capability/52-RESEARCH.md

<interfaces>
<!-- Exact lines to change — read each file before editing -->

runtimeSettings.js line 14:
  BEFORE: stepUpAmountThreshold: parseFloat(process.env.STEP_UP_AMOUNT_THRESHOLD) || 250,
  AFTER:  stepUpAmountThreshold: parseFloat(process.env.STEP_UP_AMOUNT_THRESHOLD) || 0,

mcpLocalTools.js line ~50 (inside checkLocalStepUp):
  BEFORE: const threshold = runtimeSettings.get('stepUpAmountThreshold') || 250;
  AFTER:  const threshold = runtimeSettings.get('stepUpAmountThreshold') ?? 0;

oauthUser.js initiate-otp route (~line 843):
  BEFORE: maskedEmail = user.email.replace(/(.{2}).+(@.+)/, '$1…$2')
          …return res.json({ …, maskedEmail })
  AFTER:  remove masking line entirely
          return res.json({ …, email: user.email })
  (rename the field — client in 52-03 will use data.email not data.maskedEmail)

ciba.js poll route approval branch (~line 192, inside `req.session.oauthTokens = { ... }` block):
  ADD BEFORE req.session.save():
    req.session.stepUpVerified = true;

SecuritySettings.js FIELD_META.stepUpAmountThreshold:
  BEFORE: min: 1,
  AFTER:  min: 0,
  Also update description:
  BEFORE: 'Transfers and withdrawals at or above this amount require MFA re-authentication.'
  AFTER:  'Transfers and withdrawals at or above this amount require MFA re-authentication. Set to 0 to require step-up on ALL transactions regardless of amount.'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BFF four-line fix bundle (threshold default, || bug, email unmask, CIBA stepUpVerified)</name>
  <files>
    banking_api_server/config/runtimeSettings.js,
    banking_api_server/services/mcpLocalTools.js,
    banking_api_server/routes/oauthUser.js,
    banking_api_server/routes/ciba.js
  </files>
  <action>
Read each file before editing. Make ONLY these targeted changes:

**runtimeSettings.js** — change line 14 only:
  `|| 250` → `|| 0`
  (NaN || 0 = 0 when env var not set; preserves env var override)

**mcpLocalTools.js** — in `checkLocalStepUp` function, the threshold line:
  `|| 250` → `?? 0`
  (nullish coalescing: 0 ?? 0 = 0; prevents 0 becoming 250 via JS falsy coercion)

**oauthUser.js** — in the `initiate-otp` POST handler (~line 843):
  - Remove the masking line: `const maskedEmail = user.email.replace(...)`
  - Change the JSON response field from `maskedEmail: maskedEmail` to `email: user.email`
  - Do NOT change any other part of this handler

**ciba.js** — in the `GET /poll/:authReqId` handler, approval branch:
  Find the block where `req.session.oauthTokens = { ... }` is set (around line 187).
  Add `req.session.stepUpVerified = true;` immediately BEFORE the `req.session.save(...)` call.
  Do NOT change anything else in this route.

IMPORTANT: Each change is 1–3 lines. Do not refactor surrounding code.
  </action>
  <verify>
    <automated>
grep -n "|| 0" /Users/cmuir/P1Import-apps/Banking/banking_api_server/config/runtimeSettings.js | grep stepUpAmountThreshold && \
grep -n "?? 0" /Users/cmuir/P1Import-apps/Banking/banking_api_server/services/mcpLocalTools.js | grep threshold && \
grep -n "email: user.email" /Users/cmuir/P1Import-apps/Banking/banking_api_server/routes/oauthUser.js && \
grep -n "stepUpVerified = true" /Users/cmuir/P1Import-apps/Banking/banking_api_server/routes/ciba.js && \
echo "ALL CHECKS PASS"
    </automated>
  </verify>
  <done>
- runtimeSettings.js: stepUpAmountThreshold default is 0
- mcpLocalTools.js: threshold uses ?? 0 (nullish coalescing)
- oauthUser.js: initiate-otp returns { email } not { maskedEmail }
- ciba.js: poll approval sets stepUpVerified = true before session save
  </done>
</task>

<task type="auto">
  <name>Task 2: SecuritySettings.js — allow threshold=0 in Admin UI</name>
  <files>banking_api_ui/src/components/SecuritySettings.js</files>
  <action>
In `SecuritySettings.js`, find the `FIELD_META` constant object near the top of the file.
Find the `stepUpAmountThreshold` entry.

Make exactly these two changes:
1. `min: 1,` → `min: 0,`
2. Update the description string to:
   `'Transfers and withdrawals at or above this amount require MFA re-authentication. Set to 0 to require step-up on ALL transactions regardless of amount.'`

Do not change any other fields in FIELD_META or any other code.

Also verify D-04 (silent SSO toast) — no code changes, just confirm:
- Open `banking_api_ui/src/App.js` lines 300–316 and confirm `sso_silent=1` param is handled and `notifyInfo` is called.
- Note in SUMMARY as "D-04 verified: pre-existing implementation confirmed, no changes needed."
  </action>
  <verify>
    <automated>
grep -n "min: 0" /Users/cmuir/P1Import-apps/Banking/banking_api_ui/src/components/SecuritySettings.js | grep stepUp -A2 || \
grep -n "min: 0" /Users/cmuir/P1Import-apps/Banking/banking_api_ui/src/components/SecuritySettings.js && \
cd /Users/cmuir/P1Import-apps/Banking/banking_api_ui && npm run build 2>&1 | tail -3
    </automated>
  </verify>
  <done>
- SecuritySettings.js FIELD_META stepUpAmountThreshold.min is 0
- Admin UI no longer blocks entering 0 as threshold value
- npm run build exits 0
  </done>
</task>

</tasks>

<verification>
1. `grep "|| 0" banking_api_server/config/runtimeSettings.js` — shows line 14
2. `grep "?? 0" banking_api_server/services/mcpLocalTools.js` — shows threshold line
3. `grep "email: user.email" banking_api_server/routes/oauthUser.js` — shows initiate-otp response
4. `grep "stepUpVerified = true" banking_api_server/routes/ciba.js` — shows poll approval branch
5. `grep "min: 0" banking_api_ui/src/components/SecuritySettings.js` — shows stepUpAmountThreshold entry
6. `cd banking_api_ui && npm run build` — exits 0
</verification>

<success_criteria>
- Default threshold = 0 (all write ops require step-up by default)
- threshold=0 is not negated by || fallback in mcpLocalTools.js
- initiate-otp BFF response field is `email` (full, not masked)
- CIBA poll approved branch sets stepUpVerified=true before session.save()
- Security Settings Admin UI accepts 0 as a valid threshold value
- npm run build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/52-pingone-mfa-step-up-research-and-implementation-otp-fido-totp-full-mfa-capability/52-02-SUMMARY.md`
</output>
