---
phase: 69-standardize-pingone-app-resource-and-scope-naming-across-all
plan: "01"
type: execute
wave: 1
depends_on: []
canonical_phase: 46-standardize-pingone-app-resource-and-scope-naming-across-all-use-cases
files_modified:
  - banking_api_server/config/scopes.js
  - banking_api_server/services/pingoneProvisionService.js
  - docs/PINGONE_APP_CONFIG.md
autonomous: true
requirements: [NAMING-CANONICAL-01, NAMING-CANONICAL-02]

must_haves:
  truths:
    - "config/scopes.js exports CANONICAL_APP_NAMES, CANONICAL_RESOURCE_NAMES, CANONICAL_SCOPES constants"
    - "Admin OIDC app canonical name is 'BX Finance Admin App'"
    - "User OIDC app canonical name is 'BX Finance User App'"
    - "MCP/Agent app canonical name is 'BX Finance Agent App'"
    - "Worker app canonical name is 'BX Finance Worker App'"
    - "Resource server canonical name is 'BX Finance Banking API' with audience 'banking_api_enduser'"
    - "pingoneProvisionService.js uses constants from config/scopes.js (no hardcoded name strings)"
    - "docs/PINGONE_APP_CONFIG.md contains a Canonical Resource Naming table"
  artifacts:
    - path: "banking_api_server/config/scopes.js"
      provides: "CANONICAL_APP_NAMES, CANONICAL_RESOURCE_NAMES, CANONICAL_SCOPES constants"
    - path: "banking_api_server/services/pingoneProvisionService.js"
      provides: "Uses canonical constants for all PingOne resource creation"
    - path: "docs/PINGONE_APP_CONFIG.md"
      provides: "Canonical naming reference table"
---

<objective>
Establish a single source of truth for PingOne application, resource
server, and scope names. Consolidates work planned in Phase 46.

Verify first whether CANONICAL_APP_NAMES already exists in config/scopes.js
(Phase 46 may have executed this). If constants exist and pingoneProvisionService.js
imports them, mark tasks done.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Check current state of config/scopes.js</name>
  <files>banking_api_server/config/scopes.js</files>
  <action>
Check if CANONICAL_APP_NAMES is already exported. If yes, skip Tasks 2-3.
If not, add canonical constants at the end of the file before module.exports:

```javascript
const CANONICAL_APP_NAMES = {
  ADMIN_APP:  'BX Finance Admin App',
  USER_APP:   'BX Finance User App',
  AGENT_APP:  'BX Finance Agent App',
  WORKER_APP: 'BX Finance Worker App',
};

const CANONICAL_RESOURCE_NAMES = {
  BANKING_API: 'BX Finance Banking API',
  AUDIENCE:    'banking_api_enduser',
};

const CANONICAL_SCOPES = [
  'banking:read', 'banking:write', 'banking:admin', 'banking:sensitive', 'ai_agent',
];
```

Export them in module.exports alongside existing exports.
  </action>
  <done>CANONICAL_APP_NAMES, CANONICAL_RESOURCE_NAMES, CANONICAL_SCOPES exported from config/scopes.js.</done>
</task>

<task type="auto">
  <name>Task 2: Update pingoneProvisionService.js to use constants</name>
  <files>banking_api_server/services/pingoneProvisionService.js</files>
  <action>
1. Import constants at top of file:
   `const { CANONICAL_APP_NAMES, CANONICAL_RESOURCE_NAMES } = require('../config/scopes');`

2. Search for hardcoded strings like 'Super Banking', 'Banking Admin App',
   'Banking User App', 'Banking Agent', 'Banking API' — replace with constants.

3. Replace hardcoded audience string with CANONICAL_RESOURCE_NAMES.AUDIENCE.
  </action>
  <done>pingoneProvisionService.js uses canonical name constants — no hardcoded app name strings remain.</done>
</task>

<task type="auto">
  <name>Task 3: Add canonical naming table to docs/PINGONE_APP_CONFIG.md</name>
  <files>docs/PINGONE_APP_CONFIG.md</files>
  <action>
Add or update a "Canonical Resource Naming" section:

```markdown
## Canonical Resource Naming

| Resource Type | Canonical Name | Notes |
|---|---|---|
| Admin OIDC App | BX Finance Admin App | WEB_APP, PKCE, admin scopes |
| User OIDC App | BX Finance User App | WEB_APP, PKCE, user scopes |
| Agent OIDC App | BX Finance Agent App | WEB_APP, PKCE, ai_agent scope |
| Worker App | BX Finance Worker App | WORKER type, client credentials |
| Resource Server | BX Finance Banking API | audience: banking_api_enduser |

Defined in `banking_api_server/config/scopes.js` as `CANONICAL_APP_NAMES`.
```
  </action>
  <done>docs/PINGONE_APP_CONFIG.md contains canonical naming table.</done>
</task>

</tasks>

<verification>
1. `node -e "const s=require('./banking_api_server/config/scopes'); console.log(JSON.stringify(s.CANONICAL_APP_NAMES))"` → prints names object
2. `grep -c "CANONICAL_APP_NAMES" banking_api_server/services/pingoneProvisionService.js` → ≥ 1
3. `grep -c "BX Finance" banking_api_server/services/pingoneProvisionService.js` → name references found
4. docs/PINGONE_APP_CONFIG.md contains "Canonical Resource Naming" section
</verification>

<success_criteria>
- Single constant source for all PingOne resource names
- No hardcoded name strings in pingoneProvisionService.js
- Documentation matches constants
</success_criteria>
