---
phase: 84-review-all-syntax-errors-code-failures-looping-best-practices-for-all-code
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: []

must_haves:
  truths:
    - "Comprehensive inventory of code quality issues exists and is categorized"
    - "Shell scripts are identified and their relationships documented"
    - "Debug console.log statements are cataloged with locations"
    - "Test suite status is verified (all 201 tests passing)"
  artifacts:
    - path: ".planning/phases/84-review-all-syntax-errors-code-failures-looping-best-practices-for-all-code/AUDIT.md"
      provides: "Complete code quality audit with findings, severity, and recommendations"
    - path: ".planning/phases/84-review-all-syntax-errors-code-failures-looping-best-practices-for-all-code/SHELL_SCRIPTS_AUDIT.md"
      provides: "Inventory of all shell scripts with consolidation roadmap"
  key_links:
    - from: "Phase 84 audit"
      to: "Phase 84 Plans 2-4"
      via: "Audit findings inform execution sequence"
      pattern: "AUDIT.md drives planning decisions"
---

<objective>
Audit the entire codebase for code quality issues without making fixes yet. Create an inventory of issues, categorize by severity, and identify what needs fixing.

Purpose: Understand scope before committing to fixes; avoid breaking things by knowing what's working
Output: AUDIT.md with categorized findings + SHELL_SCRIPTS_AUDIT.md with consolidation roadmap
</objective>

<execution_context>
@/Users/cmuir/P1Import-apps/Banking/.planning/phases/84-review-all-syntax-errors-code-failures-looping-best-practices-for-all-code/CONTEXT.md
@/Users/cmuir/P1Import-apps/Banking/REGRESSION_PLAN.md
@/Users/cmuir/P1Import-apps/Banking/CLAUDE.md
</execution_context>

<context>
Phase 84 scope: 613 source files across 3 codebases (UI, API, MCP server)
Test infrastructure: 201 tests (status TBD)
Zero-break requirement: Match REGRESSION_PLAN.md §1 protected areas and do not break any changes in that list

**Key directories:**
- `banking_api_ui/` — React SPA (CRA)
- `banking_api_server/` — Express BFF
- `banking_mcp_server/` — MCP server (TypeScript)
- `tests/` — Jest test suites
- Root shell scripts: `run-bank.sh`, `run-tests.sh`, `stop.sh`, `reset-upstash.sh`, `update-upstash.sh`, `start.sh` (verify which ones exist)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit shell scripts and create consolidation roadmap</name>
  <files>.planning/phases/84-review-all-syntax-errors-code-failures-looping-best-practices-for-all-code/SHELL_SCRIPTS_AUDIT.md</files>
  <action>
    List all shell scripts in the repo root and banking_api_server/ directory:
    ```bash
    find /Users/cmuir/P1Import-apps/Banking -maxdepth 2 -name "*.sh" -type f | grep -v node_modules | sort
    ```
    For each script, document:
    - Script name and current location
    - What it does (purpose, subcommands if any)
    - How it's invoked in CI/deployment
    - What environment variables it needs
    - Dependencies (other scripts, tools, services)
    
    Then create a consolidation roadmap:
    - Proposed structure: Single `run.sh` entry point with subcommands (start, stop, restart, logs, test, status)
    - How each current script maps to subcommands
    - Pre-flight checks needed (dependencies, env vars)
    - Post-start validation steps
    - PID-file management approach
    
    Output: `.planning/phases/84-review-all-syntax-errors-code-failures-looping-best-practices-for-all-code/SHELL_SCRIPTS_AUDIT.md`
    Format: Markdown with script inventory table + consolidation roadmap section
  </action>
  <verify>
    SHELL_SCRIPTS_AUDIT.md exists and contains:
    - Complete list of all shell scripts found (find command results)
    - Inventory table with script name, location, purpose, and dependencies
    - Proposed run.sh structure with all subcommands and pre-flight checks documented
    - Mapping of current scripts to new subcommands
  </verify>
  <done>
    Shell script audit complete. Consolidation roadmap is clear and feasible. All scripts have been inventoried with their purposes and dependencies mapped.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit code quality issues in banking_api_ui/src</name>
  <files>.planning/phases/84-review-all-syntax-errors-code-failures-looping-best-practices-for-all-code/UI_AUDIT.md</files>
  <action>
    Analyze banking_api_ui/src for code quality issues:
    
    1. **Unused imports and dead code**
       ```bash
       cd /Users/cmuir/P1Import-apps/Banking/banking_api_ui && \
       find src -name "*.js" -o -name "*.jsx" -o -name "*.ts" -o -name "*.tsx" | while read f; do
         echo "=== $f ===" 
         grep -E "^import |^export " "$f" | head -20
       done
       ```
    2. **Console logging**
       ```bash
       grep -r "console\." banking_api_ui/src --include="*.js" --include="*.jsx" --include="*.ts" --include="*.tsx" | wc -l
       grep -r "console\." banking_api_ui/src --include="*.js" --include="*.jsx" --include="*.ts" --include="*.tsx" | head -30
       ```
    3. **File count and structure**
       ```bash
       find banking_api_ui/src -type f \( -name "*.js" -o -name "*.jsx" -o -name "*.ts" -o -name "*.tsx" \) | wc -l
       find banking_api_ui/src/components -type f 2>/dev/null | wc -l
       find banking_api_ui/src/hooks -type f 2>/dev/null | wc -l
       ```
    
    Document findings in UI_AUDIT.md:
    - Total files, components, hooks, utils
    - Console.log count and top offenders (files with most logging)
    - Identified dead code or unused imports (sampling)
    - Any obvious TypeScript or ESLint violations (if patterns are visible)
    - Recommendations for cleanup priority
  </action>
  <verify>
    UI_AUDIT.md exists with:
    - File inventory by type (components, hooks, utils, etc.)
    - Console.log summary with count and top 10 affected files
    - Sample of unused imports or dead code patterns found
    - Severity assessment (high/medium/low for each issue type)
  </verify>
  <done>
    UI codebase audited. All quality issues categorized and prioritized. Console logging hotspots identified.
  </done>
</task>

<task type="auto">
  <name>Task 3: Audit code quality issues in banking_api_server/</name>
  <files>.planning/phases/84-review-all-syntax-errors-code-failures-looping-best-practices-for-all-code/API_AUDIT.md</files>
  <action>
    Analyze banking_api_server for code quality issues:
    
    1. **Console logging hotspots**
       ```bash
       grep -r "console\." banking_api_server --include="*.js" --include="*.ts" | wc -l
       grep -r "console\." banking_api_server --include="*.js" --include="*.ts" | cut -d: -f1 | sort | uniq -c | sort -rn | head -20
       ```
    2. **File structure**
       ```bash
       find banking_api_server -maxdepth 2 -type d | grep -v node_modules | sort
       find banking_api_server -name "*.js" -o -name "*.ts" | grep -v node_modules | wc -l
       ```
    3. **Error handling patterns**
       ```bash
       grep -r "catch\|throw\|error" banking_api_server --include="*.js" | wc -l
       grep -r "TODO\|FIXME\|XXX\|HACK" banking_api_server --include="*.js" --include="*.ts" | head -20
       ```
    
    Document findings:
    - Total code files by directory (services, routes, config, middleware, utils, etc.)
    - Console.log count and top 10 affected files
    - TODO/FIXME/HACK comments found and context
    - Identified error handling gaps (catching without logging, throwing without context)
    - Database/session management code review status
    - Authentication/token handling code status (per REGRESSION_PLAN.md §1)
  </action>
  <verify>
    API_AUDIT.md exists with:
    - Code file inventory by directory
    - Console.log summary and hotspots
    - All TODO/FIXME comments listed with line numbers
    - Error handling assessment
    - Security-sensitive areas noted (auth, tokens, sessions)
  </verify>
  <done>
    API codebase audited. Logging issues and code quality gaps documented. No protected areas (REGRESSION_PLAN.md §1) will be touched without review.
  </done>
</task>

<task type="auto">
  <name>Task 4: Test suite status and summary</name>
  <files>.planning/phases/84-review-all-syntax-errors-code-failures-looping-best-practices-for-all-code/TEST_AUDIT.md