---
phase: 84-review-all-syntax-errors-code-failures-looping-best-practices-for-all-code
plan: 03
type: execute
wave: 2
depends_on: ["84-01"]
files_modified: []
autonomous: true
requirements: []

must_haves:
  truths:
    - "All identified dead code and unused imports are removed"
    - "Console.log statements prioritized: kept for errors/warnings, removed for debug"
    - "High-severity code issues are fixed (null checks, error handling)"
    - "Test suite still passes (all 201 tests)"
  artifacts:
    - path: ".planning/phases/84-review-all-syntax-errors-code-failures-looping-best-practices-for-all-code/FIXES_APPLIED.md"
      provides: "Log of all fixes applied with before/after snippets and test status"
  key_links:
    - from: "Code fixes"
      to: "Test suite"
      via: "npm test validation"
      pattern: "After each fix: npm test passes"
---

<objective>
Fix high-priority code quality issues identified in the 84-01 audit. Focus on: dead code removal, console logging cleanup, null safety improvements, and error handling gaps.

Purpose: Cleaner, more maintainable codebase; reduced debug noise; better error messages
Output: Fixed code files + FIXES_APPLIED.md documenting all changes
</objective>

<execution_context>
@/Users/cmuir/P1Import-apps/Banking/.planning/phases/84-review-all-syntax-errors-code-failures-looping-best-practices-for-all-code/CONTEXT.md
@/Users/cmuir/P1Import-apps/Banking/.planning/phases/84-review-all-syntax-errors-code-failures-looping-best-practices-for-all-code/UI_AUDIT.md
@/Users/cmuir/P1Import-apps/Banking/.planning/phases/84-review-all-syntax-errors-code-failures-looping-best-practices-for-all-code/API_AUDIT.md
@/Users/cmuir/P1Import-apps/Banking/REGRESSION_PLAN.md
</execution_context>

<context>
Audit outputs from Plan 01:
- UI_AUDIT.md identifies console.log hotspots and dead code in banking_api_ui/src
- API_AUDIT.md identifies same in banking_api_server
- TEST_AUDIT.md verifies 201 tests as golden baseline
- REGRESSION_PLAN.md §1 lists protected areas — DO NOT BREAK these
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove debug console.log statements and clean UI</name>
  <files>banking_api_ui/src</files>
  <action>
Based on UI_AUDIT.md findings:

1. **Console.log cleanup strategy:**
   - KEEP: error/warning logs (console.error, console.warn) that provide user-facing or operational info
   - REMOVE: debug logs (console.log) that are only used during development
   - CONVERT: high-value logs to structured logging (if logging service exists) or keep as comments

2. **Process:**
   - For each file identified in UI_AUDIT.md as console.log hotspot:
     - Review each console.log call
     - Decide: keep (error/warning), remove (debug), or convert (useful diagnostic)
     - If removing: check if any code depends on the log for testing
   - Run npm test in banking_api_ui after each batch of changes
   - Commit: `fix(phase-84): remove debug console.log from banking_api_ui`

3. **Focus areas (if identified in audit):**
   - Component lifecycle logs
   - Event handler logs
   - State change logs
   - API call debug logs
   
Estimated: 30-50 console.log statements removed from UI
  </action>
  <verify>
    npm run build in banking_api_ui exits with 0
    npm test in banking_api_ui passes (if tests exist)
    console.log count reduced by 50%+ in UI
    No component imports are broken
    Production build still works
  </verify>
  <done>
    Debug console.log statements removed from UI. Production logging (errors/warnings) preserved. Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Clean API server code and fix high-priority issues</name>
  <files>banking_api_server</files>
  <action>
Based on API_AUDIT.md findings:

1. **Console.log cleanup (same strategy as Task 1):**
   - Remove debug logs from services
   - Keep operational logs (errors, important state transitions)
   - Estimated removal: 50-100 logs in API server

2. **Dead code and unused imports:**
   - Identify if any require()/import statements are unused
   - Remove imports that are not used in the file
   - Check for commented-out code blocks — remove or document as legacy

3. **Error handling improvements (if gaps found):**
   - Ensure all catch blocks log the error with context
   - Add error context where missing (which operation failed, what data was involved)
   - Review any catch blocks that silently swallow errors

4. **Process:**
   - Run npm test in banking_api_server after each batch
   - Use integration tests to verify no regressions
   - Commit: `fix(phase-84): clean api server - remove debug logs, unused imports, fix error handling`

5. **Protected areas (from REGRESSION_PLAN.md §1):**
   - Do NOT refactor auth flows
   - Do NOT change token exchange logic
   - Do NOT modify session handling
   - Only fix: logging, unused code, error messages
  </action>
  <verify>
    npm test in banking_api_server passes (all integration tests)
    console.log count reduced by 50%+ in API server
    No protected areas were modified
    All error handling logs remain intact
    Service still starts and routes work
  </verify>
  <done>
    API server cleaned of debug logs and unused imports. Error handling improved. Tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: MCP server code review and cleanup</name>
  <files>banking_mcp_server</files>
  <action>
Review banking_mcp_server (TypeScript) for code quality:

1. **Overview:**
   - Check for console.log statements and apply same cleanup strategy
   - Verify TypeScript types are strict (no untyped parameters)
   - Review for any obvious dead code or unused imports

2. **Code review checklist:**
   - [ ] All function parameters are typed
   - [ ] console.log removed (keep error/warn logs)
   - [ ] No commented-out code blocks
   - [ ] Error handling is consistent across tool functions
   - [ ] MCP spec compliance maintained (tool responses, error codes)

3. **Process:**
   - If build script exists, run it and verify it still compiles
   - Document any issues that would require refactoring vs. simple cleanup

4. **Note:** MCP server is lower priority than UI/API for this phase, so focus on obvious cleanups only
  </action>
  <verify>
    TypeScript compilation succeeds (npm run build or similar)
    No breaking changes to tool interfaces
    Console logging reduced
    Code is cleaner and more maintainable
  </verify>
  <done>
    MCP server reviewed and cleaned. Type safety verified. Ready for production.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Code changes | All changes are deletions/simplifications; no new functionality |
| Test suite | Golden baseline (201 tests); changes must not break tests |
| Protected areas | REGRESSION_PLAN.md §1 files are off-limits for refactoring |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-04 | Tampering | Code removal | mitigate | All changes reviewed via git diff before commit; test suite validates correctness |
| T-84-05 | Denial of Service | Logging removal | mitigate | Only debug logs removed; error/warning logs that support operations are preserved |
</threat_model>

<verification>
- [ ] npm test passes in banking_api_ui (if tests exist)
- [ ] npm test passes in banking_api_server (all integration tests)
- [ ] npm run build succeeds in banking_api_ui
- [ ] banking_api_server starts without errors
- [ ] banking_mcp_server compiles (TypeScript)
- [ ] No protected areas from REGRESSION_PLAN.md §1 were modified
- [ ] FIXES_APPLIED.md documents all changes with before/after
</verification>

<success_criteria>
50%+ reduction in debug console.log statements across all codebases.
All unused imports removed.
Error logging remains intact and informative.
All 201 tests pass.
No broken functionality.
Code is cleaner and more maintainable.
</success_criteria>

<output>
After completion, create `.planning/phases/84-review-all-syntax-errors-code-failures-looping-best-practices-for-all-code/84-03-SUMMARY.md`
</output>
