---
phase: 85-chase-dashboard-styling
plan: 02
type: execute
wave: 2
depends_on: ["85-01"]
files_modified: ["banking_api_ui/src/components/UserDashboard.js", "banking_api_ui/src/components/Dashboard.js", "banking_api_ui/src/components/dashboard/DashboardHero.js", "banking_api_ui/src/components/DashboardLayoutToggle.js", "banking_api_ui/src/components/DashboardQuickNav.js", "banking_api_ui/src/index.css"]
autonomous: true
requirements: []

must_haves:
  truths:
    - "All dashboard components use Chase navy (#004687) as primary color"
    - "All buttons are styled with Chase navy, white text, rounded corners"
    - "All cards have consistent padding, border radius, and subtle shadows"
    - "Color contrast meets WCAG AA accessibility standards"
    - "npm run build passes without errors"
  artifacts:
    - path: "banking_api_ui/src/components/UserDashboard.js"
      provides: "Updated dashboard with Chase styling"
    - path: "banking_api_ui/src/components/dashboard/DashboardHero.js"
      provides: "Hero section with Chase navy color scheme"
    - path: "banking_api_ui/src/index.css"
      provides: "CSS variables and global color definitions for Chase brand"
  key_links:
    - from: "Dashboard components"
      to: "CSS variables"
      via: "Use CSS custom properties for consistency"
      pattern: "var(--chase-navy), var(--chase-blue)"
    - from: "All components"
      to: "npm run build"
      via: "Validation and testing"
      pattern: "Exit code must be 0"
---

<objective>
Update all dashboard components with Chase.com styling: colors, buttons, cards, typography, and spacing. Use CSS variables for maintainability and consistency.

Purpose: Create professional banking UI that matches Chase.com's visual identity
Output: Styled dashboard components + updated CSS variables
</objective>

<execution_context>
@/Users/cmuir/P1Import-apps/Banking/.planning/phases/85-chase-dashboard-styling/CONTEXT.md
@/Users/cmuir/P1Import-apps/Banking/.planning/phases/85-chase-dashboard-styling/STYLE_AUDIT.md
@/Users/cmuir/P1Import-apps/Banking/CLAUDE.md
</execution_context>

<context>
STYLE_AUDIT.md provides the color mapping and priority order.
Target colors:
- Primary: Chase Navy #004687
- Secondary: Chase Blue #0052A3
- Text: Dark Gray #333333
- Backgrounds: White #FFFFFF, Light Gray #F5F5F5
- Buttons: Navy bg, white text, 4px border-radius, hover darker

Dashboard components to update:
1. UserDashboard.js (main dashboard layout)
2. DashboardHero.js (header section)
3. Dashboard.js (wrapper)
4. DashboardLayoutToggle.js (buttons for view modes)
5. DashboardQuickNav.js (sidebar nav)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create/update CSS variables and global styles</name>
  <files>banking_api_ui/src/index.css</files>
  <action>
Define Chase.com color scheme in CSS variables:

1. **Add or update CSS custom properties:**
   ```css
   :root {
     /* Chase Brand Colors */
     --chase-navy: #004687;
     --chase-blue: #0052A3;
     --chase-light-blue: #0066CC;
     --chase-white: #FFFFFF;
     --chase-light-gray: #F5F5F5;
     --chase-medium-gray: #E0E0E0;
     --chase-dark-gray: #333333;
     --chase-success: #4CAF50;
     --chase-warning: #FF9800;
     --chase-error: #F44336;
     
     /* Button Styles */
     --btn-primary-bg: var(--chase-navy);
     --btn-primary-text: var(--chase-white);
     --btn-primary-bg-hover: #003770;
     
     /* Card Styles */
     --card-padding: 20px;
     --card-border-radius: 8px;
     --card-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
     
     /* Typography */
     --font-primary: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', sans-serif;
     --font-size-h1: 28px;
     --font-size-h2: 22px;
     --font-size-h3: 18px;
     --font-size-body: 14px;
     --line-height-body: 1.6;
   }
   ```

2. **Update button base styles:**
   ```css
   button, .btn {
     background-color: var(--btn-primary-bg);
     color: var(--btn-primary-text);
     border: none;
     border-radius: 4px;
     padding: 10px 16px;
     font-family: var(--font-primary);
     font-size: var(--font-size-body);
     cursor: pointer;
     transition: background-color 0.2s ease;
   }
   
   button:hover, .btn:hover {
     background-color: var(--btn-primary-bg-hover);
   }
   
   button:focus, .btn:focus {
     outline: 3px solid var(--chase-light-blue);
     outline-offset: 2px;
   }
   ```

3. **Update card base styles:**
   ```css
   .card, [role="card"] {
     background-color: var(--chase-white);
     border-radius: var(--card-border-radius);
     padding: var(--card-padding);
     box-shadow: var(--card-shadow);
   }
   ```

4. **Ensure global text color:**
   ```css
   body, html {
     color: var(--chase-dark-gray);
     font-family: var(--font-primary);
     background-color: var(--chase-light-gray);
   }
   ```

This creates a maintainable foundation so all components can use `var(--chase-navy)` instead of hardcoded colors.
  </action>
  <verify>
    index.css contains:
    - All Chase color CSS variables defined
    - Button and card base styles set
    - Typography variables in place
    - npm run build succeeds
  </verify>
  <done>
    CSS variables and base styles established. All components can now use consistent colors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update DashboardHero component</name>
  <files>banking_api_ui/src/components/dashboard/DashboardHero.js</files>
  <action>
Update DashboardHero.js with Chase styling:

1. **Hero section background:**
   - Current: Likely blue but may be different shade
   - Change to: Chase Navy (#004687) or gradient (navy to slightly lighter blue)
   - Use: `background-color: var(--chase-navy);`

2. **Hero text:**
   - Headings: White or very light gray
   - Body text: White or light color for contrast
   - Use: `color: var(--chase-white);`

3. **Hero buttons (if any):**
   - Primary button: Chase Navy with white text (already in global styles)
   - Secondary: Light gray bg, navy text

4. **Implementation:**
   - Find all inline styles with color/background properties
   - Replace with CSS classes or inline styles using CSS variables
   - Example: `style={{ backgroundColor: '#2563EB' }}` → `style={{ backgroundColor: 'var(--chase-navy)' }}`

5. **Verify:**
   - Hero is clearly visible with navy background
   - Text is readable (sufficient contrast)
   - Buttons match Chase style
  </action>
  <verify>
    DashboardHero.js uses:
    - var(--chase-navy) for background
    - var(--chase-white) for text
    - Buttons styled from global button styles
    - Contrast meets WCAG AA (4.5:1 for text)
  </verify>
  <done>
    Hero section styled with Chase navy and professional white text. Ready for dashboard content.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update main dashboard components</name>
  <files>banking_api_ui/src/components/UserDashboard.js, banking_api_ui/src/components/Dashboard.js</files>
  <action>
Update UserDashboard.js and Dashboard.js with Chase styling:

1. **Background colors:**
   - Main background: Light gray (var(--chase-light-gray)) or white
   - Card backgrounds: White (var(--chase-white))
   - Replace any hard-coded bg colors

2. **Text colors:**
   - Primary text: Dark gray (var(--chase-dark-gray))
   - Secondary text: Medium gray (#666666)
   - Headings: Navy (var(--chase-navy))

3. **Dashboard cards (account tiles):**
   - Backgrounds: White
   - Border radius: 8px (var(--card-border-radius))
   - Padding: 20px (var(--card-padding))
   - Shadow: Subtle shadow (var(--card-shadow))
   - Border: Optional light gray 1px border

4. **Account balance display:**
   - Balance number: Navy blue, larger font (20px+)
   - Account type: Gray, smaller font
   - Account number: Gray, very small, obfuscated if needed

5. **Button styling:**
   - All buttons use primary button style (navy bg, white text)
   - Verify hover/focus states work

6. **Spacing:**
   - Card margin/gap: 16px or 20px (consistent rhythm)
   - Section padding: 20px

7. **Grid layout (if using Tailwind grid):**
   - Ensure responsive (3 cols desktop, 1 col mobile)
   - Gap between cards: 16px-20px using global var if available
  </action>
  <verify>
    UserDashboard.js and Dashboard.js:
    - All cards have white background and proper padding/radius
    - Text uses dark gray by default
    - Buttons match Chase style
    - Cards are evenly spaced
    - Responsive layout still works on mobile
    - npm run build passes
  </verify>
  <done>
    Main dashboard components styled. All cards and text aligned with Chase design.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update navigation and utility components</name>
  <files>banking_api_ui/src/components/DashboardLayoutToggle.js, banking_api_ui/src/components/DashboardQuickNav.js</files>
  <action>
Update DashboardLayoutToggle and DashboardQuickNav with Chase styling:

1. **DashboardLayoutToggle (view mode switcher):**
   - Active button: Navy background, white text
   - Inactive button: Light gray background, dark gray text
   - Hover state: Slightly darker
   - Implementation: Use `bg-chase-navy` or className conditionally

2. **DashboardQuickNav (sidebar nav):**
   - Background: Light gray or white
   - Nav items: Dark text on light bg
   - Active nav item: Navy background or navy text with underline
   - Hover state: Subtle bg color change (slightly darker gray)
   - Icons: Navy color to match navy theme

3. **Consistency checks:**
   - All interactive elements have visible focus state (blue outline)
   - Hover states are clear and noticeable
   - Active states are distinct from inactive

4. **Responsive behavior:**
   - Mobile: Nav may collapse or hide; ensure still readable
   - Desktop: Full nav with proper spacing
  </action>
  <verify>
    DashboardLayoutToggle.js and DashboardQuickNav.js:
    - Active/inactive button states clearly visible
    - Nav items are readable and properly spaced
    - Hover and focus states work
    - Mobile layout is clean and not crowded
  </verify>
  <done>
    Navigation and utility components styled. All buttons and nav items follow Chase design language.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS changes | Styling only; no logic changes to data display |
| Component functionality | All interactive elements remain functional |
| Build process | CSS must not break webpack build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-03 | Denial of Service | CSS size | accept | CSS vars reduce duplication; build size monitored |
| T-85-04 | Information Disclosure | Colors | accept | No sensitive data in styling |
</threat_model>

<verification>
- [ ] npm run build in banking_api_ui passes (exit code 0)
- [ ] All dashboard components render with Chase navy color scheme
- [ ] Buttons have rounded corners and white text
- [ ] Cards have consistent padding and shadows
- [ ] Text contrast meets WCAG AA (4.5:1 for normal text)
- [ ] Mobile layout is responsive and readable
- [ ] No console errors or warnings during render
- [ ] All interactive elements (buttons, nav) have visible focus states
</verification>

<success_criteria>
All dashboard components visually match Chase.com's design language. Colors are consistent, buttons are styled properly, cards have professional appearance. Build passes. No functionality broken. Ready for final polish and testing in Plan 3.
</success_criteria>

<output>
After completion, create `.planning/phases/85-chase-dashboard-styling/85-02-SUMMARY.md`
</output>
