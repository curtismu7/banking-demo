---
phase: 85-chase-dashboard-styling
plan: 03
type: execute
wave: 3
depends_on: ["85-02"]
files_modified: ["banking_api_ui/src/components/dashboard/MobileDashboard.js", "banking_api_ui/src/index.css", "banking_api_ui/src/components/UserDashboard.js"]
autonomous: true
requirements: []

must_haves:
  truths:
    - "Mobile dashboard is fully responsive and readable on all screen sizes"
    - "No horizontal scroll on mobile or tablet"
    - "Card sizes and spacing are appropriate for small screens"
    - "All text is readable without zooming"
    - "Colors and styling are consistent across all breakpoints"
    - "No regressions in other dashboard views or pages"
  artifacts:
    - path: "banking_api_ui/src/components/dashboard/MobileDashboard.js"
      provides: "Mobile-optimized dashboard with Chase styling"
  key_links:
    - from: "Mobile dashboard"
      to: "Build verification"
      via: "npm run build and visual testing"
      pattern: "Responsive layout verified across breakpoints"
---

<objective>
Optimize Chase-styled dashboards for mobile and tablet devices. Ensure responsive design, readability, and no regressions. Perform final testing and polish.

Purpose: Deliver professional mobile experience; match desktop styling across all breakpoints
Output: Mobile-optimized dashboard + verified cross-device styling
</objective>

<execution_context>
@/Users/cmuir/P1Import-apps/Banking/.planning/phases/85-chase-dashboard-styling/CONTEXT.md
@/Users/cmuir/P1Import-apps/Banking/.planning/phases/85-chase-dashboard-styling/STYLE_AUDIT.md
@/Users/cmuir/P1Import-apps/Banking/CLAUDE.md
@/Users/cmuir/P1Import-apps/Banking/REGRESSION_PLAN.md
</execution_context>

<context>
Plan 02 completed: Dashboard components styled with Chase navy, white buttons, professional cards.
MobileDashboard.js: Mobile-specific layout component to optimize for small screens
Responsive breakpoints: SM (mobile), MD (tablet), LG (desktop)
Goal: Maintain Chase styling while ensuring mobile usability
</context>

<tasks>

<task type="auto">
  <name>Task 1: Optimize MobileDashboard for small screens</name>
  <files>banking_api_ui/src/components/dashboard/MobileDashboard.js</files>
  <action>
Update MobileDashboard.js for Chase styling and mobile optimization:

1. **Layout adjustments for mobile:**
   - Card grid: 1 column on mobile (< 640px), 2 columns on tablet, 3+ on desktop
   - Use Tailwind responsive classes: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`
   - Or use media queries in CSS if custom styling needed

2. **Spacing on mobile:**
   - Reduce padding slightly on mobile: 16px instead of 20px
   - Maintain visual hierarchy
   - Ensure cards don't touch screen edges (margin/padding: 12-16px)

3. **Typography:**
   - Heading size: Reduce on mobile (20px → 18px)
   - Body text: Keep readable (14px minimum)
   - No EXCESSIVE text; use abbreviations where needed (e.g., "Savings" not "Savings Account")

4. **Buttons on mobile:**
   - Full width or reasonable width? Check Chase.com mobile experience
   - Tap target size: Minimum 44x44px (accessibility standard)
   - Stacking: Buttons should stack vertically on mobile if multiple, or be full-width

5. **Hero section:**
   - Maintain Chase navy background
   - Reduce padding on mobile (12-16px top/bottom)
   - Headings: Responsive sizes

6. **Account cards:**
   - Padding: 16px on mobile, 20px on desktop
   - Text size: Slightly smaller on mobile if needed for fit
   - Icon size: Appropriate for mobile (not oversized)

7. **Test checklist:**
   - No horizontal scroll on 375px width (iPhone SE)
   - No horizontal scroll on 768px width (iPad)
   - Cards are readable without zooming
   - Buttons are tappable (44px+)
  </action>
  <verify>
    MobileDashboard.js:
    - Responsive grid (1 col mobile, 2+ desktop)
    - No horizontal scroll on 375px or 768px widths
    - Padding/spacing is appropriate for small screens
    - Text is readable (14px+ font size)
    - Buttons are tappable (44px+ height)
    - Chase colors and styles maintained
  </verify>
  <done>
    Mobile dashboard optimized. Layout is responsive and readable across all screen sizes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify Chase styling across responsive breakpoints</name>
  <files>banking_api_ui/src/index.css</files>
  <action>
Verify and update responsive styling to ensure Chase design is consistent across all breakpoints:

1. **Media query audit:**
   - Check for any breakpoint-specific color overrides
   - Ensure CSS variables work at all breakpoints
   - Example: `@media (max-width: 768px) { ... }`

2. **Button sizing:**
   - Desktop: Reasonable button size (10px padding top/bottom)
   - Mobile: Larger touch target (44px minimum height)
   - Use responsive padding: `@media (max-width: 640px) { button { padding: 12px 14px; } }`

3. **Card spacing:**
   - Desktop: 20px padding, 20px gap between cards
   - Tablet: 20px padding, 16px gap
   - Mobile: 16px padding, 12px gap
   - Use CSS variables or media queries to adjust

4. **Typography scaling:**
   - Desktop: Full sizes (28px h1, 22px h2, 14px body)
   - Tablet: Slightly smaller (24px h1, 20px h2, 14px body)
   - Mobile: Further reduced (22px h1, 18px h2, 14px body)

5. **Verification:**
   - Open dashboard in Chrome DevTools responsive mode
   - Test at standard breakpoints: 320px, 375px, 640px, 768px, 1024px, 1440px
   - Verify no layout shifts or overflow
   - Check color consistency across breakpoints

6. **Common issues to fix:**
   - Cards not fitting on mobile (overflow: hidden or max-width adjustments)
   - Text being cut off (word-wrap: break-word)
   - Buttons being too narrow (min-width or full-width on mobile)
  </action>
  <verify>
    Responsive styling verified:
    - No horizontal scroll at 320px, 375px, 640px widths
    - Card padding and spacing is appropriate at each breakpoint
    - Typography scales well and remains readable
    - Colors are consistent across all breakpoints
    - Buttons and interactive elements work at all sizes
  </verify>
  <done>
    Responsive design verified. Chase styling is consistent and professional across all device sizes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Regression testing and final verification</name>
  <files></files>
  <action>
Perform comprehensive testing to ensure no regressions and styling is complete:

1. **Build verification:**
   ```bash
   cd banking_api_ui && npm run build
   ```
   - Must exit with code 0 (no errors)
   - Check for CSS warnings

2. **Visual regression checks:**
   - [ ] Dashboard renders without console errors
   - [ ] All colors are Chase-branded (navy, white, grays)
   - [ ] Buttons have rounded corners (4px)
   - [ ] Cards have shadows and proper padding
   - [ ] Text contrast is sufficient (WCAG AA)

3. **Responsive checks:**
   - [ ] Desktop (1440px+): Full 3-column layout, proper spacing
   - [ ] Tablet (768px-1023px): 2-column layout, readable
   - [ ] Mobile (< 640px): 1-column layout, no overflow
   - [ ] Mobile landscape: Still readable, no extreme stretching

4. **Functionality verification:**
   - [ ] All buttons are clickable
   - [ ] Navigation works
   - [ ] Data displays correctly (no truncation or overlap)
   - [ ] Hover/focus states work on all interactive elements
   - [ ] Mobile touch targets are at least 44x44px

5. **Cross-page checks:**
   - [ ] Admin dashboard (if different) also uses Chase styling
   - [ ] No style conflicts with other pages (auth, setup, etc.)
   - [ ] Marketing pages unchanged (per CLAUDE.md)

6. **Accessibility verification:**
   - [ ] Color contrast: 4.5:1 for normal text (WCAG AA)
   - [ ] Large text (18px+): 3:1 contrast ratio
   - [ ] Focus states: Visible outlines (blue, 2-3px)
   - [ ] No color-only information (shapes/icons also convey meaning)

7. **Documentation:**
   - [ ] Update CHANGELOG.md with styling changes
   - [ ] Note: Phase 85 complete, dashboard now matches Chase.com design

8. **Commit message:**
   ```
   style(phase-85): update dashboards to match chase.com design language
   
   - Updated all dashboard components with Chase navy color scheme
   - Button styles: navy bg, white text, 4px border-radius
   - Card styles: white bg, 20px padding, 8px radius, subtle shadows
   - Mobile-optimized responsive layout (1 col mobile, 3 col desktop)
   - CSS variables for color consistency and maintainability
   - Full WCAG AA accessibility compliance
   - Verified responsive at all breakpoints (320px-1440px+)
   ```
  </action>
  <verify>
    Testing complete:
    - npm run build exits with code 0
    - No console errors on dashboard render
    - All colors match Chase brand
    - Responsive layout works perfectly at all breakpoints
    - All tests pass (if test suite exists)
    - No regressions in other pages
    - WCAG AA contrast standards met
  </verify>
  <done>
    Phase 85 complete. Dashboard styling fully updated to match Chase.com. All responsive and accessible. Ready for deployment.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Styling changes | CSS only; no data logic changes |
| Mobile rendering | Must work across iOS Safari, Android Chrome, and desktop browsers |
| Accessibility | Colors must meet WCAG AA contrast requirements |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-05 | Denial of Service | Large CSS files | accept | CSS vars reduce duplication; monitor bundle size |
| T-85-06 | Information Disclosure | None | accept | Styling is purely visual; no sensitive data exposed |
| T-85-07 | Accessibility | Low contrast | mitigate | Test all color combinations against WCAG AA (4.5:1) |
</threat_model>

<verification>
- [ ] npm run build in banking_api_ui exits with code 0
- [ ] Dashboard renders without console errors
- [ ] Responsive layout verified at: 320px, 375px, 640px, 768px, 1024px, 1440px
- [ ] No horizontal scroll on any mobile width (< 1024px)
- [ ] Color contrast meets WCAG AA (4.5:1 for text, 3:1 for large text)
- [ ] Touch targets are >= 44x44px on mobile
- [ ] Chase navy (#004687) is primary color throughout
- [ ] All buttons have rounded corners and white text
- [ ] All cards have proper padding, radius, and shadows
- [ ] Focus states are visible (blue outline) on all interactive elements
- [ ] No regressions in other dashboard pages or views
- [ ] All functionality works (buttons, nav, data display)
</verification>

<success_criteria>
Dashboard styling is complete and professional. Chase.com design language is consistently applied. Fully responsive across all device sizes. WCAG AA accessible. Build passes. Ready for production.
</success_criteria>

<output>
After completion, create `.planning/phases/85-chase-dashboard-styling/85-03-SUMMARY.md` and update .planning/PHASES.md to mark Phase 85 as complete.
</output>
