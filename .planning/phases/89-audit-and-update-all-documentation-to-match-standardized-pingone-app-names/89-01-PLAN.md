---
phase: 89-documentation-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - banking_api_server/env.example
  - docs/ENVIRONMENT_MAPPING.md
  - docs/SETUP.md
  - docs/VERCEL_SETUP.md
  - docs/PINGONE_ACTUAL_ENVIRONMENT.md
  - docs/PINGONE_APP_CONFIG.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "README.md references canonical PingOne app names (Super Banking Admin App, Super Banking User App, etc.) not generic names"
    - "env.example comments use canonical PingOne app names as # Super Banking <name> (type: <TYPE>) labels"
    - "docs/ENVIRONMENT_MAPPING.md is the authoritative mapping table and uses canonical env var names"
    - "docs/PINGONE_ACTUAL_ENVIRONMENT.md matches live PingOne query results"
    - "docs/PINGONE_APP_CONFIG.md app names match actual PingOne console names"
  artifacts:
    - path: "docs/ENVIRONMENT_MAPPING.md"
      provides: "Authoritative env var → PingOne app mapping table, updated with Phase 88 renames"
    - path: "banking_api_server/env.example"
      provides: "Comments on each credential block show PingOne app name + type"
    - path: "docs/PINGONE_APP_CONFIG.md"
      provides: "App names aligned with actual PingOne console names"
  key_links:
    - from: "docs/ENVIRONMENT_MAPPING.md"
      to: "banking_api_server/env.example"
      via: "env var names must match"
      pattern: "PINGONE_ADMIN_APP_CLIENT_ID|PINGONE_USER_APP_CLIENT_ID|PINGONE_WORKER_CLIENT_ID"
---

<objective>
Audit and update core documentation files so every reference to PingOne app names, env var names, and credential blocks uses the canonical names established in Phase 88. These files are the first thing a new developer reads — they must reflect the current truth.

**Purpose:** README.md, env.example, ENVIRONMENT_MAPPING.md, SETUP.md, PINGONE_APP_CONFIG.md, and PINGONE_ACTUAL_ENVIRONMENT.md must all use consistent canonical names so developers setting up the project are not confused by mismatched names between the docs and the actual PingOne console.

**Output:** Updated docs that reflect the Phase 88 canonical naming — no old names except where backward compat is explicitly noted.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@docs/ENVIRONMENT_MAPPING.md

<!-- Canonical PingOne app names (authoritative — do not change these):
  Super Banking Admin App       → WEB_APP  → env: PINGONE_ADMIN_APP_CLIENT_ID/SECRET
  Super Banking User App        → WEB_APP  → env: PINGONE_USER_APP_CLIENT_ID/SECRET/REDIRECT_URI
  Super Banking MCP Token Exchanger → AI_AGENT → env: PINGONE_MCP_TOKEN_EXCHANGER_CLIENT_ID/SECRET/SCOPES
  Super Banking AI Agent App    → AI_AGENT → env: PINGONE_AI_AGENT_CLIENT_ID/SECRET
  Super Banking Worker Token    → WORKER   → env: PINGONE_WORKER_CLIENT_ID/SECRET

  Resource servers:
  Super Banking MCP Server      → audience: https://mcp-server.pingdemo.com → env: PINGONE_RESOURCE_MCP_SERVER_URI
  MCP Gateway                   → audience: https://mcp-gateway.pingdemo.com → env: PINGONE_RESOURCE_MCP_GATEWAY_URI
  Banking API / Two-Exchange    → audience: https://resource-server.pingdemo.com → env: PINGONE_RESOURCE_TWO_EXCHANGE_URI
  Super Banking AI Agent        → audience: https://ai-agent.pingdemo.com → env: ENDUSER_AUDIENCE

  PingOne Environment ID: d02d2305-f445-406d-82ee-7cdbf6eeabfd  Region: com
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md and banking_api_server/env.example with canonical names</name>
  <files>
    README.md
    banking_api_server/env.example
  </files>
  <read_first>
    README.md (search for PingOne, PINGONE_, env var references)
    banking_api_server/env.example (full file — audit all comments on credential blocks)
  </read_first>
  <action>
**In README.md:**

Search for all occurrences of the following old names and update to canonical:
- `AI_AGENT_CLIENT_ID` → `PINGONE_AI_AGENT_CLIENT_ID`
- `AGENT_OAUTH_CLIENT_ID` → `PINGONE_MCP_TOKEN_EXCHANGER_CLIENT_ID`
- `MCP_RESOURCE_URI` (as a standalone env var reference) → `PINGONE_RESOURCE_MCP_SERVER_URI`
- "admin app" (when referring to the PingOne app) → "Super Banking Admin App"
- "user app" (when referring to the PingOne app) → "Super Banking User App"
- "worker token" or "worker app" → "Super Banking Worker Token"
- "AI agent app" or "AI Agent" (as PingOne app) → "Super Banking AI Agent App"
- "MCP Token Exchanger" (bare) → "Super Banking MCP Token Exchanger"

Do NOT change prose that is explaining concepts (e.g., "the worker token is used for management API calls" — keep the explanation, just update the app/var names). Do NOT change code blocks showing OAuth flows — only update env var names in setup/config sections.

**In banking_api_server/env.example:**

For every credential block that has a comment above it, ensure the comment says:
```
# Super Banking Admin App (type: WEB_APP)
PINGONE_ADMIN_APP_CLIENT_ID=your-admin-app-client-id
PINGONE_ADMIN_APP_CLIENT_SECRET=your-admin-app-client-secret
# Backward compat (still accepted):
# PINGONE_ADMIN_CLIENT_ID=
# PINGONE_ADMIN_CLIENT_SECRET=
```

Apply the same pattern for all 5 apps. Also add a header comment at the top of the credentials section:
```
# ─── PingOne App Credentials ─────────────────────────────────────────────────
# Canonical var names match actual PingOne console app names.
# See docs/ENVIRONMENT_MAPPING.md for full mapping table.
```

For resource server URIs:
```
# Super Banking MCP Server (Resource Server audience)
PINGONE_RESOURCE_MCP_SERVER_URI=https://mcp-server.pingdemo.com
# Backward compat: MCP_RESOURCE_URI= (still read as fallback)
```
  </action>
  <verify>
    <automated>grep -c "PINGONE_ADMIN_APP_CLIENT_ID\|PINGONE_USER_APP_CLIENT_ID\|PINGONE_WORKER_CLIENT_ID" banking_api_server/env.example</automated>
  </verify>
  <done>
    - env.example has canonical name comments on all 5 PingOne app credential blocks
    - README.md references updated env var names in any setup/quickstart sections
    - Old names preserved as backward-compat comments, not removed entirely
  </done>
</task>

<task type="auto">
  <name>Task 2: Update docs/ENVIRONMENT_MAPPING.md, PINGONE_APP_CONFIG.md, PINGONE_ACTUAL_ENVIRONMENT.md</name>
  <files>
    docs/ENVIRONMENT_MAPPING.md
    docs/PINGONE_APP_CONFIG.md
    docs/PINGONE_ACTUAL_ENVIRONMENT.md
  </files>
  <read_first>
    docs/ENVIRONMENT_MAPPING.md (full file — this is the authoritative mapping doc, needs Phase 88 renames reflected)
    docs/PINGONE_APP_CONFIG.md (full file — check app name references)
    docs/PINGONE_ACTUAL_ENVIRONMENT.md (full file — was created in previous session, verify matches live data)
  </read_first>
  <action>
**docs/ENVIRONMENT_MAPPING.md:**

This file was created in the previous session (commit ab9d1ee) as the authoritative mapping table. Update it to:
1. Add the Phase 88 new renames: PINGONE_ADMIN_APP_CLIENT_ID, PINGONE_USER_APP_CLIENT_ID, PINGONE_WORKER_CLIENT_ID
2. Ensure the mapping table has columns: `PingOne App Name | App Type | Canonical Env Var | Old Env Var (fallback) | Internal configStore Key`
3. Add all 5 apps in a clean table:

```markdown
## PingOne Application → Env Var Mapping

| PingOne App Name | App Type | Canonical Env Var | Old Name (fallback) | configStore Key |
|-----------------|----------|-------------------|---------------------|-----------------|
| Super Banking Admin App | WEB_APP | `PINGONE_ADMIN_APP_CLIENT_ID` | `PINGONE_ADMIN_CLIENT_ID` | `admin_client_id` |
| Super Banking Admin App | WEB_APP | `PINGONE_ADMIN_APP_CLIENT_SECRET` | `PINGONE_ADMIN_CLIENT_SECRET` | `admin_client_secret` |
| Super Banking User App | WEB_APP | `PINGONE_USER_APP_CLIENT_ID` | `PINGONE_USER_CLIENT_ID` | `user_client_id` |
| Super Banking User App | WEB_APP | `PINGONE_USER_APP_CLIENT_SECRET` | `PINGONE_USER_CLIENT_SECRET` | `user_client_secret` |
| Super Banking User App | WEB_APP | `PINGONE_USER_APP_REDIRECT_URI` | `PINGONE_USER_REDIRECT_URI` | `user_redirect_uri` |
| Super Banking MCP Token Exchanger | AI_AGENT | `PINGONE_MCP_TOKEN_EXCHANGER_CLIENT_ID` | `AGENT_OAUTH_CLIENT_ID` | `pingone_mcp_token_exchanger_client_id` |
| Super Banking MCP Token Exchanger | AI_AGENT | `PINGONE_MCP_TOKEN_EXCHANGER_CLIENT_SECRET` | `AGENT_OAUTH_CLIENT_SECRET` | `pingone_mcp_token_exchanger_client_secret` |
| Super Banking AI Agent App | AI_AGENT | `PINGONE_AI_AGENT_CLIENT_ID` | `AI_AGENT_CLIENT_ID` | `pingone_ai_agent_client_id` |
| Super Banking AI Agent App | AI_AGENT | `PINGONE_AI_AGENT_CLIENT_SECRET` | `AI_AGENT_CLIENT_SECRET` | `pingone_ai_agent_client_secret` |
| Super Banking Worker Token | WORKER | `PINGONE_WORKER_CLIENT_ID` | `PINGONE_CLIENT_ID` | `pingone_client_id` |
| Super Banking Worker Token | WORKER | `PINGONE_WORKER_CLIENT_SECRET` | `PINGONE_CLIENT_SECRET` | `pingone_client_secret` |
```

Add a second table for Resource Servers:
```markdown
## Resource Server → Env Var Mapping

| Resource Server Name | Audience URI | Canonical Env Var | Old Name (fallback) | configStore Key |
|---------------------|-------------|-------------------|---------------------|-----------------|
| Super Banking MCP Server | https://mcp-server.pingdemo.com | `PINGONE_RESOURCE_MCP_SERVER_URI` | `MCP_RESOURCE_URI` | `pingone_resource_mcp_server_uri` |
| MCP Gateway | https://mcp-gateway.pingdemo.com | `PINGONE_RESOURCE_MCP_GATEWAY_URI` | `MCP_GATEWAY_AUDIENCE` | `pingone_resource_mcp_gateway_uri` |
| Super Banking Banking API | https://resource-server.pingdemo.com | `PINGONE_RESOURCE_TWO_EXCHANGE_URI` | `MCP_RESOURCE_URI_TWO_EXCHANGE` | `pingone_resource_two_exchange_uri` |
| Super Banking AI Agent | https://ai-agent.pingdemo.com | `ENDUSER_AUDIENCE` | — | — |
```

**docs/PINGONE_APP_CONFIG.md:**

Search for any PingOne app name references and update to match canonical names. Specifically:
- Any app named just "Admin App" → "Super Banking Admin App"
- "User App" → "Super Banking User App"
- "MCP Token Exchanger" or "Agent OAuth Client" → "Super Banking MCP Token Exchanger"
- "AI Agent App" or "AI Agent Client" → "Super Banking AI Agent App"
- "Worker Token" or "worker" (when referring to the PingOne app) → "Super Banking Worker Token"

**docs/PINGONE_ACTUAL_ENVIRONMENT.md:**

This file was auto-generated from a live PingOne Management API query. Verify it contains the correct 5 apps with their actual names. The file should already be accurate from commit ab9d1ee, but confirm the client IDs match:
- Super Banking Admin App: 14cefa5b-d9d6-4e51-8749-e938d4edd1c0
- Super Banking User App: b2752071-2d03-4927-b865-089dc40b9c85
- Super Banking MCP Token Exchanger: 6380065f-f328-41c2-81ed-1daeec811285
- Super Banking AI Agent App: 2533a614-fcb6-4ab9-82cc-9ab407f1dbda
- Super Banking Worker Token: 95dc946f-5e0a-4a8b-a8ba-b587b244e005

If the file is accurate, only add a note at the top: `> **Last verified:** [date] — Phase 88 canonical names reflect this live state.`
  </action>
  <verify>
    <automated>grep -c "Super Banking Admin App\|Super Banking User App\|Super Banking Worker Token\|Super Banking MCP Token Exchanger\|Super Banking AI Agent App" docs/ENVIRONMENT_MAPPING.md</automated>
  </verify>
  <done>
    - docs/ENVIRONMENT_MAPPING.md has both tables (apps + resource servers) with all 5 apps and 3 resource servers
    - All 11 canonical env var names appear in the mapping table
    - docs/PINGONE_ACTUAL_ENVIRONMENT.md confirmed accurate with 5 canonical app names
    - docs/PINGONE_APP_CONFIG.md references canonical app names
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → developers | Docs must never include actual secret values — only names and placeholder descriptions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-01-01 | Information Disclosure | docs with real credentials | mitigate | Audit docs during update — docs must not contain actual client IDs from the live environment (PINGONE_ACTUAL_ENVIRONMENT.md may contain client IDs for reference, which is acceptable as they are non-secret identifiers) |
</threat_model>

<verification>
```bash
# Check canonical names appear in key docs
grep -l "Super Banking Admin App\|PINGONE_ADMIN_APP_CLIENT_ID" docs/ENVIRONMENT_MAPPING.md banking_api_server/env.example

# Verify no stray old-only references in ENVIRONMENT_MAPPING.md
grep "AI_AGENT_CLIENT_ID\|AGENT_OAUTH_CLIENT_ID\|MCP_RESOURCE_URI" docs/ENVIRONMENT_MAPPING.md | grep -v "fallback\|Old Name\|backward"
```
</verification>

<success_criteria>
- docs/ENVIRONMENT_MAPPING.md contains a complete table with all 5 PingOne apps using canonical names
- banking_api_server/env.example has `# Super Banking <AppName> (type: <TYPE>)` above each credential block
- Old name references only appear in "backward compat" or "Old Name" columns — not as primary names
</success_criteria>

<output>
After completion, create `.planning/phases/89-audit-and-update-all-documentation-to-match-standardized-pingone-app-names/89-01-SUMMARY.md`
</output>
