---
phase: 89-documentation-alignment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PINGONE_MAY_ACT_SETUP.md
  - docs/PINGONE_MAY_ACT_ONE_TOKEN_EXCHANGE.md
  - docs/PINGONE_MAY_ACT_TWO_TOKEN_EXCHANGES.md
  - docs/PINGONE_NAMING_STANDARDIZATION_AUDIT.md
  - docs/SETUP.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "PINGONE_MAY_ACT_SETUP.md env var references use canonical names (PINGONE_MCP_TOKEN_EXCHANGER_CLIENT_ID etc.)"
    - "PINGONE_MAY_ACT_TWO_TOKEN_EXCHANGES.md references canonical app names for both exchanges"
    - "PINGONE_NAMING_STANDARDIZATION_AUDIT.md is updated to reflect Phase 88 completion status"
    - "docs/SETUP.md uses canonical env var names in any quickstart or credential setup sections"
  artifacts:
    - path: "docs/PINGONE_MAY_ACT_SETUP.md"
      provides: "Setup guide with canonical env var names throughout"
    - path: "docs/PINGONE_NAMING_STANDARDIZATION_AUDIT.md"
      provides: "Audit doc updated to show Phase 88 as complete / naming now aligned"
  key_links:
    - from: "docs/PINGONE_MAY_ACT_SETUP.md"
      to: "PINGONE_MCP_TOKEN_EXCHANGER_CLIENT_ID env var"
      via: "env var references in setup steps"
      pattern: "PINGONE_MCP_TOKEN_EXCHANGER_CLIENT_ID"
---

<objective>
Update the PingOne token exchange documentation (MAY_ACT_SETUP, MAY_ACT_ONE/TWO_TOKEN_EXCHANGES) and the naming standardization audit to reflect canonical env var names from Phase 88. These are the docs developers read when configuring the 1-exchange and 2-exchange delegation flows — incorrect env var names here cause setup failures.

**Purpose:** Someone following PINGONE_MAY_ACT_TWO_TOKEN_EXCHANGES.md to set up the 2-exchange flow must see `PINGONE_AI_AGENT_CLIENT_ID` and `PINGONE_MCP_TOKEN_EXCHANGER_CLIENT_ID` as the env vars to set — not the old `AI_AGENT_CLIENT_ID` and `AGENT_OAUTH_CLIENT_ID`.

**Output:** Updated MAY_ACT docs + finalized PINGONE_NAMING_STANDARDIZATION_AUDIT.md.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<!-- Critical env var rename mapping for MAY_ACT docs:
  OLD                              → NEW (canonical)
  AI_AGENT_CLIENT_ID               → PINGONE_AI_AGENT_CLIENT_ID
  AI_AGENT_CLIENT_SECRET           → PINGONE_AI_AGENT_CLIENT_SECRET
  AGENT_OAUTH_CLIENT_ID            → PINGONE_MCP_TOKEN_EXCHANGER_CLIENT_ID
  AGENT_OAUTH_CLIENT_SECRET        → PINGONE_MCP_TOKEN_EXCHANGER_CLIENT_SECRET
  AGENT_OAUTH_CLIENT_SCOPES        → PINGONE_MCP_TOKEN_EXCHANGER_CLIENT_SCOPES
  MCP_RESOURCE_URI                 → PINGONE_RESOURCE_MCP_SERVER_URI
  MCP_GATEWAY_AUDIENCE             → PINGONE_RESOURCE_MCP_GATEWAY_URI
  MCP_RESOURCE_URI_TWO_EXCHANGE    → PINGONE_RESOURCE_TWO_EXCHANGE_URI

  PingOne app name conventions for docs:
    "the AI Agent app"           → "the Super Banking AI Agent App (AI_AGENT type)"
    "the exchanger client"       → "the Super Banking MCP Token Exchanger (AI_AGENT type)"
    "the worker"                 → "the Super Banking Worker Token (WORKER type)"
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update MAY_ACT setup and flow docs</name>
  <files>
    docs/PINGONE_MAY_ACT_SETUP.md
    docs/PINGONE_MAY_ACT_ONE_TOKEN_EXCHANGE.md
    docs/PINGONE_MAY_ACT_TWO_TOKEN_EXCHANGES.md
  </files>
  <read_first>
    docs/PINGONE_MAY_ACT_SETUP.md (full file — find all env var references)
    docs/PINGONE_MAY_ACT_ONE_TOKEN_EXCHANGE.md (full file)
    docs/PINGONE_MAY_ACT_TWO_TOKEN_EXCHANGES.md (full file — most impacted, references both AI_AGENT and AGENT_OAUTH vars)
  </read_first>
  <action>
Apply the following find-and-replace operations across all three files:

**Env var name replacements (exact string match, not regex):**
| Old | New |
|-----|-----|
| `AI_AGENT_CLIENT_ID` | `PINGONE_AI_AGENT_CLIENT_ID` |
| `AI_AGENT_CLIENT_SECRET` | `PINGONE_AI_AGENT_CLIENT_SECRET` |
| `AGENT_OAUTH_CLIENT_ID` | `PINGONE_MCP_TOKEN_EXCHANGER_CLIENT_ID` |
| `AGENT_OAUTH_CLIENT_SECRET` | `PINGONE_MCP_TOKEN_EXCHANGER_CLIENT_SECRET` |
| `AGENT_OAUTH_CLIENT_SCOPES` | `PINGONE_MCP_TOKEN_EXCHANGER_CLIENT_SCOPES` |
| `MCP_RESOURCE_URI` (as env var) | `PINGONE_RESOURCE_MCP_SERVER_URI` |
| `MCP_GATEWAY_AUDIENCE` | `PINGONE_RESOURCE_MCP_GATEWAY_URI` |
| `MCP_RESOURCE_URI_TWO_EXCHANGE` | `PINGONE_RESOURCE_TWO_EXCHANGE_URI` |

**PingOne app name prose replacements (update when clearly referring to the PingOne app, not just code concept):**
- "AI Agent app" → "Super Banking AI Agent App"
- "the exchanger" / "the exchanger client" → "Super Banking MCP Token Exchanger"
- "the MCP Token Exchanger app" → "Super Banking MCP Token Exchanger"

**After replacements:** Add a note at the top of PINGONE_MAY_ACT_TWO_TOKEN_EXCHANGES.md:
```markdown
> **Phase 88 update:** Env var names updated to canonical PingOne app names.
> See `docs/ENVIRONMENT_MAPPING.md` for the full old→new mapping.
```

Do NOT change any OAuth flow descriptions, diagrams references, RFC citations, or code logic explanations — only env var names and PingOne app name prose references.
  </action>
  <verify>
    <automated>grep -rn "AI_AGENT_CLIENT_ID\|AGENT_OAUTH_CLIENT_ID\|MCP_RESOURCE_URI\b" docs/PINGONE_MAY_ACT_SETUP.md docs/PINGONE_MAY_ACT_ONE_TOKEN_EXCHANGE.md docs/PINGONE_MAY_ACT_TWO_TOKEN_EXCHANGES.md | grep -v "was:\|old:\|backward\|previously\|renamed\|PINGONE_AI_AGENT\|PINGONE_MCP_TOKEN\|PINGONE_RESOURCE" | wc -l</automated>
  </verify>
  <done>
    - All three files use `PINGONE_AI_AGENT_CLIENT_ID` (not `AI_AGENT_CLIENT_ID`)
    - All three files use `PINGONE_MCP_TOKEN_EXCHANGER_CLIENT_ID` (not `AGENT_OAUTH_CLIENT_ID`)
    - `grep "AI_AGENT_CLIENT_ID\b" docs/PINGONE_MAY_ACT_*.md` returns 0 lines for bare old name
    - PINGONE_MAY_ACT_TWO_TOKEN_EXCHANGES.md has Phase 88 update note at top
  </done>
</task>

<task type="auto">
  <name>Task 2: Update PINGONE_NAMING_STANDARDIZATION_AUDIT.md and docs/SETUP.md</name>
  <files>
    docs/PINGONE_NAMING_STANDARDIZATION_AUDIT.md
    docs/SETUP.md
  </files>
  <read_first>
    docs/PINGONE_NAMING_STANDARDIZATION_AUDIT.md (full file — understand its structure and what renames it tracked)
    docs/SETUP.md (full file — find env var references in setup steps)
  </read_first>
  <action>
**docs/PINGONE_NAMING_STANDARDIZATION_AUDIT.md:**

This doc was likely created during Phase 69/70 (naming standardization phases) to track app renames. Update it to:

1. Add a "Phase 88 completion" section at the TOP of the document (or update an existing status section):
```markdown
## Phase 88 Completion — Environment Variable Alignment

**Completed:** [date]

All env var names now match canonical PingOne console app names. Changes made:

| Old Env Var | New Canonical Env Var | PingOne App |
|-------------|----------------------|-------------|
| AI_AGENT_CLIENT_ID | PINGONE_AI_AGENT_CLIENT_ID | Super Banking AI Agent App |
| AI_AGENT_CLIENT_SECRET | PINGONE_AI_AGENT_CLIENT_SECRET | Super Banking AI Agent App |
| AGENT_OAUTH_CLIENT_ID | PINGONE_MCP_TOKEN_EXCHANGER_CLIENT_ID | Super Banking MCP Token Exchanger |
| AGENT_OAUTH_CLIENT_SECRET | PINGONE_MCP_TOKEN_EXCHANGER_CLIENT_SECRET | Super Banking MCP Token Exchanger |
| AGENT_OAUTH_CLIENT_SCOPES | PINGONE_MCP_TOKEN_EXCHANGER_CLIENT_SCOPES | Super Banking MCP Token Exchanger |
| MCP_RESOURCE_URI | PINGONE_RESOURCE_MCP_SERVER_URI | Super Banking MCP Server |
| MCP_GATEWAY_AUDIENCE | PINGONE_RESOURCE_MCP_GATEWAY_URI | MCP Gateway |
| MCP_RESOURCE_URI_TWO_EXCHANGE | PINGONE_RESOURCE_TWO_EXCHANGE_URI | Banking API (2-exchange) |
| PINGONE_CLIENT_ID | PINGONE_WORKER_CLIENT_ID | Super Banking Worker Token |
| PINGONE_ADMIN_CLIENT_ID | PINGONE_ADMIN_APP_CLIENT_ID | Super Banking Admin App |
| PINGONE_USER_CLIENT_ID | PINGONE_USER_APP_CLIENT_ID | Super Banking User App |

All old names preserved as backward-compat fallbacks in `configStore.getEffective()`.
See `banking_api_server/services/configStore.js` envFallbackMap.
```

2. If the document has a "TODO" or "Pending" section for env var renames, mark those items as DONE.

**docs/SETUP.md:**

Search for any env var setup instructions and update old names to canonical:
- Any reference to `AI_AGENT_CLIENT_ID`, `AGENT_OAUTH_CLIENT_ID`, `MCP_RESOURCE_URI` → use canonical names
- Add a note: "Env var names match PingOne console app names — see docs/ENVIRONMENT_MAPPING.md"
- Update any prerequisite checklists or "configure these env vars" sections

Do NOT change any installation instructions, port numbers, or non-env-var content.
  </action>
  <verify>
    <automated>grep "Phase 88" docs/PINGONE_NAMING_STANDARDIZATION_AUDIT.md | wc -l</automated>
  </verify>
  <done>
    - PINGONE_NAMING_STANDARDIZATION_AUDIT.md has Phase 88 completion section with full rename table
    - Any "TODO" items in the naming audit for env var renames are marked complete
    - docs/SETUP.md uses canonical env var names in setup instructions
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → developers | No secrets in docs; only placeholder/example values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-02-01 | Information Disclosure | SETUP.md credential examples | accept | Setup docs use placeholder values (your-client-id etc.) — not actual credentials. Any pre-existing example values using real env should be replaced with placeholders if found |
</threat_model>

<verification>
```bash
# No bare old env var names in updated MAY_ACT docs (outside of backward-compat notes)
grep -rn "\bAI_AGENT_CLIENT_ID\b\|\bAGENT_OAUTH_CLIENT_ID\b\|\bMCP_RESOURCE_URI\b" \
  docs/PINGONE_MAY_ACT_SETUP.md docs/PINGONE_MAY_ACT_TWO_TOKEN_EXCHANGES.md \
  docs/PINGONE_NAMING_STANDARDIZATION_AUDIT.md 2>/dev/null \
  | grep -v "Old Env\|was:\|renamed\|PINGONE_RESOURCE_MCP\|PINGONE_AI_AGENT\|PINGONE_MCP_TOKEN"

# Phase 88 completion note present
grep "Phase 88" docs/PINGONE_NAMING_STANDARDIZATION_AUDIT.md
```
</verification>

<success_criteria>
- `grep -rn "AI_AGENT_CLIENT_ID\b" docs/PINGONE_MAY_ACT_*.md` returns 0 results (old bare name gone)
- `grep "Phase 88" docs/PINGONE_NAMING_STANDARDIZATION_AUDIT.md` returns at least 1 match
- docs/SETUP.md canonical env var names used in any setup credential sections
</success_criteria>

<output>
After completion, create `.planning/phases/89-audit-and-update-all-documentation-to-match-standardized-pingone-app-names/89-02-SUMMARY.md`
</output>
