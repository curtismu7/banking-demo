---
phase: 98
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - architecture.mmd
  - ARCHITECTURE.md
  - README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "architecture.mmd shows ConfigPage with Token Validation tab and both introspection/JWT paths"
    - "architecture.mmd includes /api/health/introspection and /api/config/validation-mode routes"
    - "architecture.mmd shows validationModeConfig.js as a RuntimeSettings sibling"
    - "ARCHITECTURE.md reflects that token validation mode is now operator-configurable (not fixed)"
    - "ARCHITECTURE.md shows a comparison table of introspection vs JWT with tradeoffs"
    - "README.md token validation row reflects runtime-switchable modes"
  artifacts:
    - path: "architecture.mmd"
      provides: "Updated Mermaid architecture diagram with token validation toggle"
      contains: "validationModeConfig"
    - path: "ARCHITECTURE.md"
      provides: "Updated standards and implementation status with configurable validation"
      contains: "Introspection vs JWT"
    - path: "README.md"
      provides: "Updated quick-reference table row for token validation"
      contains: "introspection"
  key_links:
    - from: "architecture.mmd ConfigPage"
      to: "validationModeConfig.js"
      via: "new RuntimeConfig subgraph"
      pattern: "validationModeConfig"
    - from: "ARCHITECTURE.md"
      to: "docs/INTROSPECTION_VALIDATION_GUIDE.md"
      via: "cross-reference link"
      pattern: "INTROSPECTION_VALIDATION_GUIDE"
---

<objective>
Update the Mermaid architecture diagram and key markdown docs (ARCHITECTURE.md, README.md) to reflect the Phase 97 token validation mode toggle: operators can now select between RFC 7662 introspection (real-time, detects revocation) and local JWT validation (fast, offline). New routes /api/health/introspection and /api/config/validation-mode must be visible in the architecture.

Purpose: Demo operators and reviewers reading the docs/diagrams should immediately understand that token validation is a configurable choice, not a fixed behavior.

Output: Updated architecture.mmd, ARCHITECTURE.md, README.md
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@architecture.mmd
@ARCHITECTURE.md
@README.md
@banking_api_server/config/validationModeConfig.js
@docs/INTROSPECTION_VALIDATION_GUIDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update architecture.mmd — add token validation config nodes</name>
  <files>architecture.mmd</files>
  <read_first>
    - architecture.mmd (full file — understand existing subgraph structure, classDef names, node naming conventions)
    - banking_api_server/config/validationModeConfig.js (understand what validationModeConfig exports)
    - banking_api_server/routes/health.js (understand new /introspection route)
    - banking_api_server/server.js (confirm /api/config/validation-mode inline routes)
    - banking_api_ui/src/components/Config.js (confirm Token Validation tab exists)
  </read_first>
  <action>
Make the following targeted additions to architecture.mmd:

**1. Add ConfigPage node to UI subgraph** (after McpInspector node):
```
ConfigPage["Config Page\ntabs: OAuth · Verticals · 🔍 Token Validation\n/api/config/validation-mode"]
```

**2. Extend the RuntimeSettings subgraph** (add ValidationMode alongside RSFields):
```
ValidationMode["validationModeConfig.js\nmode: introspection | jwt\nenv: VALIDATION_MODE\nruntime switchable"]
```

**3. Add new API routes** (add inside the API subgraph, after Health node):
```
ValidationRoutes["GET /api/config/validation-mode\nPOST /api/config/validation-mode\n(session auth required)"]
IntrospectionHealth["GET /api/health/introspection\nRFC 7662 connectivity probe\n→ 200 connected | 503 failed"]
```

**4. Add connections**:
- `ConfigPage -->|"GET/POST /api/config/validation-mode"| ValidationRoutes`
- `ValidationRoutes --> ValidationMode`
- `IntrospectionHealth -->|"POST dummy token\nRFC 7662 probe"| OIDC_AS`
- `AuthMW -->|"introspection mode:\nPOST /as/introspect RFC 7662"| OIDC_AS` (add to existing AuthMW connection line — make it an OR showing both paths)
- `AuthMW -->|"jwt mode:\nRFC 7519 + JWKS verify"| JWKS` (add second path)

**5. Update the Standards Legend** — add after S10:
```
S11["RFC 7662 — Token Introspection\n(configurable: introspection | jwt mode)"]
```

**6. Apply classDef** — add ValidationMode, ValidationRoutes, IntrospectionHealth to `backend` class.
Add ConfigPage to `frontend` class.

Do NOT change any existing nodes. Only add new nodes and connections.
  </action>
  <verify>
    <automated>grep -c "validationModeConfig\|ValidationMode\|IntrospectionHealth\|ValidationRoutes\|ConfigPage" architecture.mmd</automated>
  </verify>
  <acceptance_criteria>
    - architecture.mmd contains "validationModeConfig"
    - architecture.mmd contains "IntrospectionHealth"
    - architecture.mmd contains "ValidationRoutes"
    - architecture.mmd contains "ConfigPage"
    - architecture.mmd contains "introspection | jwt"
    - architecture.mmd contains "/api/health/introspection" or "introspection\nRFC 7662"
    - File still parses as valid Mermaid (no unclosed subgraph blocks, no duplicate node IDs)
  </acceptance_criteria>
  <done>architecture.mmd shows token validation as a configurable operator choice with both introspection and JWT paths visible</done>
</task>

<task type="auto">
  <name>Task 2: Update ARCHITECTURE.md — add configurable token validation section</name>
  <files>ARCHITECTURE.md</files>
  <read_first>
    - ARCHITECTURE.md (full file — find the implementation status table and standards list)
    - docs/INTROSPECTION_VALIDATION_GUIDE.md (source of truth for tradeoffs)
    - banking_api_server/config/validationModeConfig.js (confirm mode names: 'introspection', 'jwt')
  </read_first>
  <action>
Make two targeted changes to ARCHITECTURE.md:

**Change 1: Update the implementation status table** — find the row:
```
| Token Introspection (RFC 7662) on MCP server | ✅ Done |
```
Replace with two rows:
```
| Token Introspection (RFC 7662) — BFF runtime mode | ✅ Done — set `VALIDATION_MODE=introspection` (default) or toggle via Config UI |
| Token Introspection (RFC 7662) on MCP server | ✅ Done |
| Local JWT Validation (RFC 7519 + JWKS) — BFF runtime mode | ✅ Done — set `VALIDATION_MODE=jwt` or toggle via Config UI |
```

**Change 2: Add a new section after the standards table** (after the "Patterns (not single RFCs)" paragraph and before "---"):

```markdown
### 1b. Configurable Token Validation Modes (Phase 97)

The BFF (`banking_api_server`) supports **runtime-switchable** token validation. Demo operators can change the mode without restarting the server via the Config UI (Token Validation tab) or `POST /api/config/validation-mode`.

| Mode | Mechanism | Detects Revoked Tokens | Latency | Offline |
|------|-----------|----------------------|---------|---------|
| `introspection` (default) | RFC 7662 — calls PingOne `/as/introspect` per request | ✅ Yes (within cache TTL) | ~50ms cold / ~2ms cached | ❌ No |
| `jwt` | RFC 7519 + JWKS — local RS256 signature verify | ❌ No (until token expires) | ~1ms | ✅ Yes |

**Key files:**
- `banking_api_server/config/validationModeConfig.js` — in-memory mode store; seeds from `VALIDATION_MODE` env var
- `GET /api/health/introspection` — tests PingOne connectivity (RFC 7662 probe)
- `GET/POST /api/config/validation-mode` — read/change mode at runtime (session auth required)

**Full tradeoff guide:** See `docs/INTROSPECTION_VALIDATION_GUIDE.md`
```
  </action>
  <verify>
    <automated>grep -c "Configurable Token Validation\|introspection.*default\|validationModeConfig\|INTROSPECTION_VALIDATION_GUIDE" ARCHITECTURE.md</automated>
  </verify>
  <acceptance_criteria>
    - ARCHITECTURE.md contains "Configurable Token Validation Modes"
    - ARCHITECTURE.md contains "validationModeConfig.js"
    - ARCHITECTURE.md contains "introspection.*default" (regex) or the text "introspection` (default)"
    - ARCHITECTURE.md contains "INTROSPECTION_VALIDATION_GUIDE"
    - ARCHITECTURE.md contains "jwt" in the new modes table
  </acceptance_criteria>
  <done>ARCHITECTURE.md has a clear section explaining the two validation modes, their tradeoffs, and pointing to the full guide</done>
</task>

<task type="auto">
  <name>Task 3: Update README.md — reflect switchable validation modes in quick-reference table</name>
  <files>README.md</files>
  <read_first>
    - README.md (find the token validation row around line 61 and the Token Exchange section)
  </read_first>
  <action>
Make one targeted change to README.md.

**Find the comparison table row** (around line 61) that reads approximately:
```
| Token validation | PingOne AI IAM Core introspection (HTTP call) | PingOne JWKS (JWT signature) |
```

Replace with:
```
| Token validation | **Configurable:** `introspection` mode (RFC 7662, real-time, detects revoked tokens) or `jwt` mode (local JWKS verify, ~1ms, offline). Switch via Config UI → Token Validation tab or `POST /api/config/validation-mode`. Default: `introspection`. |
```

Note: this row spans a comparison table across two deployment contexts. Adjust the cell count to match surrounding rows (keep the same number of `|` delimiters).
  </action>
  <verify>
    <automated>grep -n "Configurable\|introspection.*mode\|Token Validation tab" README.md | head -5</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains "Configurable" in the token validation row
    - README.md contains "Token Validation tab"
    - README.md contains "introspection" in the token validation row
    - README.md contains "jwt" in the token validation row
  </acceptance_criteria>
  <done>README.md quick-reference table accurately reflects that token validation mode is operator-selectable</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs/diagrams | Read-only artifacts — no executable code, no trust boundary concerns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-01 | Information Disclosure | ARCHITECTURE.md, README.md | accept | Documentation accurately describes security tradeoffs (revocation gap in jwt mode) — this is intentional transparency for demo operators |
</threat_model>

<verification>
After all tasks complete, verify:

```bash
# All three files updated
grep -q "validationModeConfig" architecture.mmd && echo "✓ architecture.mmd" || echo "✗ architecture.mmd"
grep -q "Configurable Token Validation" ARCHITECTURE.md && echo "✓ ARCHITECTURE.md" || echo "✗ ARCHITECTURE.md"
grep -q "Token Validation tab" README.md && echo "✓ README.md" || echo "✗ README.md"
```
</verification>

<success_criteria>
- architecture.mmd renders the token validation mode toggle (ConfigPage, validationModeConfig, both introspection and JWT paths, new health route)
- ARCHITECTURE.md has section 1b: introspection vs JWT table with env var, UI toggle, and guide cross-reference
- README.md token validation row describes runtime-switchable modes with both options named
- npm run build in banking_api_ui still passes (docs-only change, but verify no side effects)
</success_criteria>

<output>
After completion, create `.planning/phases/98-update-diagrams-and-docs-to-reflect-new-token-validation-options-including-introspection-vs-local-jwt-selection/98-01-SUMMARY.md`
</output>
