---
phase: 99-test-local-server-and-make-sure-it-all-works
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: []

must_haves:
  truths:
    - "UI build exits 0 with no errors"
    - "Server unit tests pass (tokenIntrospection, amountValidation)"
    - "UI unit tests pass"
  artifacts:
    - path: "banking_api_ui/build/"
      provides: "Production build output"
  key_links:
    - from: "banking_api_ui/src/"
      to: "banking_api_ui/build/"
      via: "npm run build"
      pattern: "build"
---

<objective>
Automated verification that the codebase builds cleanly and all unit tests pass before manual testing.

Purpose: Fast gating — catch build breaks or broken tests before investing time in live server testing.
Output: Confirmed green build + green tests; executor captures any failures for immediate fix.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@REGRESSION_PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: UI production build</name>
  <files>banking_api_ui/build/ (generated output, not committed)</files>
  <read_first>
    - banking_api_ui/package.json (scripts and dependencies)
    - banking_api_ui/.env (env vars used at build time)
  </read_first>
  <action>
    Run the React production build and confirm it exits 0 with no errors:

    ```
    cd banking_api_ui && npm run build
    ```

    Expected: `Compiled successfully.` or `Build complete.`

    If the build fails: read the compiler error, fix any import errors / missing deps, then re-run. Do NOT skip this — a failing build means production deploy is broken.

    Common causes: unused imports that TypeScript/ESLint treats as errors, missing env vars (all REACT_APP_* are provided in .env so build should succeed).
  </action>
  <verify>
    <automated>cd banking_api_ui && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Exit code 0; build output present in `banking_api_ui/build/`</done>
</task>

<task type="auto">
  <name>Task 2: Server unit tests</name>
  <files>(no file modifications — read-only test run)</files>
  <read_first>
    - banking_api_server/package.json (test script invocation)
    - banking_api_server/__tests__/tokenIntrospection.test.js (confirms Phase 97 introspection logic)
    - banking_api_server/tests/amountValidation.test.js (confirms Phase 52 step-up logic)
  </read_first>
  <action>
    Run the full server test suite:

    ```
    cd banking_api_server && npm test -- --forceExit 2>&1
    ```

    Expected: All test suites pass, no failures.

    Key tests to watch:
    - `tokenIntrospection.test.js` — covers Phase 97 introspection mode
    - `amountValidation.test.js` — covers step-up MFA gate amounts

    If tests fail: read the failure output carefully. Do NOT add `--passWithNoTests` or skip suites. Fix the root cause.

    Note: tests run with jest --forceExit so no hanging process.
  </action>
  <verify>
    <automated>cd banking_api_server && npm test -- --forceExit 2>&1 | tail -20</automated>
  </verify>
  <done>All test suites pass (0 failures, 0 pending errors)</done>
</task>

<task type="auto">
  <name>Task 3: UI unit tests</name>
  <files>(no file modifications — read-only test run)</files>
  <read_first>
    - banking_api_ui/package.json (test scripts)
    - banking_api_ui/src/__tests__/App.session.test.js (must mock useSearchParams — see REGRESSION_PLAN.md)
  </read_first>
  <action>
    Run the React unit test suite in non-interactive mode:

    ```
    cd banking_api_ui && CI=true npm test -- --watchAll=false --forceExit 2>&1
    ```

    Expected: All tests pass.

    Known regression guard (from REGRESSION_PLAN.md): `App.session.test.js` requires `useSearchParams` to be mocked. Do NOT remove that mock if it exists — it will cause a crash.

    If any test fails: read output, fix root cause. Pay special attention to:
    - `DemoDataPage.test.js` — presenter lesson radios / Bearer probe
    - `BankingAgent.chips.test.js` — agent chip interactions
    - Snapshot tests (Header, Footer) — if snapshots are stale, update with `npm test -- --updateSnapshot`
  </action>
  <verify>
    <automated>cd banking_api_ui && CI=true npm test -- --watchAll=false --forceExit 2>&1 | tail -20</automated>
  </verify>
  <done>All UI test suites pass (0 failures)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → filesystem | Read-only; no sensitive data written |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-01 | Information Disclosure | build output | accept | Build artifacts not committed; low-value target in local dev context |
</threat_model>

<verification>
All three commands exit 0:
- `cd banking_api_ui && npm run build` → exit 0
- `cd banking_api_server && npm test -- --forceExit` → 0 failures
- `cd banking_api_ui && CI=true npm test -- --watchAll=false --forceExit` → 0 failures
</verification>

<success_criteria>
Production build succeeds and all automated tests pass. No failures, no skipped suites that hide errors.
</success_criteria>

<output>
After completion, create `.planning/phases/99-test-local-server-and-make-sure-it-all-works/99-01-SUMMARY.md`
</output>
