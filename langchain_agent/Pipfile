[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
langchain = "==0.0.353"
openai = "<1.0.0"
pydantic = "<2.0.0"
fastapi = ">=0.100.0"
uvicorn = ">=0.20.0"
websockets = ">=11.0.0"
httpx = ">=0.24.0"
aiohttp = ">=3.8.0"
cryptography = ">=41.0.0"
python-dotenv = ">=1.0.0"
pytest = ">=7.0.0"
pytest-asyncio = ">=0.21.0"
pytest-mock = ">=3.10.0"
aioresponses = ">=0.7.0"
black = ">=23.0.0"
isort = ">=5.12.0"
mypy = ">=1.0.0"
redis = ">=4.5.0"
sqlalchemy = ">=2.0.0"
alembic = ">=1.10.0"

[dev-packages]

[requires]
python_version = "3.10"
